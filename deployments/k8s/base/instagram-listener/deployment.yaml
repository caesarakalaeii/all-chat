apiVersion: apps/v1
kind: Deployment
metadata:
  name: instagram-listener
  namespace: allchat
  labels:
    app: instagram-listener
    component: listener
    platform: instagram
spec:
  # Single replica by design (ADR-0062): the live-comments poll is a per-IG-user
  # comment_id cursor; a second replica would double the Graph call count for
  # zero throughput gain. Instagram polling is light (one live-media resolve +
  # one live_comments fetch per IG user per interval), so no HPA.
  replicas: 1
  selector:
    matchLabels:
      app: instagram-listener
  template:
    metadata:
      labels:
        app: instagram-listener
        component: listener
        platform: instagram
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: instagram-listener
        image: ghcr.io/caesarakalaeii/allchat-instagram-listener:latest
        imagePullPolicy: Always
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          readOnlyRootFilesystem: true
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        ports:
        - containerPort: 8100
          name: http
        env:
        - name: PORT
          value: "8100"
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: allchat-config
              key: LOG_LEVEL
        - name: DATABASE_HOST
          valueFrom:
            configMapKeyRef:
              name: allchat-config
              key: DATABASE_HOST
        - name: DATABASE_PORT
          valueFrom:
            configMapKeyRef:
              name: allchat-config
              key: DATABASE_PORT
        - name: DATABASE_NAME
          valueFrom:
            configMapKeyRef:
              name: allchat-config
              key: DATABASE_NAME
        - name: DATABASE_USER
          valueFrom:
            configMapKeyRef:
              name: allchat-config
              key: DATABASE_USER
        - name: DATABASE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: allchat-secrets
              key: database-password
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: allchat-config
              key: REDIS_HOST
        - name: REDIS_PORT
          valueFrom:
            configMapKeyRef:
              name: allchat-config
              key: REDIS_PORT
        # Decrypts stored IG tokens (Phase 14 KeyChain V1). Live key name
        # is lowercase in allchat-secrets.
        - name: TOKEN_ENCRYPTION_KEY_V1
          valueFrom:
            secretKeyRef:
              name: allchat-secrets
              key: token-encryption-key-v1
        - name: INSTAGRAM_POLLING_INTERVAL_MS
          value: "10000"
        - name: INSTAGRAM_SOURCE_SYNC_SECONDS
          value: "30"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8100
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8100
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
      volumes:
      - name: tmp
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: instagram-listener
  namespace: allchat
  labels:
    app: instagram-listener
spec:
  type: ClusterIP
  ports:
  - port: 8100
    targetPort: 8100
    protocol: TCP
    name: http
  selector:
    app: instagram-listener
