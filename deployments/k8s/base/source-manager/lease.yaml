apiVersion: coordination.k8s.io/v1
kind: Lease
metadata:
  name: shard-coordinator
  namespace: allchat
spec:
  # Lease spec is empty - managed by leaderelection library
  # holderIdentity, leaseDurationSeconds, acquireTime, renewTime set by library
