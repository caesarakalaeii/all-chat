---
phase: quick-260328-v03
plan: 01
subsystem: observability
tags: [grafana, dashboard, prometheus, legend, metrics]
dependency_graph:
  requires: []
  provides: [readable-grafana-legends]
  affects: [allchat-listeners-dashboard]
tech_stack:
  added: []
  patterns: [sum-by-promql, grafana-legend-template-variables]
key_files:
  created: []
  modified:
    - /home/caesar/git/caesar-deployment/apps/platform/kube-prometheus-stack/dashboards/allchat-grafana-dashboards.yaml
decisions:
  - "sum by (pod) used for Twitch Published Rate — result label only has success/failed, pod name distinguishes replicas which is more actionable"
  - "sum by (message_type) used for EventSub Received Rate — channel_id is always empty for eventsub (set to empty string in RecordMessage call), message_type identifies the subscription type"
  - "{{channel_id}} template used for 3 panels (Twitch Received, InnerTube Published, Discord Received) — direct channel identifier in each metric"
  - "{{result}} used for EventSub Published — only meaningful split dimension for publish outcome"
metrics:
  duration: "~5 minutes"
  completed_date: "2026-03-28"
  tasks_completed: 1
  tasks_total: 2
  files_modified: 1
---

# Quick Task 260328-v03: Fix Grafana Dashboard Legends Summary

**One-liner:** Added `sum by (label)` PromQL groupings and Prometheus template variable legendFormat values to 6 allchat-listeners dashboard panels so time series lines show channel IDs, pod names, message types, and result values instead of generic "received/s" / "published/s" text.

## Tasks Completed

| Task | Name | Status | Commit |
|------|------|--------|--------|
| 1 | Fix PromQL queries and legendFormat values for all 6 affected panels | Complete | cc3982f (caesar-deployment) |
| 2 | Verify dashboard legends in Grafana | Needs human verification after ArgoCD sync | — |

## Changes Made

All edits are in `/home/caesar/git/caesar-deployment/apps/platform/kube-prometheus-stack/dashboards/allchat-grafana-dashboards.yaml`.

### Panel Changes

| Panel | Old expr | New expr | Old legend | New legend |
|-------|----------|----------|------------|------------|
| Twitch Messages Received Rate | `rate(listener_messages_received_total{platform="twitch"}[5m])` | `sum by (channel_id) (rate(...))` | `received/s` | `{{channel_id}}` |
| Twitch Messages Published Rate | `rate(listener_messages_published_total{platform="twitch"}[5m])` | `sum by (pod) (rate(...))` | `published/s` | `{{pod}} published/s` |
| InnerTube Messages Published Rate | `rate(youtube_listener_messages_published_total[5m])` | `sum by (channel_id) (rate(...))` | `published/s` | `{{channel_id}}` |
| Twitch EventSub Messages Rate | `rate(listener_messages_received_total{platform="twitch-eventsub"}[5m])` | `sum by (message_type) (rate(...))` | `received/s` | `{{message_type}}` |
| Twitch EventSub Messages Published Rate | `rate(listener_messages_published_total{platform="twitch-eventsub"}[5m])` | `sum by (result) (rate(...))` | `published/s` | `{{result}}` |
| Discord Messages Received Rate | `rate(discord_listener_messages_received_total[5m])` | `sum by (channel_id) (rate(...))` | `received/s` | `{{channel_id}}` |

## Verification

- Automated check passed: no bare `received/s` or `published/s` legendFormat strings remain
- `sum by` clauses present for all 6 panels
- `kubectl apply --dry-run=client` passed — YAML is syntactically valid
- Changes pushed to caesar-deployment `main` at cc3982f — ArgoCD will sync

## Task 2: Human Verification Required

After ArgoCD syncs the ConfigMap to the cluster:

1. Open Grafana and navigate to the "AllChat Listeners" dashboard
2. Check each affected panel:
   - "Twitch Messages Received Rate" — legend should show Twitch channel IDs
   - "Twitch Messages Published Rate" — legend should show pod names (e.g., `twitch-listener-abc123`)
   - "InnerTube Messages Published Rate" — legend should show YouTube channel IDs
   - "Twitch EventSub Messages Rate" — legend should show event types (e.g., `channel.channel_points_custom_reward_redemption.add`)
   - "Twitch EventSub Messages Published Rate" — legend should show `success` or `error`
   - "Discord Messages Received Rate" — legend should show Discord channel IDs
3. Verify time series lines are distinguishable in the graphs

## Deviations from Plan

None - plan executed exactly as written.

## Self-Check: PASSED

- File modified: `/home/caesar/git/caesar-deployment/apps/platform/kube-prometheus-stack/dashboards/allchat-grafana-dashboards.yaml` — confirmed exists and modified
- Commit `cc3982f` exists in caesar-deployment repo — confirmed via `git rev-parse --short HEAD`
- kubectl dry-run passed — YAML valid
- Verification script passed — no generic legends remain, sum by clauses present
