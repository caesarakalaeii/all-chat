---
phase: quick-260328-v03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/caesar/git/caesar-deployment/apps/platform/kube-prometheus-stack/dashboards/allchat-grafana-dashboards.yaml
autonomous: false
requirements: [fix-grafana-legends]
must_haves:
  truths:
    - "Twitch Messages Received Rate panel shows one legend line per channel_id instead of a single generic 'received/s'"
    - "Twitch Messages Published Rate panel shows one legend line per pod instead of a single generic 'published/s'"
    - "InnerTube Messages Published Rate panel shows one legend line per channel_id instead of 'published/s'"
    - "Twitch EventSub Messages Rate panel shows one legend line per message_type instead of 'received/s'"
    - "Twitch EventSub Messages Published Rate panel shows one legend line per result instead of 'published/s'"
    - "Discord Messages Received Rate panel shows one legend line per channel_id instead of 'received/s'"
  artifacts:
    - path: "/home/caesar/git/caesar-deployment/apps/platform/kube-prometheus-stack/dashboards/allchat-grafana-dashboards.yaml"
      provides: "Updated Grafana dashboard ConfigMap with meaningful legendFormat values"
  key_links:
    - from: "allchat-grafana-dashboards.yaml"
      to: "Prometheus metric labels"
      via: "PromQL by() clauses and legendFormat template variables"
---

<objective>
Fix Grafana dashboard legend labels across 6 panels in the allchat-listeners dashboard that currently show generic "received/s" or "published/s" text. Replace with meaningful labels derived from Prometheus metric label dimensions.

Purpose: Dashboard panels are unreadable when multiple time series appear — they all show the same generic legend text, making it impossible to distinguish which channel, pod, or event type each line represents.

Output: Updated `allchat-grafana-dashboards.yaml` ConfigMap with corrected PromQL queries and legendFormat values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/caesar/git/caesar-deployment/apps/platform/kube-prometheus-stack/dashboards/allchat-grafana-dashboards.yaml

The dashboard file is a Kubernetes ConfigMap containing 5 JSON dashboard definitions as inline YAML string values. The affected panels are in the `allchat-listeners.json` dashboard.

<interfaces>
<!-- Available Prometheus metric labels (from Go service code) -->

From shared/metrics/listener.go — listener_messages_received_total:
  Labels: ["platform", "service", "channel_id", "message_type"]

From shared/metrics/listener.go — listener_messages_published_total:
  Labels: ["platform", "service", "result"]

From services/youtube-listener-innertube/metrics/innertube_metrics.go — youtube_listener_messages_published_total:
  Labels: ["service", "channel_id"]

From services/discord-listener/metrics/metrics.go — discord_listener_messages_received_total:
  Labels: ["guild_id", "channel_id"]

From services/discord-listener/metrics/metrics.go — discord_listener_messages_published_total:
  Labels: ["result"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PromQL queries and legendFormat values for all 6 affected panels</name>
  <files>/home/caesar/git/caesar-deployment/apps/platform/kube-prometheus-stack/dashboards/allchat-grafana-dashboards.yaml</files>
  <action>
Edit the `allchat-listeners.json` dashboard definition inside the ConfigMap. For each of the 6 affected panels, add a `sum by (label)` grouping to the PromQL expr and update legendFormat to use the Prometheus template variable.

The JSON is embedded as escaped strings in YAML, so edits must preserve the escaping (literal `\n` for newlines, escaped quotes, etc.).

Panel fixes (find each by its title string in the JSON):

1. **"Twitch Messages Received Rate"** panel:
   - Current expr: `rate(listener_messages_received_total{platform=\"twitch\"}[5m])`
   - New expr: `sum by (channel_id) (rate(listener_messages_received_total{platform=\"twitch\"}[5m]))`
   - Current legendFormat: `received/s`
   - New legendFormat: `{{channel_id}}`

2. **"Twitch Messages Published Rate"** panel:
   - Current expr: `rate(listener_messages_published_total{platform=\"twitch\"}[5m])`
   - New expr: `sum by (pod) (rate(listener_messages_published_total{platform=\"twitch\"}[5m]))`
   - Current legendFormat: `published/s`
   - New legendFormat: `{{pod}} published/s`
   - Rationale: `result` label only has "success"/"failed"; pod name is more useful for distinguishing replicas. The `pod` label is auto-added by Prometheus ServiceMonitor scraping.

3. **"InnerTube Messages Published Rate"** panel:
   - Current expr: `rate(youtube_listener_messages_published_total[5m])`
   - New expr: `sum by (channel_id) (rate(youtube_listener_messages_published_total[5m]))`
   - Current legendFormat: `published/s`
   - New legendFormat: `{{channel_id}}`

4. **"Twitch EventSub Messages Rate"** panel:
   - Current expr: `rate(listener_messages_received_total{platform=\"twitch-eventsub\"}[5m])`
   - New expr: `sum by (message_type) (rate(listener_messages_received_total{platform=\"twitch-eventsub\"}[5m]))`
   - Current legendFormat: `received/s`
   - New legendFormat: `{{message_type}}`
   - Rationale: EventSub receives different notification types (channel.channel_points_custom_reward_redemption.add, etc.) — message_type distinguishes them. channel_id is always empty for eventsub (see handler.go line: RecordMessage("twitch-eventsub", "twitch-eventsub-listener", "", notification.Subscription.Type)).

5. **"Twitch EventSub Messages Published Rate"** panel:
   - Current expr: `rate(listener_messages_published_total{platform=\"twitch-eventsub\"}[5m])`
   - New expr: `sum by (result) (rate(listener_messages_published_total{platform=\"twitch-eventsub\"}[5m]))`
   - Current legendFormat: `published/s`
   - New legendFormat: `{{result}}`

6. **"Discord Messages Received Rate"** panel:
   - Current expr: `rate(discord_listener_messages_received_total[5m])`
   - New expr: `sum by (channel_id) (rate(discord_listener_messages_received_total[5m]))`
   - Current legendFormat: `received/s`
   - New legendFormat: `{{channel_id}}`

IMPORTANT: The file uses escaped JSON inside YAML double-quoted strings. All `"` inside the JSON are `\"`, all newlines are `\n`. Make sure edits preserve this encoding. Use sed or Python to make the replacements safely. Do NOT reformat or re-serialize the entire file — make targeted string replacements only.
  </action>
  <verify>
    <automated>cd /home/caesar/git/caesar-deployment && python3 -c "
import re
with open('apps/platform/kube-prometheus-stack/dashboards/allchat-grafana-dashboards.yaml') as f:
    content = f.read()
# Verify no more bare 'received/s' or 'published/s' legendFormats exist
bare_legends = re.findall(r'legendFormat.*?\"(received/s|published/s)\"', content)
assert len(bare_legends) == 0, f'Still found generic legends: {bare_legends}'
# Verify sum by clauses were added
assert 'sum by' in content, 'No sum by clauses found'
print('All 6 panels updated successfully — no generic legends remain')
"</automated>
  </verify>
  <done>All 6 affected panels have PromQL queries with appropriate `sum by (label)` groupings and legendFormat values using Prometheus template variables instead of static strings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dashboard legends in Grafana</name>
  <files>none</files>
  <action>Human verifies the updated dashboard panels show meaningful legends in Grafana after ArgoCD syncs the ConfigMap.</action>
  <what-built>Updated Grafana dashboard legends for 6 panels in the allchat-listeners dashboard. Changed generic "received/s" and "published/s" legends to show meaningful metric dimensions (channel_id, pod, message_type, result).</what-built>
  <how-to-verify>
    1. Commit and push the changes to caesar-deployment (ArgoCD will sync the ConfigMap)
    2. Open Grafana and navigate to the "AllChat Listeners" dashboard
    3. Check each affected panel:
       - "Twitch Messages Received Rate" — legend should show channel IDs (Twitch channel names/IDs)
       - "Twitch Messages Published Rate" — legend should show pod names
       - "InnerTube Messages Published Rate" — legend should show YouTube channel IDs
       - "Twitch EventSub Messages Rate" — legend should show event types
       - "Twitch EventSub Messages Published Rate" — legend should show "success"/"error"
       - "Discord Messages Received Rate" — legend should show Discord channel IDs
    4. Verify the time series lines are distinguishable in the graph
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- No bare `received/s` or `published/s` legendFormat strings remain in the dashboard ConfigMap
- All 6 affected panels have `sum by (label)` in their PromQL expressions
- YAML file is syntactically valid (kubectl apply --dry-run=client passes)
- Dashboard renders correctly in Grafana after ArgoCD sync
</verification>

<success_criteria>
All 6 Grafana dashboard panels show meaningful, distinguishable legend labels instead of generic "received/s" and "published/s" text.
</success_criteria>

<output>
After completion, create `.planning/quick/260328-v03-fix-grafana-dashboard-legends-to-show-me/260328-v03-SUMMARY.md`
</output>
