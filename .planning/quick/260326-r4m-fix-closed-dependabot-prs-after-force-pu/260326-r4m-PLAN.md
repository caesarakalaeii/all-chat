---
phase: quick-260326-r4m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/go.mod
  - shared/go.sum
  - services/api-gateway/go.mod
  - services/api-gateway/go.sum
  - services/auth-service/go.mod
  - services/auth-service/go.sum
  - services/discord-listener/go.mod
  - services/discord-listener/go.sum
  - services/emote-service/go.mod
  - services/emote-service/go.sum
  - services/kick-listener/go.mod
  - services/kick-listener/go.sum
  - services/message-processor/go.mod
  - services/message-processor/go.sum
  - services/overlay-manager/go.mod
  - services/overlay-manager/go.sum
  - services/share-service/go.mod
  - services/share-service/go.sum
  - services/source-manager/go.mod
  - services/source-manager/go.sum
  - services/token-refresh-service/go.mod
  - services/token-refresh-service/go.sum
  - services/twitch-eventsub-listener/go.mod
  - services/twitch-eventsub-listener/go.sum
  - services/twitch-listener/go.mod
  - services/twitch-listener/go.sum
  - services/youtube-listener/go.mod
  - services/youtube-listener/go.sum
  - services/youtube-listener-innertube/go.mod
  - services/youtube-listener-innertube/go.sum
  - frontend/package.json
  - frontend/package-lock.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "All Go modules build cleanly with updated dependencies"
    - "Frontend builds cleanly with updated npm packages"
    - "No dependabot PRs are needed for the listed packages after this update"
  artifacts:
    - path: "shared/go.sum"
      provides: "Updated shared module dependency lock"
    - path: "frontend/package-lock.json"
      provides: "Updated frontend dependency lock"
  key_links:
    - from: "services/*/go.mod"
      to: "shared/go.mod"
      via: "replace directive"
      pattern: "replace.*allchat/shared"
---

<objective>
Restore all dependency updates that were in the 20+ closed dependabot PRs, applying them directly to the working tree.

Purpose: Force push to main closed all open dependabot PRs and their branches. Dependabot will eventually re-open new PRs, but the updates are known and safe to apply now directly.
Output: Updated go.mod/go.sum for all 15 Go service modules + shared module, updated package.json/package-lock.json for frontend.
</objective>

<execution_context>
@/home/moersener/.claude/get-shit-done/workflows/execute-plan.md
@/home/moersener/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all Go module dependencies</name>
  <files>
    shared/go.mod, shared/go.sum,
    services/api-gateway/go.mod, services/api-gateway/go.sum,
    services/auth-service/go.mod, services/auth-service/go.sum,
    services/discord-listener/go.mod, services/discord-listener/go.sum,
    services/emote-service/go.mod, services/emote-service/go.sum,
    services/kick-listener/go.mod, services/kick-listener/go.sum,
    services/message-processor/go.mod, services/message-processor/go.sum,
    services/overlay-manager/go.mod, services/overlay-manager/go.sum,
    services/share-service/go.mod, services/share-service/go.sum,
    services/source-manager/go.mod, services/source-manager/go.sum,
    services/token-refresh-service/go.mod, services/token-refresh-service/go.sum,
    services/twitch-eventsub-listener/go.mod, services/twitch-eventsub-listener/go.sum,
    services/twitch-listener/go.mod, services/twitch-listener/go.sum,
    services/youtube-listener/go.mod, services/youtube-listener/go.sum,
    services/youtube-listener-innertube/go.mod, services/youtube-listener-innertube/go.sum
  </files>
  <action>
    Run `go get -u ./... && go mod tidy` in each module directory in this order:
    1. shared/ (run first — other services may use replace directives pointing here)
    2. Each service directory in alphabetical order: api-gateway, auth-service, discord-listener, emote-service, kick-listener, message-processor, overlay-manager, share-service, source-manager, token-refresh-service, twitch-eventsub-listener, twitch-listener, youtube-listener, youtube-listener-innertube

    For each module:
    ```
    cd /home/moersener/Hobby/all-chat/{module-path}
    go get -u ./...
    go mod tidy
    ```

    After all modules are updated, verify the whole codebase builds:
    ```
    cd /home/moersener/Hobby/all-chat
    make build-all
    ```

    If `make build-all` does not exist, compile each service individually:
    ```
    cd /home/moersener/Hobby/all-chat/{service-path}
    go build ./...
    ```

    Key packages that should be updated by this (from closed dependabot PRs):
    - github.com/jackc/pgx/v5 → 5.9.1 (9 services)
    - google.golang.org/grpc → 1.79.3 (youtube-listener)
    - google.golang.org/api → 0.272.0 (overlay-manager, youtube-listener)
    - k8s.io/apimachinery → 0.35.3, k8s.io/client-go → 0.35.3, k8s.io/api → 0.35.3 (source-manager)

    Do NOT update github.com/nareix/joy4 or any package that is pinned with a comment explaining why — check go.mod comments before running go get -u on those.
  </action>
  <verify>
    <automated>cd /home/moersener/Hobby/all-chat && make build-all 2>/dev/null || (for d in shared services/api-gateway services/auth-service services/discord-listener services/emote-service services/kick-listener services/message-processor services/overlay-manager services/share-service services/source-manager services/token-refresh-service services/twitch-eventsub-listener services/twitch-listener services/youtube-listener services/youtube-listener-innertube; do echo "=== $d ===" && cd /home/moersener/Hobby/all-chat/$d && go build ./... && cd /home/moersener/Hobby/all-chat; done)</automated>
  </verify>
  <done>All Go services compile without errors. go.mod/go.sum updated in all 15 service modules and shared/.</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend npm dependencies</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <action>
    Update the frontend dependencies that were in closed dependabot PRs.

    Run targeted updates for the specific packages identified from closed PRs:
    ```
    cd /home/moersener/Hobby/all-chat/frontend
    npm update next @shadcn/ui vite @vitejs/plugin-react
    ```

    Then run a full `npm update` to catch the 14 dev dependency group updates and testing group:
    ```
    npm update
    ```

    Then verify the build:
    ```
    npm run build
    ```

    Target versions from closed PRs:
    - next: 16.1.6 → 16.2.1
    - shadcn: 4.0.8 → 4.1.0
    - vite: 7.3.1 → 8.0.1
    - @vitejs/plugin-react: 5.1.4 → 6.0.1

    Note: `npm update` respects semver ranges in package.json. If vite 8.0.1 or @vitejs/plugin-react 6.0.1 is a major bump outside the declared range, use `npm install vite@8 @vitejs/plugin-react@6` explicitly for those packages. Check package.json ranges before running.
  </action>
  <verify>
    <automated>cd /home/moersener/Hobby/all-chat/frontend && npm run build</automated>
  </verify>
  <done>Frontend builds successfully. package.json and package-lock.json reflect updated dependency versions.</done>
</task>

<task type="auto">
  <name>Task 3: Trigger dependabot refresh via GitHub API</name>
  <files></files>
  <action>
    After all dependency updates are committed, trigger dependabot to re-evaluate the repo so it creates new PRs for any remaining updates (and stops creating PRs for what we just updated).

    Use the GitHub CLI to trigger a dependabot check:
    ```
    gh api --method POST repos/moersener/all-chat/automated-security-fixes
    ```

    If that endpoint is not available, use the dependabot endpoints:
    ```
    gh api repos/moersener/all-chat/dependabot/alerts
    ```

    To verify the repo owner/name:
    ```
    gh repo view --json nameWithOwner
    ```

    The main outcome of this task is confirming dependabot is healthy and will create new PRs on its configured schedule. Since dependabot runs on a schedule (configured in .github/dependabot.yml), there is no way to force it to run immediately via the API — just confirm the config is intact:
    ```
    cat /home/moersener/Hobby/all-chat/.github/dependabot.yml
    ```

    Document the result in the summary: dependabot will naturally re-run on its next scheduled check and find no remaining outdated packages for what we updated, or create PRs for any packages with new releases since.
  </action>
  <verify>
    <automated>cat /home/moersener/Hobby/all-chat/.github/dependabot.yml && gh repo view --json nameWithOwner</automated>
  </verify>
  <done>Dependabot config confirmed present and valid. No action needed beyond the dependency updates already applied — dependabot will self-heal on next scheduled run.</done>
</task>

</tasks>

<verification>
- `go build ./...` passes in all 15 service directories and shared/
- `npm run build` passes in frontend/
- `git diff --stat` shows go.mod, go.sum, package.json, package-lock.json changes across services
- `gh repo view --json nameWithOwner` confirms repo identity for dependabot health check
</verification>

<success_criteria>
- All Go services compile with updated dependencies (pgx 5.9.1, grpc 1.79.3, k8s 0.35.3 where applicable)
- Frontend builds with updated next, vite, shadcn, and dev dependencies
- Dependabot config confirmed intact — will self-heal on next schedule
</success_criteria>

<output>
After completion, create `.planning/quick/260326-r4m-fix-closed-dependabot-prs-after-force-pu/260326-r4m-SUMMARY.md`
</output>
