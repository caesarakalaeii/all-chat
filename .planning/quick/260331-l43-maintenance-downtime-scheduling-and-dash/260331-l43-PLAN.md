---
phase: quick
plan: 260331-l43
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/045_maintenance_windows.sql
  - services/overlay-manager/models/maintenance.go
  - services/overlay-manager/repository/maintenance_repo.go
  - services/overlay-manager/handlers/maintenance.go
  - services/overlay-manager/cmd/main.go
  - services/api-gateway/cmd/main.go
  - services/api-gateway/models/service_config.go
  - frontend/src/lib/types/maintenance.ts
  - frontend/src/lib/api/maintenance.ts
  - frontend/src/app/admin/maintenance/page.tsx
  - frontend/src/components/MaintenanceBanner.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/AdminNav.tsx
autonomous: true
requirements: [QUICK-L43]

must_haves:
  truths:
    - "Admins can create scheduled maintenance windows with title, description, start/end times"
    - "Admins can view and delete upcoming maintenance windows"
    - "All authenticated users see a banner on the dashboard for upcoming/active maintenance"
    - "Past maintenance windows are hidden from the user-facing banner"
  artifacts:
    - path: "migrations/045_maintenance_windows.sql"
      provides: "maintenance_windows table"
      contains: "CREATE TABLE"
    - path: "services/overlay-manager/handlers/maintenance.go"
      provides: "Admin CRUD endpoints for maintenance windows"
      exports: ["MaintenanceHandler"]
    - path: "frontend/src/app/admin/maintenance/page.tsx"
      provides: "Admin UI for scheduling maintenance"
    - path: "frontend/src/components/MaintenanceBanner.tsx"
      provides: "User-facing banner component"
  key_links:
    - from: "frontend/src/app/admin/maintenance/page.tsx"
      to: "/api/v1/admin/maintenance"
      via: "apiClient calls"
      pattern: "apiClient\\.(get|post|delete)"
    - from: "frontend/src/components/MaintenanceBanner.tsx"
      to: "/api/v1/maintenance/upcoming"
      via: "apiClient.get in useEffect"
      pattern: "apiClient\\.get.*maintenance"
    - from: "services/api-gateway/cmd/main.go"
      to: "overlay-manager"
      via: "proxy route registration"
      pattern: "admin/maintenance"
---

<objective>
Create a maintenance/downtime notification system. Admins schedule downtime windows via an admin UI page, and all authenticated users see upcoming maintenance notifications as a banner on the dashboard.

Purpose: Proactive user communication before planned downtimes — reduces confusion and support load.
Output: Database table, REST API (admin CRUD + public upcoming), admin page, dashboard banner component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@services/overlay-manager/cmd/main.go
@services/overlay-manager/handlers/admin.go
@services/overlay-manager/repository/overlay_repo.go
@services/api-gateway/cmd/main.go
@services/api-gateway/models/service_config.go
@frontend/src/app/admin/page.tsx
@frontend/src/app/admin/layout.tsx
@frontend/src/components/AdminNav.tsx
@frontend/src/app/dashboard/page.tsx
@frontend/src/lib/api/client.ts
@migrations/044_feature_gates.sql

<interfaces>
<!-- Existing patterns the executor must follow -->

Admin handler pattern (from services/overlay-manager/handlers/admin.go):
- Handler struct with repository deps + zap.Logger
- Constructor NewXxxHandler(...)
- Methods on handler struct with (c *gin.Context)
- JSON responses via c.JSON()

Admin route registration (from services/overlay-manager/cmd/main.go):
- admin group: router.Group("/admin") with JWTAuth + AdminOnly middleware
- Routes registered inside admin group block

API Gateway proxy pattern (from services/api-gateway/cmd/main.go):
- protectedAPI.GET/POST/DELETE("/admin/maintenance...", proxyHandler.ForwardRequest)
- ServiceConfig entry in service_config.go for path rewriting

Frontend API pattern (from frontend/src/lib/api/client.ts):
- apiClient.get<T>('/api/v1/...'), apiClient.post<T>('/api/v1/...', data)
- Module per domain (e.g., frontend/src/lib/api/maintenance.ts)

Admin UI pattern (from frontend/src/app/admin/page.tsx):
- 'use client' pages under /admin/ route
- Uses Card, Button, Skeleton, Dialog from @/components/ui/
- toastManager.add({ title, type }) for notifications
- cn() from @/lib/utils for conditional classNames
- Tailwind classes matching existing admin pages (bg-bg, text-text, text-text-sub, etc.)

AdminNav links array (from frontend/src/components/AdminNav.tsx):
- ADMIN_LINKS array: { href, label, exact? }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration + overlay-manager backend (model, repository, handler, route wiring)</name>
  <files>
    migrations/045_maintenance_windows.sql
    services/overlay-manager/models/maintenance.go
    services/overlay-manager/repository/maintenance_repo.go
    services/overlay-manager/handlers/maintenance.go
    services/overlay-manager/cmd/main.go
    services/api-gateway/cmd/main.go
    services/api-gateway/models/service_config.go
  </files>
  <action>
    1. Create migration `migrations/045_maintenance_windows.sql`:
       - CREATE TABLE IF NOT EXISTS maintenance_windows (
           id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
           title VARCHAR(200) NOT NULL,
           description TEXT NOT NULL DEFAULT '',
           starts_at TIMESTAMP WITH TIME ZONE NOT NULL,
           ends_at TIMESTAMP WITH TIME ZONE NOT NULL,
           created_by VARCHAR(100) NOT NULL,
           created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
         );
       - CREATE INDEX idx_maintenance_windows_ends_at ON maintenance_windows (ends_at);
       - Add COMMENT ON TABLE explaining purpose.

    2. Create `services/overlay-manager/models/maintenance.go`:
       - MaintenanceWindow struct with json tags matching API response (id, title, description, starts_at, ends_at, created_by, created_at).
       - CreateMaintenanceRequest struct with binding:"required" on title, starts_at, ends_at. Description optional.

    3. Create `services/overlay-manager/repository/maintenance_repo.go`:
       - MaintenanceRepository struct holding *pgxpool.Pool.
       - NewMaintenanceRepository(pool *pgxpool.Pool) constructor.
       - Create(ctx, req CreateMaintenanceRequest, createdBy string) (*MaintenanceWindow, error) — INSERT RETURNING *.
       - ListAll(ctx) ([]MaintenanceWindow, error) — SELECT * ORDER BY starts_at ASC.
       - ListUpcoming(ctx) ([]MaintenanceWindow, error) — SELECT * WHERE ends_at > NOW() ORDER BY starts_at ASC.
       - Delete(ctx, id string) error — DELETE WHERE id = $1.

    4. Create `services/overlay-manager/handlers/maintenance.go`:
       - MaintenanceHandler struct with maintenanceRepo + logger.
       - NewMaintenanceHandler(repo, logger) constructor.
       - HandleCreateMaintenance: POST, bind JSON CreateMaintenanceRequest, validate starts_at < ends_at, call repo.Create with user_id from JWT context ("user_id" key set by JWTAuth middleware), return 201.
       - HandleListMaintenance: GET, call repo.ListAll, return 200 with array.
       - HandleDeleteMaintenance: DELETE /:id, call repo.Delete, return 204.
       - HandleListUpcoming: GET, call repo.ListUpcoming, return 200 with array. This endpoint is for non-admin users.

    5. Wire in `services/overlay-manager/cmd/main.go`:
       - Create maintenanceRepo := repository.NewMaintenanceRepository(dbPool)
       - Create maintenanceHandler := handlers.NewMaintenanceHandler(maintenanceRepo, log)
       - Add admin routes inside existing admin group block:
         admin.POST("/maintenance", maintenanceHandler.HandleCreateMaintenance)
         admin.GET("/maintenance", maintenanceHandler.HandleListMaintenance)
         admin.DELETE("/maintenance/:id", maintenanceHandler.HandleDeleteMaintenance)
       - Add public (JWT-protected but not admin-only) route inside the existing protected group:
         protected.GET("/maintenance/upcoming", maintenanceHandler.HandleListUpcoming)

    6. Wire API Gateway proxy in `services/api-gateway/cmd/main.go`:
       - Add three admin routes to the protectedAPI group (near existing admin/overlays lines):
         protectedAPI.POST("/admin/maintenance", proxyHandler.ForwardRequest)       // -> overlay-manager
         protectedAPI.GET("/admin/maintenance", proxyHandler.ForwardRequest)        // -> overlay-manager
         protectedAPI.DELETE("/admin/maintenance/:id", proxyHandler.ForwardRequest) // -> overlay-manager
       - Add one user-facing route in protectedAPI:
         protectedAPI.GET("/maintenance/upcoming", proxyHandler.ForwardRequest)     // -> overlay-manager

    7. Add ServiceConfig entry in `services/api-gateway/models/service_config.go`:
       - "admin-maintenance" config pointing to overlayURL with PathPrefix "/api/v1/admin/maintenance", RewritePrefix "/admin/maintenance".
       - "maintenance-upcoming" config pointing to overlayURL with PathPrefix "/api/v1/maintenance/upcoming", RewritePrefix "/maintenance/upcoming".

    Note: Use proper Go typing throughout — never use `any`. Follow existing handler/repo patterns exactly (zap.Logger, pgxpool.Pool, gin.Context).
  </action>
  <verify>
    <automated>cd /home/moersener/Hobby/all-chat && make build-all 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Migration file exists with correct schema
    - overlay-manager compiles with new maintenance CRUD endpoints
    - api-gateway compiles with proxy routes for admin/maintenance and maintenance/upcoming
    - All 4 endpoints wired: POST/GET/DELETE admin + GET upcoming
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — admin maintenance page + dashboard banner</name>
  <files>
    frontend/src/lib/types/maintenance.ts
    frontend/src/lib/api/maintenance.ts
    frontend/src/app/admin/maintenance/page.tsx
    frontend/src/components/MaintenanceBanner.tsx
    frontend/src/app/dashboard/page.tsx
    frontend/src/components/AdminNav.tsx
  </files>
  <action>
    1. Create `frontend/src/lib/types/maintenance.ts`:
       - MaintenanceWindow interface: { id: string; title: string; description: string; starts_at: string; ends_at: string; created_by: string; created_at: string }
       - CreateMaintenanceRequest interface: { title: string; description?: string; starts_at: string; ends_at: string }

    2. Create `frontend/src/lib/api/maintenance.ts`:
       - import { apiClient } from './client' and types
       - maintenanceApi object with:
         - list(): apiClient.get<MaintenanceWindow[]>('/api/v1/admin/maintenance')
         - create(data: CreateMaintenanceRequest): apiClient.post<MaintenanceWindow>('/api/v1/admin/maintenance', data)
         - remove(id: string): apiClient.delete(`/api/v1/admin/maintenance/${id}`)
         - upcoming(): apiClient.get<MaintenanceWindow[]>('/api/v1/maintenance/upcoming')

    3. Create `frontend/src/app/admin/maintenance/page.tsx`:
       - 'use client' page
       - State: maintenances (MaintenanceWindow[]), loading boolean, showCreate dialog boolean
       - Form fields: title (required, text input), description (optional, textarea), starts_at (datetime-local input), ends_at (datetime-local input)
       - On submit: call maintenanceApi.create(), refresh list, close dialog, show success toast via toastManager.add({ title: 'Maintenance scheduled', type: 'success' })
       - Display list as rows/cards showing title, time range (formatted nicely), description, delete button (Trash2 icon)
       - Delete: confirm via window.confirm(), call maintenanceApi.remove(), refresh list, show toast
       - Use existing UI components: Card, Button, Dialog, Skeleton, Input from @/components/ui/
       - Use cn() from @/lib/utils for conditional classNames
       - Follow existing admin page styling: max-w-7xl, px-4 py-8, text-text, text-text-sub, bg-bg conventions
       - Show time range with Intl.DateTimeFormat for locale-aware formatting
       - Color-code: active maintenance (starts_at <= now <= ends_at) with amber/yellow accent, future with blue accent

    4. Create `frontend/src/components/MaintenanceBanner.tsx`:
       - 'use client' component
       - Fetches upcoming maintenance windows from maintenanceApi.upcoming() on mount
       - If no upcoming windows, renders null (no banner)
       - For each upcoming window, render a banner at top of content:
         - Amber/yellow background for active (starts_at <= now <= ends_at): "Maintenance in progress: {title} — Expected completion: {ends_at formatted}"
         - Blue background for upcoming (starts_at > now): "Scheduled maintenance: {title} — {starts_at formatted} to {ends_at formatted}"
       - Include a dismiss button (X icon) per banner that hides it for the session (useState)
       - Use Wrench or AlertTriangle icon from lucide-react
       - Tailwind styling: rounded-lg, px-4 py-3, flex items-center gap-3, text-sm

    5. Add MaintenanceBanner to `frontend/src/app/dashboard/page.tsx`:
       - Import MaintenanceBanner from '@/components/MaintenanceBanner'
       - Place it inside the dashboard page content, after the AppNav and before the overlay grid
       - Wrap in a div with mb-4 for spacing

    6. Add "Maintenance" link to AdminNav in `frontend/src/components/AdminNav.tsx`:
       - Add { href: '/admin/maintenance', label: 'Maintenance' } to ADMIN_LINKS array (after 'Features')
  </action>
  <verify>
    <automated>cd /home/moersener/Hobby/all-chat/frontend && npx next lint 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Admin maintenance page renders at /admin/maintenance with create/list/delete functionality
    - MaintenanceBanner component fetches and displays upcoming maintenance on dashboard
    - AdminNav includes "Maintenance" link
    - TypeScript compiles without errors, ESLint passes
  </done>
</task>

</tasks>

<verification>
1. `make build-all` passes (Go services compile)
2. `cd frontend && npx tsc --noEmit` passes (TypeScript compiles)
3. `cd frontend && npx next lint` passes (ESLint clean)
4. Migration file exists at migrations/045_maintenance_windows.sql
5. API Gateway routes registered for admin/maintenance and maintenance/upcoming
</verification>

<success_criteria>
- Admins can schedule maintenance windows with title, description, start/end time via /admin/maintenance
- Admins can view all scheduled windows and delete them
- Authenticated users see a dismissible banner on the dashboard for upcoming/active maintenance
- Past maintenance windows (ends_at < now) are hidden from the user-facing banner
- All code compiles and lints cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260331-l43-maintenance-downtime-scheduling-and-dash/260331-l43-SUMMARY.md`
</output>
