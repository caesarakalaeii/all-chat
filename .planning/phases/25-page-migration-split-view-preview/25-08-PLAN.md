---
phase: 25-page-migration-split-view-preview
plan: 08
type: execute
wave: 6
depends_on:
  - 25-02
  - 25-03
  - 25-04
  - 25-05
  - 25-06
  - 25-07
files_modified: []
autonomous: false
requirements:
  - PAGE-04
  - PAGE-07
  - PAGE-08
  - FEAT-03
  - FEAT-04

must_haves:
  truths:
    - "Landing page renders correctly at 375px, 768px, and 1920px viewports"
    - "Dashboard overlay grid stacks to 1 column at 375px"
    - "Split-view stacks vertically at 375px and shows side-by-side at 1920px"
    - "Overlay preview page (marketplace CSS) is unchanged and renders correctly"
    - "All Storybook stories pass a11y audit in error mode (zero violations)"
    - "No gray-scale classes remain anywhere in frontend/src/app"
  artifacts:
    - path: "frontend/src/app/overlays/[id]/preview/page.tsx"
      provides: "Preserved overlay preview page — must remain byte-for-byte identical to pre-phase-25 state"
  key_links:
    - from: "human verification"
      to: "frontend/src/app/"
      via: "browser visual inspection at multiple viewport sizes"
      pattern: "responsive"
---

<objective>
Final human visual verification of all page migrations, responsive layouts, and marketplace CSS preservation.

Purpose: Automated tests can validate logic and a11y attributes, but viewport-dependent layout behavior (responsive grid stacking, split-view mobile/desktop modes) and marketplace theme compatibility require visual inspection in a real browser. This plan is the phase completion gate.

Output: Human sign-off on visual quality, responsive layouts, and overlay preview page integrity. Automated grey-class audit passes. Storybook a11y green.
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-page-migration-split-view-preview/25-CONTEXT.md
@.planning/phases/25-page-migration-split-view-preview/25-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated quality gates — gray class audit + build verification</name>
  <files>frontend/src/app/</files>
  <action>
Run automated checks to catch any remaining legacy patterns before human verification:

1. **Gray class audit** — verify no gray-scale classes remain in migrated pages:
   ```bash
   cd /home/caesar/git/all-chat/frontend
   grep -rn "bg-gray-\|text-gray-\|border-gray-" src/app/ --include="*.tsx" --include="*.ts"
   ```
   Expected: 0 results (any remaining results are bugs to fix before proceeding).

2. **alert()/window.confirm() audit:**
   ```bash
   grep -rn "window\.confirm\|window\.alert\|alert(" src/app/ --include="*.tsx"
   ```
   Expected: 0 results.

3. **notification state audit:**
   ```bash
   grep -rn "setNotification\|notification.*useState" src/app/ --include="*.tsx"
   ```
   Expected: 0 results.

4. **Preview page unchanged audit:**
   ```bash
   git diff HEAD -- frontend/src/app/overlays/\[id\]/preview/page.tsx
   ```
   Expected: empty output (no changes to preview page).

5. **events.css unchanged audit:**
   ```bash
   git diff HEAD -- frontend/src/styles/events.css
   ```
   Expected: empty output (no changes to events.css — stability contract).

6. **Full build:**
   ```bash
   cd /home/caesar/git/all-chat/frontend && npm run build
   ```
   Expected: exits 0 with no errors.

Fix any failures before proceeding to the checkpoint task. These are automated — Claude executes them.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/frontend && npm run build 2>&1 | tail -5 && echo "BUILD_OK" && grep -rn "bg-gray-\|window\.confirm\|setNotification" src/app/ --include="*.tsx" 2>/dev/null | wc -l | xargs -I{} sh -c 'if [ {} -eq 0 ]; then echo "AUDIT_CLEAN"; else echo "AUDIT_FAILED: {} legacy patterns remain"; fi'</automated>
  </verify>
  <done>Build exits 0, gray class audit returns 0 results, window.confirm audit returns 0 results, notification state audit returns 0 results, preview page and events.css show no git diff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — responsive layouts, split-view, and overlay preview</name>
  <files>frontend/src/app/</files>
  <action>Human visual verification required — see how-to-verify instructions below.</action>
  <verify>
    <automated>MISSING — manual verification required for responsive layouts and marketplace CSS (PAGE-07, FEAT-03, FEAT-04)</automated>
  </verify>
  <done>Human approves visual quality at all viewport sizes, split-view functional, overlay preview unchanged, Storybook a11y passes.</done>
  <what-built>
All 6 application pages migrated to design system (landing, dashboard, overlay editor, new overlay form, settings, admin pages + sub-pages). SplitView component with draggable divider added to overlay editor. All stories updated.

Start the frontend dev server: `make frontend-dev` then `cd frontend && npm run dev`
  </what-built>
  <how-to-verify>
**Viewport testing (open browser DevTools → device toolbar):**

1. **375px (mobile):**
   - Landing: Hero stat cards stack 2x2 grid, login buttons stack vertically
   - Dashboard: Overlay grid shows 1 column
   - Overlay editor: Config panel full width on top, preview iframe below (no side-by-side)
   - Settings: Card sections full width

2. **768px (tablet):**
   - Dashboard: Overlay grid shows 2 columns
   - Overlay editor: Confirm the responsive breakpoint — at exactly 768px should transition to side-by-side

3. **1920px (desktop):**
   - Dashboard: Overlay grid shows 3 columns
   - Overlay editor: Config left ~40%, preview right ~60% with draggable divider
   - Landing: Hero cards in 4-column row, feature grid in 3 columns

**Functional checks:**
4. Navigate to an overlay editor page — confirm split-view shows the overlay preview iframe on the right
5. Try dragging the split-view divider — confirm both panels resize, divider doesn't collapse to 0
6. Navigate to `/admin` — confirm dark nav (NOT white), dark card stats
7. Navigate to `/admin/users` — confirm dark table (NOT white), tokens applied
8. Landing page: magnetic glow cards should react to cursor movement (hover the hero section)

**Overlay preview preservation (PAGE-04, FEAT-04):**
9. Open the overlay preview page directly at `http://localhost:3000/overlays/{any-id}/preview`
10. Confirm it renders the overlay CSS correctly (NOT redesigned — should look exactly as it did before Phase 25)
11. Confirm marketplace themes load correctly in the preview

**A11y validation:**
12. Run `cd frontend && npm run storybook` → open in browser → check each story under "Pages/" section
13. In each story, open the "Accessibility" panel → confirm "Violations: 0" in error mode

Type "approved" when all checks pass, or describe specific issues found.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 25, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Automated gray class audit returns 0 results
- Build exits 0
- Human approves visual verification checkpoint
- Responsive layouts work at 375px / 768px / 1920px
- Split-view draggable divider functional
- Overlay preview page unchanged
- Storybook a11y panel shows 0 violations for all Pages/ stories
</verification>

<success_criteria>
All PAGE-01 through PAGE-10 requirements and FEAT-01 through FEAT-04 requirements verified by both automated audits and human visual inspection. Phase 25 complete. Ready for Phase 26 (Enforcement & Quality Gates).
</success_criteria>

<output>
After completion, create `.planning/phases/25-page-migration-split-view-preview/25-08-SUMMARY.md`
</output>
