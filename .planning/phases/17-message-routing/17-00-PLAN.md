---
phase: 17-message-routing
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - services/message-processor/router/overlay_router_test.go
  - services/overlay-manager/handlers/sources_shared_overlay_test.go
autonomous: true
requirements:
  - SOURCE-04
must_haves:
  truths:
    - "overlay_router_test.go exists with failing tests for FindOverlaysForMessage fan-out"
    - "sources_shared_overlay_test.go has TestHandleAddSource_SharedOverlay_IsActiveTrue that fails RED"
  artifacts:
    - path: "services/message-processor/router/overlay_router_test.go"
      provides: "Failing unit tests for UNION router (direct, shared, both, no-match, revoked-excluded)"
    - path: "services/overlay-manager/handlers/sources_shared_overlay_test.go"
      provides: "Failing unit test asserting is_active=true for shared_overlay platform"
  key_links:
    - from: "overlay_router_test.go"
      to: "router.FindOverlaysForMessage"
      via: "pgxmock or table-driven stub"
      pattern: "TestFindOverlaysForMessage"
    - from: "sources_shared_overlay_test.go"
      to: "handlers.HandleAddSource"
      via: "mockSourceRepository.createFunc capturing source"
      pattern: "TestHandleAddSource_SharedOverlay_IsActiveTrue"
---

<objective>
Create failing test scaffolds (Wave 0 / Nyquist compliance) for the two Phase 17 implementation tasks. Tests must fail RED before any implementation touches the production files.

Purpose: Nyquist rule — every automated verify in Wave 1 needs an existing test file. Wave 0 writes the failing tests so Wave 1 can go GREEN.
Output: Two test files with RED tests; no production code changed.
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-message-routing/17-CONTEXT.md
@.planning/phases/17-message-routing/17-RESEARCH.md
@.planning/phases/17-message-routing/17-VALIDATION.md
</context>

<interfaces>
<!-- Key types the executor needs. No codebase exploration required. -->

From services/message-processor/models/message.go:
```go
type OverlayTarget struct {
    OverlayID string
    UserID    string
}
```

From services/message-processor/router/overlay_router.go:
```go
type Repository struct { db *pgxpool.Pool }
func (r *Repository) FindOverlaysForMessage(ctx context.Context, platform, channelID string) ([]models.OverlayTarget, error)
```

From services/overlay-manager/handlers/sources_shared_overlay_test.go (existing):
```go
// mockSourceRepository already defined in this file (createFunc, listByOverlayFunc, etc.)
// mockOverlayRepository is defined in overlay_test.go (same package)
// TestHandleAddSource_SharedOverlay_Forbidden already exists — do NOT duplicate it
```

From services/overlay-manager/models/chat_source.go:
```go
type ChatSource struct {
    OverlayID     string
    Platform      string
    ChannelID     string
    ChannelName   string
    ChannelHandle *string
    AuthRequired  bool
    Config        map[string]interface{}
    IsActive      bool  // must be true for shared_overlay after Wave 1
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Router test scaffold — failing tests for FindOverlaysForMessage UNION</name>
  <files>services/message-processor/router/overlay_router_test.go</files>
  <behavior>
    - TestFindOverlaysForMessage/direct_only: query with platform="twitch", channelID="xqc" returns [overlayA]; no shared_overlay fan-out configured
    - TestFindOverlaysForMessage/shared_fan_out: sender overlay has twitch source, recipient overlay has shared_overlay source pointing to sender; returns [overlayA, overlayB]
    - TestFindOverlaysForMessage/no_match: platform/channel combo not in any source; returns []
    - TestFindOverlaysForMessage/revoked_excluded: share_requests row exists but status='revoked'; recipient NOT returned
    - TestFindOverlaysForMessage/both_direct_and_shared: overlay is both a direct recipient AND a fan-out recipient; returned exactly once (UNION dedup)
  </behavior>
  <action>
    Create services/message-processor/router/overlay_router_test.go with package router.

    Use pgxmock (github.com/pashagolub/pgxmock/v2) if already in go.mod, otherwise use a table-driven approach with a real pgxpool pointing to a test schema via testcontainers. Check go.mod first.

    If neither pgxmock nor testcontainers is available in the message-processor go.mod, use a simpler approach: define a RouterRepository interface in the test file (matching the Repository method signature), implement a stub, and test the SQL query string directly by inspecting what is passed to the stub. However, the preferred approach is to mock at the pgxpool level.

    The tests MUST fail at Wave 0 because the current FindOverlaysForMessage query has no UNION branch. The failure mode will be: the "shared_fan_out" and "revoked_excluded" cases return wrong results (empty or missing the recipient overlay).

    Write table-driven tests using t.Run for each case. Import testify/assert. Follow existing test patterns in services/message-processor/dedup/ or services/message-processor/registry/ for mock patterns.

    Do NOT implement the UNION — that is Wave 1. These are RED tests only.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/services/message-processor && go test ./router/... -run TestFindOverlaysForMessage -v 2>&1 | tail -20</automated>
  </verify>
  <done>Test file compiles. At least the shared_fan_out and revoked_excluded sub-tests FAIL (RED state). Direct and no_match sub-tests may pass or fail depending on mock approach — both must compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sources test — failing test for shared_overlay is_active=true</name>
  <files>services/overlay-manager/handlers/sources_shared_overlay_test.go</files>
  <behavior>
    - TestHandleAddSource_SharedOverlay_IsActiveTrue: POST platform=shared_overlay with a valid accepted share (simulated via real DB row or mock) → source created with is_active=true
    - The test uses a mockSourceRepository.createFunc that captures the *models.ChatSource argument and asserts source.IsActive == true
    - Since real DB is needed to simulate the accepted share check, inject a real pgxpool pointing to a test DB, OR use miniredis pattern. Actually: the real share_requests check in HandleAddSource uses h.db directly (not through an interface), so a real pgxpool is needed.
    - Simplest approach: use alicebob/miniredis pattern is not applicable here (that's Redis). Instead, use testcontainers (available in overlay-manager) with a minimal schema seeded with one overlays row and one share_requests row with status='accepted'.
    - If testcontainers setup is too heavy for a Wave 0 stub, use t.Skip with a TODO comment and rely on the existing TestHandleAddSource_SharedOverlay_Forbidden test going GREEN (which covers the handler path). In that case, add a simple assertion test that can go RED without DB: test that IsActive is NOT set to true today (inspect the source struct before DB call using the createFunc hook).
  </behavior>
  <action>
    APPEND to services/overlay-manager/handlers/sources_shared_overlay_test.go (do NOT replace — existing TestHandleAddSource_SharedOverlay_Forbidden must be preserved).

    Add TestHandleAddSource_SharedOverlay_IsActiveTrue using the mockSourceRepository.createFunc to capture the source argument. The test needs to simulate a successful share validation path. Since h.db is a real *pgxpool.Pool used for share_requests queries, and the Wave 0 test must be self-contained without a real DB:

    Use this approach:
    1. The test sets h.db = nil (same as the Forbidden test)
    2. When db is nil, the handler returns 403 early (per existing nil-guard pattern from Phase 16)
    3. This means we CANNOT reach the CreateFunc with is_active=true via nil db
    4. Instead, write the test as a DOCUMENTED FAILING test using t.Skip("RED: requires DB to pass share validation — goes GREEN in Wave 1 integration test") with a comment explaining what it should assert when GREEN
    5. Then add a companion test TestHandleAddSource_NonSharedPlatform_IsActiveFalse that verifies the EXISTING behavior (non-shared platforms have is_active=false) — this gives a passing baseline test to confirm the createFunc capture pattern works

    The TestHandleAddSource_SharedOverlay_IsActiveTrue should be structured so that when the Wave 1 implementation adds is_active=true and a test-DB approach is used, the Skip can be removed. For now, the file must compile and the Skip-based test satisfies Nyquist (it documents the contract).

    NOTE: Do NOT remove or modify TestHandleAddSource_SharedOverlay_Forbidden.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/services/overlay-manager && go test ./handlers/... -run TestHandleAddSource_SharedOverlay -v 2>&1 | tail -20</automated>
  </verify>
  <done>File compiles without errors. TestHandleAddSource_SharedOverlay_Forbidden still passes (was already GREEN from Phase 16). TestHandleAddSource_SharedOverlay_IsActiveTrue is SKIPPED (documented RED contract). TestHandleAddSource_NonSharedPlatform_IsActiveFalse passes GREEN. All three tests visible in output.</done>
</task>

</tasks>

<verification>
Both services compile: `cd /home/caesar/git/all-chat/services/message-processor && go build ./... && cd /home/caesar/git/all-chat/services/overlay-manager && go build ./...`

Router tests exist and at least shared_fan_out fails RED: `cd /home/caesar/git/all-chat/services/message-processor && go test ./router/... -v`

Sources test compiles with new test functions: `cd /home/caesar/git/all-chat/services/overlay-manager && go test ./handlers/... -run TestHandleAddSource -v`
</verification>

<success_criteria>
- overlay_router_test.go exists and compiles in package router
- TestFindOverlaysForMessage has sub-tests for: direct_only, shared_fan_out, no_match, revoked_excluded, both_direct_and_shared
- At least shared_fan_out and revoked_excluded sub-tests FAIL RED (current query has no UNION)
- sources_shared_overlay_test.go has TestHandleAddSource_SharedOverlay_IsActiveTrue (Skipped) and TestHandleAddSource_NonSharedPlatform_IsActiveFalse (Green)
- Existing TestHandleAddSource_SharedOverlay_Forbidden still passes
- Both services compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/17-message-routing/17-00-SUMMARY.md`
</output>
