---
phase: 26-enforcement-quality-gates
plan: 04
type: execute
wave: 3
depends_on:
  - 26-01
  - 26-02
  - 26-03
files_modified:
  - .github/workflows/frontend-quality.yml
  - frontend/src/styles/MARKETPLACE_MIGRATION_GUIDE.md
  - frontend/package.json
autonomous: false
requirements:
  - ENFORCE-05
  - ENFORCE-06
  - ENFORCE-07
  - ENFORCE-10

must_haves:
  truths:
    - "A `frontend-quality.yml` GitHub Actions workflow exists that runs on PRs touching frontend/**"
    - "The workflow runs ESLint, Prettier check, TypeScript check, build, Storybook tests, and Chromatic in sequence"
    - "The Chromatic step uses `exitZeroOnChanges: false` — PRs are blocked until visual changes are reviewed"
    - "MARKETPLACE_MIGRATION_GUIDE.md exists alongside EVENTS_CSS_API.md with class name mapping and cascade layer explanation"
    - "The human verifier has set up the Chromatic project token and added it as a GitHub Actions secret"
  artifacts:
    - path: ".github/workflows/frontend-quality.yml"
      provides: "CI quality gates: ESLint + Prettier + tsc + build + bundle analysis + Storybook + Chromatic"
    - path: "frontend/src/styles/MARKETPLACE_MIGRATION_GUIDE.md"
      provides: "Migration guide for overlay marketplace theme authors upgrading to v1.3"
  key_links:
    - from: ".github/workflows/frontend-quality.yml"
      to: "frontend/eslint.config.mjs"
      via: "npx eslint . step"
      pattern: "npx eslint"
    - from: ".github/workflows/frontend-quality.yml"
      to: "CHROMATIC_PROJECT_TOKEN secret"
      via: "chromaui/action"
      pattern: "secrets.CHROMATIC_PROJECT_TOKEN"
---

<objective>
Create the CI/CD quality gates workflow and marketplace migration documentation.

Purpose: Plans 01-03 set up local enforcement. This plan makes enforcement apply to every PR via GitHub Actions. Also creates the MARKETPLACE_MIGRATION_GUIDE.md that Phase 26's ENFORCE-07 requires — companion to the EVENTS_CSS_API.md from Phase 23.

Output: `.github/workflows/frontend-quality.yml` running all quality checks + Chromatic visual regression, plus `MARKETPLACE_MIGRATION_GUIDE.md` for theme authors. Contains one human checkpoint for Chromatic token setup.
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-enforcement-quality-gates/26-CONTEXT.md
@.planning/phases/26-enforcement-quality-gates/26-RESEARCH.md
@.planning/phases/26-enforcement-quality-gates/26-01-SUMMARY.md
@.planning/phases/26-enforcement-quality-gates/26-02-SUMMARY.md
@.planning/phases/26-enforcement-quality-gates/26-03-SUMMARY.md
@frontend/src/styles/EVENTS_CSS_API.md

<interfaces>
<!-- Existing workflow for reference (Docker builds) -->
<!-- .github/workflows/build-and-push.yml exists — do NOT touch it -->
<!-- New workflow goes in: .github/workflows/frontend-quality.yml -->

<!-- From Plan 01: eslint script is `npx eslint .` (not `next lint` — removed in Next.js 16) -->
<!-- From Plan 01: prettier check is `npx prettier --check .` -->
<!-- From Plan 02: tsc is `npx tsc --noEmit` (no --incremental) -->
<!-- From Plan 03: bundle analyzer ready; storybook tests via `npx vitest --project storybook --run` -->

Chromatic action: chromaui/action@latest
Required secret: CHROMATIC_PROJECT_TOKEN (must be added by repo owner)
Key config: exitZeroOnChanges: false (block PR until changes reviewed)
Key config: autoAcceptChanges: "main" (auto-accept baseline updates on main)
Key config: fetch-depth: 0 (required for Chromatic baseline tracking)

hashicorp/nextjs-bundle-analysis: compares bundle against committed baseline
Requires: a baseline JSON in the repo (generated by first build with ANALYZE enabled)
Config lives in: frontend/package.json under "nextBundleAnalysis" key

Open question from research: hashicorp/nextjs-bundle-analysis + Next.js 16 Turbopack
compatibility. To be safe: use NEXT_DISABLE_TURBOPACK=true in the build step.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create frontend-quality.yml CI workflow</name>
  <files>
    .github/workflows/frontend-quality.yml
    frontend/package.json
  </files>
  <action>
Create `.github/workflows/frontend-quality.yml`:

```yaml
name: Frontend Quality Gates

on:
  pull_request:
    branches: [main]
    paths: ['frontend/**']
  # Also run on pushes to main to establish Chromatic baseline
  push:
    branches: [main]
    paths: ['frontend/**']

jobs:
  quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Required for Chromatic baseline comparison

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: ESLint
        run: npx eslint . --max-warnings 0

      - name: Prettier check
        run: npx prettier --check .

      - name: TypeScript check
        run: npx tsc --noEmit

      - name: Build (for bundle analysis)
        run: npm run build
        env:
          ANALYZE: 'false'
          # Disable Turbopack for bundle analysis compatibility with hashicorp action
          NEXT_DISABLE_TURBOPACK: 'true'

      - name: Bundle size analysis
        uses: hashicorp/nextjs-bundle-analysis@v1
        with:
          working-directory: frontend

      - name: Storybook tests (a11y + performance)
        run: npx vitest --project storybook --run

      - name: Chromatic visual regression
        uses: chromaui/action@latest
        with:
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          workingDir: frontend
          exitZeroOnChanges: false
          autoAcceptChanges: 'main'
```

Also add the `nextBundleAnalysis` configuration to `frontend/package.json` (add as a top-level key alongside `scripts`, `dependencies`, etc.):

```json
"nextBundleAnalysis": {
  "budget": 20480,
  "budgetPercentIncreaseRed": 20,
  "minimumChangeThreshold": 0,
  "showDetails": true
}
```

The `budget: 20480` is 20KB in bytes. PRs exceeding this threshold get a red status in the PR comment from the hashicorp action — satisfying the ENFORCE-05 ">20KB requires justification" gate.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat && cat .github/workflows/frontend-quality.yml | grep -c "chromaui/action"</automated>
  </verify>
  <done>
    `.github/workflows/frontend-quality.yml` exists. File contains all 7 steps: ESLint, Prettier, tsc, build, bundle analysis, Storybook tests, Chromatic. `frontend/package.json` has `nextBundleAnalysis` config with `budget: 20480`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create MARKETPLACE_MIGRATION_GUIDE.md</name>
  <files>
    frontend/src/styles/MARKETPLACE_MIGRATION_GUIDE.md
  </files>
  <action>
Create `frontend/src/styles/MARKETPLACE_MIGRATION_GUIDE.md`:

```markdown
# Overlay Marketplace Migration Guide — v1.3

**Companion to:** [EVENTS_CSS_API.md](./EVENTS_CSS_API.md)
**Updated:** 2026-03-14

This guide is for overlay theme authors who publish CSS themes to the All-Chat marketplace. It documents what changed in v1.3 (Frontend Redesign) and how to update your themes.

---

## What Changed in v1.3

### CSS Cascade Layer Architecture (New)

The most important change in v1.3 is the introduction of CSS cascade layers. This **eliminates the need for `!important` in your theme CSS**.

The full layer order is:

```
@layer base, design-system, marketplace-themes, user-overrides;
```

| Layer | Priority | Who writes it |
|-------|----------|---------------|
| `user-overrides` | Highest | You — theme authors using this layer |
| `marketplace-themes` | High | `events.css` (platform default styles) |
| `design-system` | Medium | Platform design tokens |
| `base` | Lowest | Browser normalization, CSS reset |

**Action required:** Replace `!important` declarations in your themes with `@layer user-overrides { ... }` wrapping.

**Before (v1.2 and earlier):**
```css
.event-message {
  border-radius: 0 !important;
}
```

**After (v1.3):**
```css
@layer user-overrides {
  .event-message {
    border-radius: 0;
  }
}
```

---

## Frozen Class Names (No Changes Required)

All class names in `EVENTS_CSS_API.md` are **unchanged** in v1.3. Your existing selectors continue to work without modification:

| Class / Selector | Status |
|-----------------|--------|
| `.event-message` | Frozen — unchanged |
| `.event-content` | Frozen — unchanged |
| `.event-icon` | Frozen — unchanged |
| `.event-title` | Frozen — unchanged |
| `.event-value` | Frozen — unchanged |
| `.event-tier-high` | Frozen — unchanged |
| `.event-tier-medium` | Frozen — unchanged |
| `.event-tier-low` | Frozen — unchanged |
| `.event-type-subscription` | Frozen — unchanged |
| `.event-type-gift_subscription` | Frozen — unchanged |
| `.event-type-super_chat` | Frozen — unchanged |
| `.event-type-super_sticker` | Frozen — unchanged |
| `.event-type-raid` | Frozen — unchanged |
| `.event-type-bits` | Frozen — unchanged |
| `.event-type-channel_points` | Frozen — unchanged |
| `.event-type-gift` | Frozen — unchanged |
| `.event-type-mystery_gift` | Frozen — unchanged |
| `.event-type-follow` | Frozen — unchanged |
| `.event-type-like_aggregate` | Frozen — unchanged |
| `.event-type-member` | Frozen — unchanged |
| `.event-type-token_expiration_warning` | Frozen — unchanged |
| `.event-message[data-platform="twitch"]` | Frozen — unchanged |
| `.event-message[data-platform="youtube"]` | Frozen — unchanged |
| `.event-message[data-platform="kick"]` | Frozen — unchanged |
| `.event-message[data-platform="tiktok"]` | Frozen — unchanged |
| `.event-message[data-platform="system"]` | Frozen — unchanged |

---

## Theme Template for v1.3

Minimal v1.3-compatible theme using the cascade layer architecture:

```css
/* my-overlay-theme.css — v1.3 compatible */

/* Import this AFTER the platform's default CSS */
@layer user-overrides {
  /* Override event message appearance */
  .event-message {
    border-radius: 8px;
    border-width: 2px;
  }

  /* Style high-tier events (large Super Chats, raids) */
  .event-tier-high {
    border-color: gold;
    background-color: rgba(255, 215, 0, 0.1);
  }

  /* Platform-specific styling */
  .event-message[data-platform="twitch"] {
    border-left-color: #9146ff;
  }

  .event-message[data-platform="youtube"] {
    border-left-color: #ff0000;
  }
}
```

---

## Migration Checklist

- [ ] Replace `!important` rules with `@layer user-overrides { ... }` wrapper
- [ ] Verify your theme loads AFTER the platform CSS (the overlay page handles this automatically)
- [ ] Test all event types: subscription, raid, Super Chat, bits, follow
- [ ] Test all platforms: Twitch, YouTube, Kick, TikTok
- [ ] No action needed for frozen class names — they are unchanged

---

## Questions and Support

- **Class name freeze policy:** See [EVENTS_CSS_API.md](./EVENTS_CSS_API.md) — Change Policy section
- **Report a breaking change:** Open a GitHub issue with the `breaking-css-api` label
- **CSS cascade layers reference:** [MDN @layer](https://developer.mozilla.org/en-US/docs/Web/CSS/@layer)
```
  </action>
  <verify>
    <automated>ls /home/caesar/git/all-chat/frontend/src/styles/MARKETPLACE_MIGRATION_GUIDE.md</automated>
  </verify>
  <done>
    `frontend/src/styles/MARKETPLACE_MIGRATION_GUIDE.md` exists alongside `EVENTS_CSS_API.md`. Contains all three sections: cascade layer explanation, frozen class name table, and migration checklist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Chromatic setup and quality gate sign-off</name>
  <what-built>
    Complete Phase 26 enforcement tooling:
    - Plan 01: ESLint flat config + Prettier (ENFORCE-01, ENFORCE-02, ENFORCE-03)
    - Plan 02: Husky pre-commit hook (ENFORCE-04)
    - Plan 03: Bundle analyzer + performance Storybook story (ENFORCE-08, ENFORCE-09)
    - Plan 04 Task 1: CI workflow with Chromatic (ENFORCE-05, ENFORCE-06)
    - Plan 04 Task 2: Marketplace migration guide (ENFORCE-07)
    - ENFORCE-10: Already satisfied by existing preview.ts a11y: 'error' config
  </what-built>
  <how-to-verify>
    **Step 1: Verify local tooling works**
    ```bash
    cd /home/caesar/git/all-chat/frontend && npx eslint . --max-warnings 0 && echo "ESLint: OK"
    cd /home/caesar/git/all-chat/frontend && npx prettier --check . && echo "Prettier: OK"
    cd /home/caesar/git/all-chat/frontend && npx tsc --noEmit && echo "TypeScript: OK"
    cd /home/caesar/git/all-chat/frontend && sh .husky/pre-commit && echo "Pre-commit hook: OK"
    ```

    **Step 2: Verify Storybook tests pass**
    ```bash
    cd /home/caesar/git/all-chat/frontend && npx vitest --project storybook --run
    ```
    All stories should pass, including the new `Performance/RenderTimingTest` story.

    **Step 3: Set up Chromatic (required for ENFORCE-06)**
    1. Visit https://www.chromatic.com/ and sign in with GitHub
    2. Click "Add project" and connect the `all-chat` repository
    3. Copy the project token (shown after project creation)
    4. Go to the GitHub repo → Settings → Secrets and variables → Actions
    5. Click "New repository secret"
    6. Name: `CHROMATIC_PROJECT_TOKEN`, Value: (paste token)
    7. Click "Add secret"

    **Step 4: Verify the workflow file**
    ```bash
    cat /home/caesar/git/all-chat/.github/workflows/frontend-quality.yml
    ```
    Confirm it has all 7 steps: ESLint, Prettier, tsc, build, bundle analysis, Storybook tests, Chromatic.

    **Step 5: Read the migration guide**
    ```bash
    cat /home/caesar/git/all-chat/frontend/src/styles/MARKETPLACE_MIGRATION_GUIDE.md
    ```
    Verify it has: cascade layer explanation, frozen class name table (all 25 classes), and migration checklist.

    **Expected: First Chromatic run on main will establish the baseline (no PR to compare against yet — this is expected behavior). After baseline is established, all future PRs will diff against it.**
  </how-to-verify>
  <resume-signal>
    Type "approved" once Chromatic token is set up and all local checks pass. Describe any issues found.
  </resume-signal>
  <action>Human verification step — see how-to-verify for instructions. Set up Chromatic project token as GitHub Actions secret CHROMATIC_PROJECT_TOKEN.</action>
  <verify><automated>cd /home/caesar/git/all-chat/frontend && npx eslint . --max-warnings 0 && npx prettier --check . && npx tsc --noEmit && echo "All local checks passing"</automated></verify>
  <done>All local quality checks pass (ESLint, Prettier, tsc, Storybook vitest). CHROMATIC_PROJECT_TOKEN added as GitHub Actions secret. Human has reviewed and typed "approved".</done>
</task>

</tasks>

<verification>
All automated checks:
```bash
cd /home/caesar/git/all-chat/frontend && \
  npx eslint . --max-warnings 0 && \
  npx prettier --check . && \
  npx tsc --noEmit && \
  npx vitest --project storybook --run && \
  echo "All quality gates passing"
```

CI workflow syntax check:
```bash
cd /home/caesar/git/all-chat && \
  cat .github/workflows/frontend-quality.yml | grep -E "(name:|uses:|run:)" | head -20
```

Documentation verification:
```bash
ls /home/caesar/git/all-chat/frontend/src/styles/
```
Should show: `events.css`, `EVENTS_CSS_API.md`, `MARKETPLACE_MIGRATION_GUIDE.md`
</verification>

<success_criteria>
- `.github/workflows/frontend-quality.yml` exists with all 7 CI steps
- `frontend/package.json` has `nextBundleAnalysis` config with `budget: 20480`
- `frontend/src/styles/MARKETPLACE_MIGRATION_GUIDE.md` exists with cascade layer explanation, frozen class table, and migration checklist
- Human verifier has added `CHROMATIC_PROJECT_TOKEN` as a GitHub Actions secret
- All local automated checks pass (ESLint, Prettier, tsc, Storybook vitest)
- Chromatic first run on main establishes baseline (ENFORCE-06)
- ENFORCE-10 acknowledged as already satisfied by `preview.ts` a11y: 'error' setting
</success_criteria>

<output>
After completion, create `.planning/phases/26-enforcement-quality-gates/26-04-SUMMARY.md`
</output>
