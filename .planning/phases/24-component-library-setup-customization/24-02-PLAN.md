---
phase: 24-component-library-setup-customization
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/skeleton.tsx
  - frontend/src/components/ui/button.tsx
  - frontend/src/stories/Card.stories.tsx
  - frontend/src/stories/Input.stories.tsx
  - frontend/src/stories/Skeleton.stories.tsx
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05]

must_haves:
  truths:
    - "Card component renders with design tokens (--color-surface, --color-border) and hover scale+shadow micro-interaction"
    - "Input component wraps @base-ui/react/input with focus-visible ring matching Button's established pattern"
    - "Skeleton component renders animate-pulse with --color-surface-2 background"
    - "Button has a gradient variant applying linear-gradient(90deg, #9146FF, #69C9D0) — same as nav underline"
    - "All dark: orphan classes removed from button.tsx (dark-only app, they never activate)"
    - "Stories import from @/components/ui/* (real components, not stub placeholders)"
    - "TypeScript compiles clean, CVA variant types are correct"
  artifacts:
    - path: "frontend/src/components/ui/card.tsx"
      provides: "Card component with CVA interactive variant (hover:scale-[1.02] + shadow-lg)"
      exports: ["Card", "cardVariants"]
    - path: "frontend/src/components/ui/input.tsx"
      provides: "Input component wrapping @base-ui/react/input"
      exports: ["Input", "inputVariants"]
    - path: "frontend/src/components/ui/skeleton.tsx"
      provides: "Skeleton component with animate-pulse"
      exports: ["Skeleton"]
    - path: "frontend/src/components/ui/button.tsx"
      provides: "Button with gradient variant added, dark: classes removed"
      exports: ["Button", "buttonVariants"]
  key_links:
    - from: "frontend/src/components/ui/card.tsx"
      to: "frontend/src/app/globals.css"
      via: "CSS custom properties (--color-surface, --color-border) referenced as Tailwind tokens"
      pattern: "bg-surface|border-border"
    - from: "frontend/src/components/ui/input.tsx"
      to: "@base-ui/react/input"
      via: "InputPrimitive import"
      pattern: "from \"@base-ui/react/input\""
    - from: "frontend/src/components/ui/button.tsx"
      to: "gradient variant"
      via: "bg-[linear-gradient(90deg,#9146FF,#69C9D0)] Tailwind arbitrary value"
      pattern: "gradient"
---

<objective>
Build the three simpler components (Card, Input, Skeleton) and extend Button with the gradient variant.

Purpose: Card establishes the micro-interaction pattern (COMP-04) — hover scale + shadow lift on interactive containers. Input demonstrates @base-ui/react wrapping for form elements. Skeleton completes the loading state system (COMP-07). Button gradient (COMP-05) implements the "platforms are the theme" identity CTA. All four follow the exact pattern established by the existing button.tsx.

Output: Four production-ready UI components in frontend/src/components/ui/, stories updated to import from real components.
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-component-library-setup-customization/24-CONTEXT.md
@.planning/phases/24-component-library-setup-customization/24-RESEARCH.md
@.planning/phases/24-component-library-setup-customization/24-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From frontend/src/components/ui/button.tsx (reference implementation — follow exactly):
```typescript
"use client"

import { Button as ButtonPrimitive } from "@base-ui/react/button"
import { cva, type VariantProps } from "class-variance-authority"
import { cn } from "@/lib/utils"

const buttonVariants = cva(
  "group/button inline-flex shrink-0 items-center justify-center rounded-lg border border-transparent bg-clip-padding text-sm font-medium whitespace-nowrap transition-all outline-none select-none focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50 disabled:pointer-events-none disabled:opacity-50 aria-invalid:border-destructive aria-invalid:ring-3 aria-invalid:ring-destructive/20 dark:aria-invalid:border-destructive/50 dark:aria-invalid:ring-destructive/40 [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4",
  {
    variants: {
      variant: {
        default: "bg-primary text-primary-foreground [a]:hover:bg-primary/80",
        outline: "border-border bg-background hover:bg-muted hover:text-foreground aria-expanded:bg-muted aria-expanded:text-foreground dark:border-input dark:bg-input/30 dark:hover:bg-input/50",
        secondary: "bg-secondary text-secondary-foreground hover:bg-secondary/80 aria-expanded:bg-secondary aria-expanded:text-secondary-foreground",
        ghost: "hover:bg-muted hover:text-foreground aria-expanded:bg-muted aria-expanded:text-foreground dark:hover:bg-muted/50",
        destructive: "bg-destructive/10 text-destructive hover:bg-destructive/20 focus-visible:border-destructive/40 focus-visible:ring-destructive/20 dark:bg-destructive/20 dark:hover:bg-destructive/30 dark:focus-visible:ring-destructive/40",
        link: "text-primary underline-offset-4 hover:underline",
      },
      size: { /* ... existing sizes unchanged ... */ }
    },
    defaultVariants: { variant: "default", size: "default" }
  }
)

function Button({ className, variant = "default", size = "default", ...props }) {
  return (
    <ButtonPrimitive data-slot="button" className={cn(buttonVariants({ variant, size, className }))} {...props} />
  )
}

export { Button, buttonVariants }
```

From frontend/src/app/globals.css (available CSS custom property tokens):
- `--color-surface` → use as `bg-surface`
- `--color-surface-2` → use as `bg-surface-2`
- `--color-border` → use as `border-border`
- `--color-text` → use as `text-text`
- `--color-text-sub` → use as `text-text-sub`
- `--color-text-dim` → use as `text-text-dim`
- `--color-ring` → use as `ring-ring`, `border-ring`
- `--shadow-md` → use as `shadow-md` (or inline style)

From @base-ui/react/input (Input primitive):
```typescript
import { Input as InputPrimitive } from "@base-ui/react/input"
// InputPrimitive.Props is the props type
// Renders an accessible <input> element
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Card, Input, and Skeleton components</name>
  <files>
    frontend/src/components/ui/card.tsx,
    frontend/src/components/ui/input.tsx,
    frontend/src/components/ui/skeleton.tsx
  </files>
  <behavior>
    Card:
    - Default variant: rounded-xl border border-border bg-surface text-text transition-all
    - interactive=true variant: adds hover:scale-[1.02] hover:shadow-lg cursor-pointer
    - interactive=false (default): no hover effects
    - Renders a plain div with data-slot="card"

    Input:
    - Wraps @base-ui/react/input InputPrimitive
    - Base: w-full rounded-lg border border-border bg-surface px-3 py-2 text-sm text-text placeholder:text-text-dim transition-all outline-none
    - Focus ring: focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50 (exactly matching Button pattern)
    - Disabled: disabled:pointer-events-none disabled:opacity-50
    - Renders with data-slot="input"
    - Size variants: default (h-9), sm (h-7 text-xs)

    Skeleton:
    - Plain div with data-slot="skeleton"
    - Base classes: animate-pulse rounded-md bg-surface-2
    - No CVA needed — single style, consumers control width/height/radius via className
    - Accepts className and all HTMLDivElement props
  </behavior>
  <action>
    Create three files following the established button.tsx pattern.

    card.tsx:
    - "use client" directive
    - Import cva, type VariantProps from "class-variance-authority"
    - Import cn from "@/lib/utils"
    - cardVariants = cva("rounded-xl border border-border bg-surface text-text transition-all", { variants: { interactive: { true: "hover:scale-[1.02] hover:shadow-lg cursor-pointer", false: "" } }, defaultVariants: { interactive: false } })
    - Card function accepts React.HTMLAttributes<HTMLDivElement> & VariantProps<typeof cardVariants>
    - Export Card and cardVariants

    input.tsx:
    - "use client" directive
    - Import InputPrimitive from "@base-ui/react/input"
    - Import cva, type VariantProps from "class-variance-authority"
    - Import cn from "@/lib/utils"
    - inputVariants = cva with base classes and size variants as per behavior spec above
    - Input function accepts InputPrimitive.Props & VariantProps<typeof inputVariants>
    - Export Input and inputVariants

    skeleton.tsx:
    - "use client" directive
    - Import cn from "@/lib/utils"
    - No CVA — simple function
    - Skeleton({ className, ...props }: React.HTMLAttributes<HTMLDivElement>) returns div with data-slot="skeleton" and className={cn("animate-pulse rounded-md bg-surface-2", className)}
    - Export Skeleton
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    Three component files exist, TypeScript compiles clean, Card/Input/inputVariants/Skeleton are exported, Card has interactive CVA variant, Input wraps @base-ui/react/input with focus ring, Skeleton has animate-pulse bg-surface-2
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add gradient variant to Button and remove orphan dark: classes</name>
  <files>frontend/src/components/ui/button.tsx</files>
  <behavior>
    - New `gradient` variant renders the Twitch→TikTok gradient CTA
    - Gradient applied via Tailwind arbitrary value: bg-[linear-gradient(90deg,#9146FF,#69C9D0)]
    - Gradient variant base text: text-white font-semibold (gradient already provides color, just need white text)
    - All `dark:` prefixed classes removed (app is dark-only, dark: classes never activate — confirmed in research)
    - All other existing variants and sizes preserved unchanged
  </behavior>
  <action>
    Read the current button.tsx carefully. Make two targeted changes:

    1. Add `gradient` variant to the `variant` key in buttonVariants CVA:
       ```
       gradient: "bg-[linear-gradient(90deg,#9146FF,#69C9D0)] text-white font-semibold border-transparent",
       ```

    2. Remove all `dark:` prefixed classes from every variant. Specifically:
       - In `outline` variant: remove `dark:border-input dark:bg-input/30 dark:hover:bg-input/50`
       - In `ghost` variant: remove `dark:hover:bg-muted/50`
       - In `destructive` variant: remove `dark:bg-destructive/20 dark:hover:bg-destructive/30 dark:focus-visible:ring-destructive/40`
       - In base cva string: remove `dark:aria-invalid:border-destructive/50 dark:aria-invalid:ring-destructive/40`

    Preserve all non-dark variant classes exactly as written. Do not change any size variants. Do not change the function signature or render logic.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/frontend && npx tsc --noEmit 2>&1 | head -20 && grep -c "gradient" frontend/src/components/ui/button.tsx</automated>
  </verify>
  <done>
    button.tsx has gradient variant with bg-[linear-gradient(90deg,#9146FF,#69C9D0)], zero dark: classes remain, TypeScript compiles clean, all other variants untouched
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Card, Input, and Skeleton stories to import real components</name>
  <files>
    frontend/src/stories/Card.stories.tsx,
    frontend/src/stories/Input.stories.tsx,
    frontend/src/stories/Skeleton.stories.tsx
  </files>
  <action>
    Replace the stub placeholder components in the three story files with imports from the real components built in Task 1. Also update Button.stories (if it exists) or verify gradient story exists.

    Card.stories.tsx — replace inline Card placeholder with:
    ```typescript
    import { Card } from '@/components/ui/card'
    ```
    Keep existing story exports (Default, Interactive). Add a third story showing padding:
    ```typescript
    export const WithContent: Story = {
      args: { children: React.createElement('div', { className: 'p-6' }, 'Card with padding'), interactive: false }
    }
    ```
    Note: stories are .tsx so JSX works.

    Input.stories.tsx — replace inline placeholder with:
    ```typescript
    import { Input } from '@/components/ui/input'
    ```
    Stories: Default, Disabled (args: { disabled: true }), WithPlaceholder (args: { placeholder: 'Search...' }), Small (args: { size: 'sm' })

    Skeleton.stories.tsx — replace inline placeholder with:
    ```typescript
    import { Skeleton } from '@/components/ui/skeleton'
    ```
    Stories:
    - Default: { args: { className: 'h-4 w-32' } }
    - Card: { args: { className: 'h-32 w-64 rounded-xl' } }
    - TextLines: show a div with three Skeleton children stacked (manual render function, not args)

    Also update the Button.stories.ts to add a Gradient story variant if it doesn't already have one (Button.stories.ts → .tsx may need renaming; check the existing file extension — if .ts, that's fine, add the story with args: { variant: 'gradient', children: 'Get Started' }).

    After changes: all stories must import from @/components/ui/* not from inline placeholders.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/frontend && npx vitest run --project storybook 2>&1 | tail -20</automated>
  </verify>
  <done>
    Storybook test suite passes for Card, Input, Skeleton stories. Imports are from @/components/ui/* (not inline placeholders). No a11y errors in 'todo' mode (warnings are acceptable).
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. `ls frontend/src/components/ui/` — shows card.tsx, input.tsx, skeleton.tsx alongside button.tsx
2. `cd frontend && npx tsc --noEmit` — zero errors
3. `grep -c "gradient" frontend/src/components/ui/button.tsx` — returns 1 (gradient variant line)
4. `grep -rn "dark:" frontend/src/components/ui/button.tsx` — returns nothing (all dark: removed)
5. `cd frontend && npx vitest run --project storybook 2>&1 | tail -5` — Card, Input, Skeleton tests pass
</verification>

<success_criteria>
- Card, Input, Skeleton components exist in frontend/src/components/ui/ following button.tsx pattern
- Card interactive variant includes hover:scale-[1.02] + hover:shadow-lg (COMP-04)
- Input uses @base-ui/react/input with focus-visible ring matching Button (COMP-01, COMP-02)
- Skeleton uses animate-pulse bg-surface-2 (COMP-07)
- Button has gradient variant with #9146FF→#69C9D0 gradient (COMP-05)
- Zero dark: classes in button.tsx
- Stories import from real components, Storybook tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-component-library-setup-customization/24-02-SUMMARY.md`
</output>
