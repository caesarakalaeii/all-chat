---
phase: 24-component-library-setup-customization
plan: 05
type: execute
wave: 4
depends_on: [24-04]
files_modified:
  - frontend/.storybook/preview.ts
autonomous: false
requirements: [COMP-08]

must_haves:
  truths:
    - "Storybook a11y addon is in error mode — a11y violations fail tests, not just show warnings"
    - "Full Storybook test suite passes in error mode with zero a11y violations across all six components"
    - "Performance budget documented: bundle size delta from component additions is below 100KB"
    - "Human has visually verified micro-interactions, gradient CTA, and platform badge glow dots in Storybook"
  artifacts:
    - path: "frontend/.storybook/preview.ts"
      provides: "a11y test mode set to 'error'"
      contains: "test: 'error'"
  key_links:
    - from: "frontend/.storybook/preview.ts"
      to: "@storybook/addon-a11y"
      via: "a11y parameter test: 'error'"
      pattern: "test: 'error'"
---

<objective>
Finalize the component library by enabling strict a11y enforcement and measuring the performance budget.

Purpose: The a11y gate (changing 'todo' to 'error' in preview.ts) is intentionally the LAST step — if any component has an a11y violation, this catches it cleanly without blocking earlier component development. Performance budget (COMP-08) requires measuring bundle size delta after all components are built. Human visual verification confirms the tactile quality of micro-interactions, gradient CTA color accuracy, and platform badge glow dot appearance that automated tests cannot fully capture.

Output: a11y set to 'error' and all stories passing. Bundle size delta documented. Human visual sign-off on COMP-04 (hover animations), COMP-05 (gradient), COMP-06 (glow dots).
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-component-library-setup-customization/24-CONTEXT.md
@.planning/phases/24-component-library-setup-customization/24-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable a11y error mode and verify full suite passes</name>
  <files>frontend/.storybook/preview.ts</files>
  <action>
    Change `test: 'todo'` to `test: 'error'` in frontend/.storybook/preview.ts.

    This is a one-line change. The file currently reads:
    ```typescript
    a11y: {
      test: 'todo'
    }
    ```
    Change to:
    ```typescript
    a11y: {
      test: 'error'
    }
    ```

    After making the change, run the full Storybook test suite. If any a11y violations are reported, fix them before proceeding:

    Common a11y fixes:
    - Input without label: stories must include a label or aria-label
    - Button without accessible name: Button stories use children (text or icon with aria-label)
    - Dialog without aria-label: DialogContent uses DialogTitle which provides the accessible name
    - Badge color contrast: verify platform color text has sufficient contrast against bg-badge-bg (dark background means bright platform colors should pass WCAG AA)
    - Skeleton: aria-busy="true" or aria-label="Loading" may be needed

    Fix any violations in the story files (NOT the component source — stories should demonstrate accessible usage patterns). If a fix is needed in the component itself, make the targeted change.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/frontend && npx vitest run --project storybook 2>&1 | tail -30</automated>
  </verify>
  <done>
    preview.ts has `test: 'error'`. Full Storybook test suite passes with zero a11y violations. No story is skipped or excluded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Measure performance budget (COMP-08)</name>
  <files></files>
  <action>
    Run Next.js production build and capture First Load JS sizes for key routes. This establishes the baseline and confirms the component additions stay under the 100KB budget.

    ```bash
    cd /home/caesar/git/all-chat/frontend && npx next build 2>&1 | grep -E "First Load|Route|Page"
    ```

    Record the output. Key routes to inspect:
    - / (landing page)
    - /dashboard
    - Any shared chunks that include the new components

    The comparison baseline is not formally recorded (Phase 24 adds the components for the first time), but:
    - If the total "First Load JS" for any route exceeds 250KB, investigate — this may indicate bundle splitting issues
    - The 100KB budget is relative to what Phase 25 will add (page redesigns). This measurement establishes the baseline for Phase 25 to compare against.

    Document the build output in the SUMMARY.md rather than modifying any source files. Note the route sizes.

    If the build fails for any reason unrelated to components (e.g., other TypeScript errors in the codebase), note this but do not fix pre-existing issues outside Phase 24 scope.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/frontend && npx next build 2>&1 | grep "First Load" | head -10</automated>
  </verify>
  <done>
    Next.js build completes successfully. Route bundle sizes captured. No route exceeds 250KB First Load JS. Results documented in SUMMARY.md.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verify — micro-interactions, gradient CTA, platform badge glow dots</name>
  <action>Automated tests cannot verify visual quality of transitions, gradient colors, or glow dot appearance. Human reviews Storybook to confirm all tactile and visual design decisions from CONTEXT.md are correctly implemented.</action>
  <what-built>
    Complete component library: Card (with hover scale micro-interaction), Input, Badge (with platform glow dots), Dialog (frosted glass backdrop), Toast (bottom-right stacking), Skeleton (pulse animation), and Button gradient variant.

    All components themed with Phase 23 design tokens. Storybook stories passing with a11y in 'error' mode.
  </what-built>
  <how-to-verify>
    Start Storybook: `cd /home/caesar/git/all-chat/frontend && npx storybook dev -p 6006`

    Then verify these specific visual behaviors:

    1. **Card micro-interaction (COMP-04)**: Open UI/Card → Interactive story. Hover the card — confirm subtle scale up (~1.02x) and shadow lift. Should feel like a gentle lift, not a dramatic jump.

    2. **Button gradient (COMP-05)**: Add to URL: /story/ui-button--gradient OR find Button stories → Gradient story. Confirm the gradient flows from Twitch purple (#9146FF) on the left to TikTok teal (#69C9D0) on the right. Compare visually to the nav active underline gradient (if visible in Storybook layout) — they should match exactly.

    3. **Platform badges (COMP-06)**: Open UI/PlatformBadge. Check each platform:
       - Twitch: purple dot glow + purple TWITCH text
       - YouTube: red dot glow + red YOUTUBE text
       - Kick: green dot glow + green KICK text (bright neon green — should be visible but not harsh)
       - TikTok: teal dot glow + teal TIKTOK text
       The dot should have a soft glow halo effect (box-shadow), not just a solid dot. Background should be neutral dark, not colored.

    4. **Input focus ring**: Open UI/Input → Default story. Click the input — confirm a ring appears on focus matching the Button's focus ring style (ring-3 ring-ring/50).

    5. **Dialog backdrop**: Open UI/Dialog → Default story. Click "Open Dialog" — confirm frosted glass backdrop (slightly blurred background with dark overlay), dialog centered, close X button visible.

    6. **Toast visual appearance**: Open UI/Toast → Success, Error, Info stories. Confirm left border color coding (green for success, red for error). Background should be slightly elevated from base (bg-surface-2).

    7. **Skeleton pulse**: Open UI/Skeleton → Default story. Confirm the block pulses with a slow opacity fade (not a shimmer/wave — should be a calm pulse).
  </how-to-verify>
  <resume-signal>Type "approved" if all visual checks pass, or describe specific issues to fix (e.g., "gradient colors wrong — showing blue not purple", "badge glow not visible")</resume-signal>
</task>

</tasks>

<verification>
Phase 24 complete when:
1. `grep -n "test: 'error'" frontend/.storybook/preview.ts` — confirms error mode enabled
2. `cd frontend && npx vitest run --project storybook` — full suite green, zero a11y violations
3. `grep -n 'important' frontend/src/styles/events.css || echo "COMP-09 VERIFIED"` — zero matches
4. `ls frontend/src/components/ui/` — shows button.tsx, card.tsx, input.tsx, badge.tsx, dialog.tsx, toast.tsx, skeleton.tsx
5. `cd frontend && npx next build` — succeeds, bundle sizes within budget
6. Human visual verification approved
</verification>

<success_criteria>
- a11y test: 'error' mode enabled in Storybook, zero violations across all stories (COMP-01 through COMP-07)
- Performance budget documented: bundle size baseline captured, under 100KB delta (COMP-08)
- Human visual sign-off on micro-interactions, gradient CTA, and platform glow dots
- All nine COMP-0X requirements satisfied:
  - COMP-01: Card, Input, Badge, Dialog, Toast, Skeleton all exist
  - COMP-02: All components use design tokens (--color-* CSS variables via Tailwind token aliases)
  - COMP-03: CVA used for all components with variants
  - COMP-04: Card interactive variant with hover scale + shadow
  - COMP-05: Button gradient variant with #9146FF→#69C9D0
  - COMP-06: PlatformBadge with glow dot using PLATFORM_COLORS
  - COMP-07: Skeleton with animate-pulse bg-surface-2
  - COMP-08: Performance budget documented
  - COMP-09: Zero !important in events.css (verified in Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/24-component-library-setup-customization/24-05-SUMMARY.md`
</output>
