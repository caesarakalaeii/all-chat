---
phase: 01-discord-support-bot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/support-bot/package.json
  - services/support-bot/tsconfig.json
  - services/support-bot/vitest.config.ts
  - services/support-bot/src/types.ts
  - services/support-bot/src/index.ts
  - services/support-bot/src/claude/agent.ts
  - services/support-bot/src/github/issues.ts
  - services/support-bot/src/__tests__/agent.test.ts
  - services/support-bot/src/__tests__/github.test.ts
  - services/support-bot/src/__tests__/index.test.ts
autonomous: true
requirements:
  - BOT-03
  - BOT-04
  - BOT-05
  - BOT-06
user_setup:
  - service: anthropic
    why: "Claude API for answering questions"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com -> API Keys -> Create Key"
  - service: github
    why: "Creating issues for code change proposals"
    env_vars:
      - name: GITHUB_TOKEN
        source: "github.com -> Settings -> Developer settings -> Fine-grained tokens -> create with 'Issues: Write' on all-chat and all-chat-extension repos"
  - service: discord
    why: "Bot token for Discord gateway"
    env_vars:
      - name: DISCORD_BOT_TOKEN
        source: "discord.com/developers/applications -> New Application -> Bot -> Reset Token"

must_haves:
  truths:
    - "Claude agent queries codebase with read-only tools only (Read, Glob, Grep)"
    - "GitHub issues are created with title, body, and labels when code changes are proposed"
    - "Missing environment variables cause immediate process exit with error message"
    - "SIGTERM/SIGINT trigger graceful shutdown destroying Discord client"
  artifacts:
    - path: "services/support-bot/package.json"
      provides: "Project manifest with discord.js, @anthropic-ai/claude-agent-sdk, @octokit/rest, vitest"
      contains: "type.*module"
    - path: "services/support-bot/src/types.ts"
      provides: "Shared TypeScript interfaces for QueryResult, IssueProposal"
      exports: ["QueryResult", "IssueProposal"]
    - path: "services/support-bot/src/claude/agent.ts"
      provides: "Claude Agent SDK wrapper with read-only tool restriction"
      exports: ["queryCodebase"]
    - path: "services/support-bot/src/github/issues.ts"
      provides: "Octokit issue creation wrapper"
      exports: ["createIssue"]
    - path: "services/support-bot/src/index.ts"
      provides: "Entry point with env validation and graceful shutdown"
      contains: "process.exit"
  key_links:
    - from: "services/support-bot/src/claude/agent.ts"
      to: "@anthropic-ai/claude-agent-sdk"
      via: "import { query } from SDK"
      pattern: "allowedTools.*Read.*Glob.*Grep"
    - from: "services/support-bot/src/github/issues.ts"
      to: "@octokit/rest"
      via: "Octokit instance"
      pattern: "octokit\\.rest\\.issues\\.create"
---

<objective>
Scaffold the support-bot service and implement the core logic modules: Claude Agent SDK wrapper for read-only codebase queries, GitHub issue creation via Octokit, shared types, and the entry point with environment validation and graceful shutdown.

Purpose: Establishes the project foundation and all non-Discord modules so Plan 02 can focus purely on Discord integration and wiring.
Output: Working npm project with tested claude/agent.ts, github/issues.ts, index.ts, and shared types.
</objective>

<execution_context>
@/home/moersener/.claude/get-shit-done/workflows/execute-plan.md
@/home/moersener/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-discord-support-bot-that-answers-user-questions-with-codebase-awareness-proposes-code-changes-without-making-them-and-uses-claude-code-login-to-avoid-additional-charges/01-CONTEXT.md
@.planning/phases/01-discord-support-bot-that-answers-user-questions-with-codebase-awareness-proposes-code-changes-without-making-them-and-uses-claude-code-login-to-avoid-additional-charges/01-RESEARCH.md

<interfaces>
<!-- Existing discord-bot patterns to follow -->

From services/discord-bot/package.json:
```json
{
  "type": "module",
  "dependencies": {
    "discord.js": "^14.14.1"
  }
}
```

From services/discord-bot/src/index.js (shutdown pattern):
```javascript
process.on('SIGINT', shutdown);
process.on('SIGTERM', shutdown);
async function shutdown() {
  console.log('Shutting down...');
  discordClient.destroy();
  process.exit(0);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold project and implement core modules (types, agent, issues)</name>
  <files>
    services/support-bot/package.json,
    services/support-bot/tsconfig.json,
    services/support-bot/vitest.config.ts,
    services/support-bot/src/types.ts,
    services/support-bot/src/claude/agent.ts,
    services/support-bot/src/github/issues.ts,
    services/support-bot/src/__tests__/agent.test.ts,
    services/support-bot/src/__tests__/github.test.ts
  </files>
  <read_first>
    services/discord-bot/package.json,
    services/discord-bot/Dockerfile,
    .planning/phases/01-discord-support-bot-that-answers-user-questions-with-codebase-awareness-proposes-code-changes-without-making-them-and-uses-claude-code-login-to-avoid-additional-charges/01-RESEARCH.md
  </read_first>
  <behavior>
    - queryCodebase("how does twitch work?", ["/repos/all-chat"]) returns a string response (mocked SDK)
    - queryCodebase passes allowedTools: ["Read", "Glob", "Grep"] to the SDK query call — no Write, Edit, or Bash
    - createIssue("moersener", "all-chat", "Fix bug", "## Details\n...") returns a GitHub URL string
    - createIssue calls octokit.rest.issues.create with labels ["bot-proposed", "needs-review"]
    - IssueProposal type has fields: repo, title, body
    - QueryResult type has fields: answer (string), issueProposal (IssueProposal | null)
  </behavior>
  <action>
    1. Create `services/support-bot/` directory.

    2. Create `package.json`:
       - name: "all-chat-support-bot"
       - version: "1.0.0"
       - type: "module"
       - scripts.start: "node --import tsx src/index.ts" (tsx for TypeScript execution)
       - scripts.dev: "node --import tsx --watch src/index.ts"
       - scripts.test: "vitest run"
       - scripts.deploy-commands: "node --import tsx src/commands/deploy.ts"
       - dependencies: discord.js ^14.14.1, @anthropic-ai/claude-agent-sdk ^0.2.81, @octokit/rest ^22.0.1
       - devDependencies: typescript ^5.7.0, @types/node ^20.0.0, tsx ^4.0.0, vitest ^3.0.0

    3. Create `tsconfig.json`:
       - target: "ES2022", module: "NodeNext", moduleResolution: "NodeNext"
       - strict: true, esModuleInterop: true, skipLibCheck: true
       - outDir: "./dist", rootDir: "./src"
       - include: ["src/**/*"]

    4. Create `vitest.config.ts`:
       ```typescript
       import { defineConfig } from 'vitest/config';
       export default defineConfig({
         test: {
           globals: true,
           environment: 'node',
         },
       });
       ```

    5. Create `src/types.ts` with these exact exports:
       ```typescript
       export interface IssueProposal {
         repo: 'all-chat' | 'all-chat-extension';
         title: string;
         body: string;
       }

       export interface QueryResult {
         answer: string;
         issueProposal: IssueProposal | null;
       }

       export interface BotConfig {
         discordToken: string;
         anthropicApiKey: string;
         githubToken: string;
         githubOwner: string;
         allChatRepoPath: string;
         allChatExtensionRepoPath: string;
       }
       ```

    6. Create `src/claude/agent.ts`:
       - Import `query` from `@anthropic-ai/claude-agent-sdk`
       - Import `QueryResult` from `../types.js`
       - Export async function `queryCodebase(question: string, repoPaths: string[]): Promise<QueryResult>`
       - System prompt instructs Claude:
         - "You are a support bot for the All-Chat project."
         - "You help with: setup & configuration, architecture questions, and bug triage."
         - "You can read files at: {repoPaths joined}"
         - "Deployment/Kubernetes questions are out of scope — politely decline."
         - "If a code change is needed, end your response with exactly: PROPOSE_ISSUE:repo_name|||title|||body"
         - "repo_name must be 'all-chat' or 'all-chat-extension'"
       - Call `query({ prompt: question, options: { allowedTools: ['Read', 'Glob', 'Grep'], systemPrompt } })`
       - Iterate the async generator collecting the final result text
       - Parse the result: if it contains `PROPOSE_ISSUE:`, extract repo, title, body into IssueProposal; otherwise issueProposal = null
       - Return `{ answer: resultText, issueProposal }`

    7. Create `src/github/issues.ts`:
       - Import `Octokit` from `@octokit/rest`
       - Export function `createOctokitClient(token: string): Octokit` that returns `new Octokit({ auth: token })`
       - Export async function `createIssue(octokit: Octokit, owner: string, repo: string, title: string, body: string): Promise<string>`
       - createIssue calls `octokit.rest.issues.create({ owner, repo, title, body, labels: ['bot-proposed', 'needs-review'] })`
       - Returns `response.data.html_url`

    8. Write tests:
       - `src/__tests__/agent.test.ts`: Mock `@anthropic-ai/claude-agent-sdk` using vi.mock. Test that queryCodebase returns an answer string. Test that allowedTools is exactly ['Read', 'Glob', 'Grep']. Test that PROPOSE_ISSUE: in the response is parsed into an IssueProposal.
       - `src/__tests__/github.test.ts`: Mock `@octokit/rest`. Test that createIssue calls octokit.rest.issues.create with correct args including labels. Test it returns the html_url from the response.

    9. Run `cd services/support-bot && npm install && npm test` to verify all tests pass.
  </action>
  <verify>
    <automated>cd services/support-bot && npm test</automated>
  </verify>
  <acceptance_criteria>
    - services/support-bot/package.json contains `"type": "module"`
    - services/support-bot/package.json contains `"@anthropic-ai/claude-agent-sdk"`
    - services/support-bot/package.json contains `"@octokit/rest"`
    - services/support-bot/package.json contains `"discord.js"`
    - services/support-bot/package.json contains `"vitest"`
    - services/support-bot/src/types.ts contains `export interface IssueProposal`
    - services/support-bot/src/types.ts contains `export interface QueryResult`
    - services/support-bot/src/types.ts contains `export interface BotConfig`
    - services/support-bot/src/claude/agent.ts contains `allowedTools` with Read, Glob, Grep
    - services/support-bot/src/claude/agent.ts does NOT contain `Bash` or `Write` or `Edit` in allowedTools
    - services/support-bot/src/claude/agent.ts contains `export async function queryCodebase`
    - services/support-bot/src/claude/agent.ts contains `PROPOSE_ISSUE:`
    - services/support-bot/src/github/issues.ts contains `octokit.rest.issues.create`
    - services/support-bot/src/github/issues.ts contains `bot-proposed`
    - services/support-bot/src/__tests__/agent.test.ts exits 0
    - services/support-bot/src/__tests__/github.test.ts exits 0
  </acceptance_criteria>
  <done>
    package.json created with all dependencies, tsconfig.json configured for ESM, vitest configured, types.ts exports IssueProposal + QueryResult + BotConfig, agent.ts wraps Claude Agent SDK with read-only tools and PROPOSE_ISSUE parsing, issues.ts wraps Octokit for issue creation with bot-proposed label, all tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Entry point with env validation and graceful shutdown</name>
  <files>
    services/support-bot/src/index.ts,
    services/support-bot/src/__tests__/index.test.ts
  </files>
  <read_first>
    services/discord-bot/src/index.js,
    services/support-bot/src/types.ts
  </read_first>
  <behavior>
    - When DISCORD_BOT_TOKEN is missing, process.exit(1) is called with error message containing "DISCORD_BOT_TOKEN"
    - When ANTHROPIC_API_KEY is missing, process.exit(1) is called with error message containing "ANTHROPIC_API_KEY"
    - When GITHUB_TOKEN is missing, process.exit(1) is called with error message containing "GITHUB_TOKEN"
    - When ALL_CHAT_REPO_PATH is missing, process.exit(1) is called with error message containing "ALL_CHAT_REPO_PATH"
    - When ALL_CHAT_EXTENSION_REPO_PATH is missing, process.exit(1) is called with error message containing "ALL_CHAT_EXTENSION_REPO_PATH"
    - validateEnv() returns a BotConfig when all vars present
    - SIGTERM triggers shutdown function
  </behavior>
  <action>
    1. Create `src/index.ts`:
       - Import `BotConfig` from `./types.js`
       - Export function `validateEnv(): BotConfig` that checks these required env vars and calls `console.error` + `process.exit(1)` for each missing one:
         - DISCORD_BOT_TOKEN
         - ANTHROPIC_API_KEY
         - GITHUB_TOKEN
         - ALL_CHAT_REPO_PATH
         - ALL_CHAT_EXTENSION_REPO_PATH
       - Also read optional vars: GITHUB_OWNER (default "moersener"), DISCORD_CLIENT_ID (needed for slash command registration)
       - Returns a BotConfig object with all values
       - Export async function `shutdown(client?: { destroy: () => void })`: logs "Shutting down support-bot...", calls client?.destroy(), calls process.exit(0)
       - Register `process.on('SIGINT', () => shutdown(discordClient))` and `process.on('SIGTERM', () => shutdown(discordClient))`
       - Main flow: call validateEnv(), then import and call `startBot(config)` from `./bot.js` (bot.ts created in Plan 02 — for now, create a placeholder `src/bot.ts` that exports `async function startBot(config: BotConfig): Promise<void> { console.log('Bot starting...'); }`)
       - Wrap startBot in try/catch — on error log and process.exit(1)

    2. Write `src/__tests__/index.test.ts`:
       - Test validateEnv throws/exits when DISCORD_BOT_TOKEN missing
       - Test validateEnv throws/exits when ANTHROPIC_API_KEY missing
       - Test validateEnv returns BotConfig when all vars set
       - Mock process.exit and process.env for testing
       - Test shutdown calls client.destroy()

    3. Run `cd services/support-bot && npm test` to verify.
  </action>
  <verify>
    <automated>cd services/support-bot && npm test</automated>
  </verify>
  <acceptance_criteria>
    - services/support-bot/src/index.ts contains `export function validateEnv`
    - services/support-bot/src/index.ts contains `DISCORD_BOT_TOKEN`
    - services/support-bot/src/index.ts contains `ANTHROPIC_API_KEY`
    - services/support-bot/src/index.ts contains `GITHUB_TOKEN`
    - services/support-bot/src/index.ts contains `ALL_CHAT_REPO_PATH`
    - services/support-bot/src/index.ts contains `ALL_CHAT_EXTENSION_REPO_PATH`
    - services/support-bot/src/index.ts contains `process.exit(1)`
    - services/support-bot/src/index.ts contains `SIGTERM`
    - services/support-bot/src/index.ts contains `SIGINT`
    - services/support-bot/src/index.ts contains `shutdown`
    - services/support-bot/src/__tests__/index.test.ts exits 0
    - `cd services/support-bot && npm test` exits 0
  </acceptance_criteria>
  <done>
    index.ts validates all 5 required env vars with clear error messages, returns typed BotConfig, registers SIGINT/SIGTERM shutdown handlers that destroy the Discord client, placeholder bot.ts exists for Plan 02 to replace, all tests pass.
  </done>
</task>

</tasks>

<verification>
- `cd services/support-bot && npm test` — all unit tests pass (agent, github, index)
- `grep -r "allowedTools" services/support-bot/src/claude/agent.ts` shows Read, Glob, Grep only
- `grep -r "Bash\|Write\|Edit" services/support-bot/src/claude/agent.ts` returns nothing (security check)
- `grep "bot-proposed" services/support-bot/src/github/issues.ts` confirms label present
</verification>

<success_criteria>
- All 3 test files pass: agent.test.ts, github.test.ts, index.test.ts
- types.ts exports IssueProposal, QueryResult, BotConfig interfaces
- agent.ts restricts Claude to read-only tools (Read, Glob, Grep) — no Bash, Write, Edit
- issues.ts creates GitHub issues with bot-proposed label via Octokit
- index.ts validates 5 required env vars and exits on any missing
- Graceful shutdown on SIGTERM/SIGINT
</success_criteria>

<output>
After completion, create `.planning/phases/01-discord-support-bot-that-answers-user-questions-with-codebase-awareness-proposes-code-changes-without-making-them-and-uses-claude-code-login-to-avoid-additional-charges/01-01-SUMMARY.md`
</output>
