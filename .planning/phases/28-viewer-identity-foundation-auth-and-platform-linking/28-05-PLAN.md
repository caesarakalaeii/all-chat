---
phase: 28-viewer-identity-foundation-auth-and-platform-linking
plan: 05
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - frontend/src/app/settings/viewer/page.tsx
autonomous: true
requirements:
  - VID-03
  - VID-06

must_haves:
  truths:
    - "/settings/viewer route exists and is accessible to authenticated users"
    - "Page shows viewer display name and current auth status (viewer_id from JWT)"
    - "Page renders a name color section matching Phase 28 scope (picker stub; full editor is Phase 29)"
    - "Page has 'Linked Platforms' section showing current platform as Connected; multi-platform display is Phase 29 scope"
  artifacts:
    - path: "frontend/src/app/settings/viewer/page.tsx"
      provides: "Viewer Identity settings page stub"
  key_links:
    - from: "frontend/src/app/settings/viewer/page.tsx"
      to: "/api/v1/auth/viewer/me"
      via: "fetch viewer info from existing GET /viewer/me endpoint"
      pattern: "viewer/me"
---

<objective>
Create the minimal /settings/viewer page stub required by EXT-03 (Open Settings button destination) and as the foundation for the full cosmetics editor in Phase 29. Shows auth status, viewer identity info, and a placeholder for the color editor.

Purpose: EXT-03 requires the page to exist. Phase 29 will expand this page with the full color picker and gradient editor.
Output: A single Next.js page component that's navigable and shows viewer identity information.
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-viewer-identity-foundation-auth-and-platform-linking/28-CONTEXT.md
@.planning/phases/28-viewer-identity-foundation-auth-and-platform-linking/28-01-SUMMARY.md

<interfaces>
<!-- Existing settings page to match style -->

From frontend/src/app/settings/page.tsx — use same layout/auth pattern.
Design system: Tailwind v4 + design tokens from Phase 23, shadcn/ui components from Phase 24.
Auth context: use existing useAuth() hook or viewer JWT from localStorage (key: viewer_jwt).

Viewer JWT claims accessible via decode:
  viewer_id, display_name, username, avatar_url, platform
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /settings/viewer page stub</name>
  <files>frontend/src/app/settings/viewer/page.tsx</files>
  <action>
Create `frontend/src/app/settings/viewer/page.tsx`. This is a client component (`'use client'`).

The page should:

1. **Read viewer JWT from localStorage** (`localStorage.getItem('viewer_jwt')`). If no JWT, show an unauthenticated state with sign-in links pointing to Twitch/YouTube/Kick login flows.

2. **Decode JWT** (same one-liner pattern: `JSON.parse(atob(token.split('.')[1]))`). If expired, treat as unauthenticated.

3. **Render sections:**

   **Header section:**
   - `<h1>Viewer Identity</h1>` with subtitle "Customize how your name appears across all overlays"
   - If authenticated: show avatar (if present) + display name + platform badge (small colored pill showing "twitch"/"youtube"/"kick")

   **Name Color section (Phase 28 stub):**
   - Section heading: "Name Color"
   - Description: "Set a fallback color for when your platform doesn't provide one (YouTube, Kick, TikTok)"
   - Show a `<input type="color">` that reads/writes from the viewer JWT's name_color (fetched from GET /api/viewer/me or from localStorage `name_color`)
   - On color change: call PATCH `/api/viewer/cosmetics` (same pattern as extension popup)
   - Note: The full gradient editor will be added in Phase 29

   **Linked Platforms section:**
   - Section heading: "Linked Platforms"
   - Description: "Sign in with each platform to enable cosmetics across all your chats"
   - Shows three rows for Twitch, YouTube, Kick
   - Each row: platform icon color dot + platform name + "Connected" badge (if JWT platform matches) or "Connect" button (that links to `/api/v1/auth/viewer/{platform}/login`)
   - Phase 28 scope: only the current JWT's platform shows as "Connected". All other platforms show "Connect". Multi-platform linking where a second platform links to the same viewer_id is Phase 29.

4. **Layout**: Use the same card/section pattern as the existing settings page. Dark theme consistent with Phase 23/24 design system. Class names from Tailwind v4.

The page should be under `frontend/src/app/settings/viewer/page.tsx`. No new route configuration needed — Next.js App Router handles it automatically via the directory structure.

Check `frontend/src/app/settings/page.tsx` first to understand the existing layout pattern and replicate it.

Keep this page intentionally minimal — it's a Phase 28 stub. Phase 29 adds the full color + gradient editor.
  </action>
  <verify>
    <automated>test -f /home/caesar/git/all-chat/frontend/src/app/settings/viewer/page.tsx && cd /home/caesar/git/all-chat/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <done>/settings/viewer/page.tsx exists; TypeScript compilation passes (no type errors in the new file); page is a valid Next.js client component</done>
</task>

</tasks>

<verification>
1. `test -f /home/caesar/git/all-chat/frontend/src/app/settings/viewer/page.tsx` — file exists
2. `cd /home/caesar/git/all-chat/frontend && npx tsc --noEmit` — no new type errors
3. Page has `'use client'` directive
4. Page renders without crashing when viewer_jwt is absent (shows unauthenticated state)
5. Page renders without crashing when viewer_jwt is present (shows viewer info)
</verification>

<success_criteria>
- /settings/viewer route exists in Next.js App Router
- Page handles unauthenticated state gracefully (no crash, shows sign-in prompt)
- Page shows viewer display name, platform badge, name color section, and linked platforms section
- Linked Platforms section shows only current JWT platform as "Connected" (Phase 28 scope; multi-platform is Phase 29)
- TypeScript compiles without errors
- Design matches existing settings page style (dark theme, card layout)
</success_criteria>

<output>
After completion, create `.planning/phases/28-viewer-identity-foundation-auth-and-platform-linking/28-05-SUMMARY.md`
</output>
