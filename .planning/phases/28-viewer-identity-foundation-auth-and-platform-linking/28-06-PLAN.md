---
phase: 28-viewer-identity-foundation-auth-and-platform-linking
plan: 06
type: execute
wave: 6
depends_on: [28-05]
files_modified:
  - all-chat-extension/src/content-scripts/base/PlatformDetector.ts
  - all-chat-extension/src/content-scripts/twitch.ts
  - all-chat-extension/src/content-scripts/youtube.ts
  - all-chat-extension/src/content-scripts/kick.ts
  - all-chat-extension/src/popup/popup.tsx
autonomous: false
gap_closure: true
requirements: [EXT-01, EXT-02, EXT-03, EXT-04, VID-05, VID-06]

must_haves:
  truths:
    - "Content scripts write current_platform to chrome.storage.session on page load (twitch, youtube, kick)"
    - "Popup shows only the matching platform sign-in button when current_platform is set; shows all three when not on a supported platform"
    - "Color picker has a reset-to-default control that sends null to SAVE_NAME_COLOR and restores #ffffff"
  artifacts:
    - path: "all-chat-extension/src/content-scripts/twitch.ts"
      provides: "chrome.storage.session.set({ current_platform: 'twitch' }) call in initialize()"
      contains: "current_platform"
    - path: "all-chat-extension/src/content-scripts/youtube.ts"
      provides: "chrome.storage.session.set({ current_platform: 'youtube' }) call in initialize()"
      contains: "current_platform"
    - path: "all-chat-extension/src/content-scripts/kick.ts"
      provides: "chrome.storage.session.set({ current_platform: 'kick' }) call in initialize()"
      contains: "current_platform"
    - path: "all-chat-extension/src/popup/popup.tsx"
      provides: "currentPlatform state read from chrome.storage.session, filtered button list, reset button"
      contains: "current_platform"
  key_links:
    - from: "twitch.ts initialize()"
      to: "chrome.storage.session"
      via: "chrome.storage.session.set({ current_platform: 'twitch' })"
      pattern: "current_platform.*twitch"
    - from: "popup.tsx useEffect"
      to: "chrome.storage.session"
      via: "chrome.storage.session.get(['current_platform'])"
      pattern: "current_platform"
    - from: "popup.tsx handleColorReset"
      to: "service-worker SAVE_NAME_COLOR"
      via: "chrome.runtime.sendMessage({ type: 'SAVE_NAME_COLOR', color: null })"
      pattern: "SAVE_NAME_COLOR.*null"
---

<objective>
Close three verification gaps in the browser extension UI layer:

1. Content scripts (twitch.ts, youtube.ts, kick.ts) must write `current_platform` to `chrome.storage.session` on page load so the popup knows which platform the user is currently on.
2. The popup must read `current_platform` from `chrome.storage.session` and show only the matching sign-in button (or all three if not on a supported platform).
3. The color picker must gain a reset-to-default control that clears `viewer_name_color` by sending `{ color: null }` to the existing `SAVE_NAME_COLOR` handler.

Purpose: Satisfy EXT-01 (reset-to-default option) fully and fix context-aware popup behavior (verification truths #14 and #15 that failed in 28-VERIFICATION.md).

Output: Three modified extension TypeScript files. No new files, no backend changes.
</objective>

<execution_context>
@/home/moersener/.claude/get-shit-done/workflows/execute-plan.md
@/home/moersener/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-viewer-identity-foundation-auth-and-platform-linking/28-VERIFICATION.md

<interfaces>
<!-- Key types and patterns the executor needs. No codebase exploration required. -->

From all-chat-extension/src/content-scripts/twitch.ts:
- Module-level `initialize()` function (lines 144–174) is the entry point. It creates `globalDetector = new TwitchDetector()` then calls `globalDetector.init()`. The session write must happen BEFORE calling `init()`, right after the extensionEnabled guard.
- `chrome.runtime.getManifest()` is called at line 145 — place session write after that, before `globalDetector.init()` at line 163/166.

From all-chat-extension/src/content-scripts/youtube.ts:
- Module-level `initialize()` function (lines 229–252). Creates `globalDetector = new YouTubeDetector()` then awaits `waitForElement` then calls `globalDetector.init()`. Session write goes after `globalDetector = new YouTubeDetector()` (line 239), before the waitForElement call.

From all-chat-extension/src/content-scripts/kick.ts:
- Same pattern — module-level `initialize()` creates a KickDetector instance. Session write goes in the same position.

From all-chat-extension/src/popup/popup.tsx:
- State: `const [nameColor, setNameColor] = useState<string>('#ffffff')` (line 16)
- `useEffect` load (lines 20–34): reads getSyncStorage + getLocalStorage. Add `chrome.storage.session.get(['current_platform'])` here; store result in new `currentPlatform` state (`string | null`).
- handleColorChange (lines 53–68): sends `{ type: 'SAVE_NAME_COLOR', color }`. The reset handler sends `{ type: 'SAVE_NAME_COLOR', color: null }` — same message, null color.
- Sign-in button list (lines 174–181): currently `(['twitch', 'youtube', 'kick'] as const).map(...)`. Change to: derive `platformsToShow` from `currentPlatform` — if currentPlatform is one of the three supported values show only that one; otherwise show all three.
- Reset button placement: adjacent to the color `<input>` at lines 155–163. Add a `<button>` after the color input and before the saveStatus spans. Style it to match the existing small text buttons (fontSize 11px, color #adadb8).

From all-chat-extension/src/background/service-worker.ts:
- `saveNameColor(color: string | null)` at line 467: already handles null — removes from local storage and sends `{ name_color: null }` to PATCH cosmetics. No changes needed in service-worker.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write current_platform to chrome.storage.session in all three content scripts</name>
  <files>
    all-chat-extension/src/content-scripts/twitch.ts,
    all-chat-extension/src/content-scripts/youtube.ts,
    all-chat-extension/src/content-scripts/kick.ts
  </files>
  <action>
In each content script's module-level `initialize()` function, add a `chrome.storage.session.set({ current_platform: ... })` call immediately after the `extensionEnabled` guard returns and before `globalDetector.init()` is called. The write must be fire-and-forget (no await needed, log on error).

**twitch.ts** — in `initialize()` at approximately line 155 (after `globalDetector = new TwitchDetector()` is assigned and `setupGlobalMessageRelay()` is called), add:
```typescript
chrome.storage.session.set({ current_platform: 'twitch' }).catch((err: unknown) => {
  console.warn('[AllChat Twitch] Failed to write current_platform to session:', err);
});
```
Place it before the `document.readyState` check (line 161).

**youtube.ts** — in `initialize()` at approximately line 239 (after `globalDetector = new YouTubeDetector()` is assigned), add:
```typescript
chrome.storage.session.set({ current_platform: 'youtube' }).catch((err: unknown) => {
  console.warn('[AllChat YouTube] Failed to write current_platform to session:', err);
});
```
Place it before the `waitForElement` call (line 247).

**kick.ts** — apply the same pattern in the kick `initialize()` function, after the KickDetector instance is created and before `globalDetector.init()` is called. Use `'kick'` as the platform value.

Do NOT add this to the base `PlatformDetector.ts` — that class does not know when init is decided, and YouTube/Kick guard against non-live pages before proceeding. The session write signals "user is on this platform page" not "All-Chat UI was injected", so it should fire even when the streamer is not configured.

Verify the platform string literals exactly match the `platform` property in each detector class (`'twitch'`, `'youtube'`, `'kick'`).
  </action>
  <verify>
    <automated>cd /home/moersener/Hobby/all-chat-extension && grep -n "current_platform" src/content-scripts/twitch.ts src/content-scripts/youtube.ts src/content-scripts/kick.ts</automated>
  </verify>
  <done>
    Each of twitch.ts, youtube.ts, and kick.ts contains a `chrome.storage.session.set({ current_platform: '...' })` call with the correct platform string in the `initialize()` function. The TypeScript build succeeds (`npm run build` exits 0 in the extension directory).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add currentPlatform state and context-aware sign-in buttons to popup.tsx</name>
  <files>all-chat-extension/src/popup/popup.tsx</files>
  <action>
Three targeted changes to `popup.tsx`:

**Change A — new state for current platform (after line 17):**
Add a new state variable after `saveStatus`:
```typescript
const [currentPlatform, setCurrentPlatform] = useState<string | null>(null);
```

**Change B — read current_platform in useEffect (inside the `load` async function, after line 26):**
After `setNameColor(local.viewer_name_color || '#ffffff')`, add:
```typescript
const sessionData = await chrome.storage.session.get(['current_platform']) as { current_platform?: string };
setCurrentPlatform(sessionData.current_platform ?? null);
```

**Change C — context-aware platform button list (replace line 175):**
Replace the hard-coded array literal with a derived list. The existing line is:
```tsx
{(['twitch', 'youtube', 'kick'] as const).map((p) => (
```
Replace with:
```tsx
{((['twitch', 'youtube', 'kick'] as const).filter(
  (p) => currentPlatform === null || currentPlatform === p
)).map((p) => (
```
Leave the rest of the map body unchanged. This shows all three when `currentPlatform` is null (not on a supported platform), and only the matching button when it is set.

**Change D — reset-to-default button for color picker (after line 159):**
Add a `handleColorReset` function alongside `handleColorChange` (near line 53). Place it after `handleColorChange`:
```typescript
const handleColorReset = async () => {
  setNameColor('#ffffff');
  setSaveStatus('saving');
  try {
    await chrome.runtime.sendMessage({ type: 'SAVE_NAME_COLOR', color: null });
    setSaveStatus('saved');
    setTimeout(() => setSaveStatus(''), 1500);
  } catch (err) {
    console.error('Failed to reset color:', err);
    setSaveStatus('');
  }
};
```

In the JSX color picker section (lines 153–163), add a reset button adjacent to the color input. After the `<input type="color" .../>` and before the saveStatus spans:
```tsx
<button
  onClick={handleColorReset}
  title="Reset to default"
  style={{ fontSize: '11px', color: '#adadb8', background: 'none', border: 'none', cursor: 'pointer', padding: '0 4px' }}
>
  ↺
</button>
```

Do NOT change any other part of the file. The SAVE_NAME_COLOR handler in service-worker.ts already handles `null` correctly — no backend changes needed.
  </action>
  <verify>
    <automated>cd /home/moersener/Hobby/all-chat-extension && grep -n "current_platform\|currentPlatform\|handleColorReset\|SAVE_NAME_COLOR.*null\|color: null" src/popup/popup.tsx</automated>
  </verify>
  <done>
    popup.tsx contains: (1) `currentPlatform` state initialized to null, (2) `chrome.storage.session.get(['current_platform'])` call in useEffect, (3) filter logic on the platform array using `currentPlatform`, (4) `handleColorReset` function sending `{ type: 'SAVE_NAME_COLOR', color: null }`, (5) reset button (↺) adjacent to the color input. TypeScript build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of all three gap-closure changes</name>
  <what-built>
    Three gap-closure changes in the browser extension:
    1. twitch.ts, youtube.ts, kick.ts now write current_platform to chrome.storage.session on page load.
    2. popup.tsx reads current_platform and shows only the matching sign-in button (or all three when not on a platform page).
    3. popup.tsx color picker has a ↺ reset button that clears viewer_name_color and sends null to PATCH cosmetics.
  </what-built>
  <how-to-verify>
    1. Build the extension: `cd /home/moersener/Hobby/all-chat-extension && npm run build` — must exit 0 with no TypeScript errors.

    2. Load the built extension in Chrome (chrome://extensions → Load unpacked → select dist/ folder).

    3. Navigate to twitch.tv (any channel page). Open the extension popup. In signed-out state, verify ONLY "Sign in with Twitch" is shown (not YouTube or Kick buttons).

    4. Navigate to youtube.com/watch (any live stream). Open popup. Verify ONLY "Sign in with YouTube" is shown.

    5. Navigate to a plain web page (e.g. google.com). Open popup. Verify ALL THREE sign-in buttons appear (fallback when no platform detected).

    6. Sign in with any platform. In signed-in state, verify the color picker shows a ↺ symbol adjacent to the color swatch. Click ↺ — verify the color resets to white (#ffffff) and a brief "Saved" indicator appears.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all six checks passed, or describe any issues found.</resume-signal>
  <action>Load the built extension in Chrome and run the six verification checks described in how-to-verify.</action>
  <verify><automated>cd /home/moersener/Hobby/all-chat-extension && npm run build</automated></verify>
  <done>All six manual checks pass and human types "approved".</done>
</task>

</tasks>

<verification>
- `npm run build` in all-chat-extension exits 0 (no TypeScript errors)
- `grep -n "current_platform" all-chat-extension/src/content-scripts/twitch.ts` returns a line with `chrome.storage.session.set`
- `grep -n "current_platform" all-chat-extension/src/content-scripts/youtube.ts` returns a line with `chrome.storage.session.set`
- `grep -n "current_platform" all-chat-extension/src/content-scripts/kick.ts` returns a line with `chrome.storage.session.set`
- `grep -n "currentPlatform\|current_platform" all-chat-extension/src/popup/popup.tsx` returns state declaration, session.get call, and filter expression
- `grep -n "handleColorReset\|↺" all-chat-extension/src/popup/popup.tsx` returns function definition and button JSX
</verification>

<success_criteria>
- All three content scripts write `current_platform` to `chrome.storage.session` on page load
- Popup shows platform-filtered sign-in buttons based on `chrome.storage.session` read
- Color picker ↺ reset button exists and triggers `SAVE_NAME_COLOR` with `color: null`
- Extension TypeScript build passes without errors
- Human verification checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/28-viewer-identity-foundation-auth-and-platform-linking/28-06-SUMMARY.md`
</output>
