---
phase: 31-all-chat-platform-badges
plan: "03"
type: execute
wave: 2
depends_on:
  - "31-02"
files_modified:
  - all-chat-extension/src/ui/components/AllChatBadge.tsx
  - all-chat-extension/src/ui/components/PremiumBadge.tsx
  - all-chat-extension/src/lib/badgeOrder.ts
  - all-chat-extension/src/ui/components/ChatContainer.tsx
autonomous: true
requirements:
  - BADGE-01
  - BADGE-02
  - BADGE-03

must_haves:
  truths:
    - "Extension ChatContainer renders AllChatBadge and PremiumBadge by badge name (not dropped silently)"
    - "Extension badgeOrder.ts has allchat:-2, premium:-1 in ROLE_PRIORITIES"
    - "AllChatBadge and PremiumBadge components exist in the extension with identical props interface"
  artifacts:
    - path: "all-chat-extension/src/ui/components/AllChatBadge.tsx"
      provides: "Extension AllChatBadge component"
      exports: ["AllChatBadge"]
    - path: "all-chat-extension/src/ui/components/PremiumBadge.tsx"
      provides: "Extension PremiumBadge component"
      exports: ["PremiumBadge"]
    - path: "all-chat-extension/src/lib/badgeOrder.ts"
      provides: "Extension sortBadges with allchat/premium priorities"
      contains: "allchat: -2"
    - path: "all-chat-extension/src/ui/components/ChatContainer.tsx"
      provides: "Extension chat renderer with name-check badge render"
      contains: "badge.name === 'allchat'"
  key_links:
    - from: "ChatContainer.tsx badge render block"
      to: "AllChatBadge / PremiumBadge components"
      via: "badge.name === 'allchat' || badge.name === 'premium' conditional"
      pattern: "badge.name.*allchat"
    - from: "extension badgeOrder.ts"
      to: "sortBadges output order"
      via: "ROLE_PRIORITIES negative ranks"
      pattern: "allchat.*-2"
---

<objective>
Mirror the frontend badge changes into the browser extension: add AllChatBadge and PremiumBadge components, extend badgeOrder.ts ROLE_PRIORITIES with allchat:-2/premium:-1, and update ChatContainer.tsx badge render block to use the 3-way name-check pattern.

Purpose: Extension badge rendering parity with overlay — without this, All-Chat badges are silently dropped in the extension because the current render path requires a truthy icon_url.
Output: Two new component files in extension, updated extension badgeOrder.ts, updated ChatContainer.tsx.
</objective>

<execution_context>
@/home/moersener/.claude/get-shit-done/workflows/execute-plan.md
@/home/moersener/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-all-chat-platform-badges/31-CONTEXT.md
@.planning/phases/31-all-chat-platform-badges/31-RESEARCH.md
@.planning/phases/31-all-chat-platform-badges/31-02-SUMMARY.md

<interfaces>
<!-- Key contracts extracted from extension codebase. -->

Extension InfinityLogo (all-chat-extension/src/ui/components/InfinityLogo.tsx):
- Identical implementation to frontend InfinityLogo
- AllChatBadge in extension mirrors the same pattern: wrap InfinityLogo at size=18
- Extension is NOT a Next.js app — no 'use client' directive needed (React 18 but no Next.js App Router)

Extension ChatContainer badge render (all-chat-extension/src/ui/components/ChatContainer.tsx ~line 454):
- Current pattern: only renders badge if badge.icon_url is truthy → All-Chat badges (empty icon_url) are silently dropped
- Extension uses native <img> tag, not Next.js <Image> component
- Replace with 3-way name-check:
  badge.name === 'allchat' → <AllChatBadge size={18} title={badge.name} />
  badge.name === 'premium' → <PremiumBadge size={18} title={badge.name} />
  badge.icon_url truthy → <img src={badge.icon_url} alt={badge.name} style={{height:'1em',width:'auto'}} title={badge.name} />
  else → (skip or span fallback)

Extension badgeOrder.ts (all-chat-extension/src/lib/badgeOrder.ts):
- Identical to frontend badgeOrder.ts — same ROLE_PRIORITIES, sortBadges export
- Add allchat:-2, premium:-1 identically to Plan 02 changes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AllChatBadge + PremiumBadge components in extension + badgeOrder extension</name>
  <files>
    all-chat-extension/src/ui/components/AllChatBadge.tsx
    all-chat-extension/src/ui/components/PremiumBadge.tsx
    all-chat-extension/src/lib/badgeOrder.ts
  </files>
  <action>
1. Read all-chat-extension/src/ui/components/InfinityLogo.tsx to confirm its exact export signature before writing AllChatBadge.

2. Create all-chat-extension/src/ui/components/AllChatBadge.tsx:
   - No 'use client' directive (extension environment — not Next.js App Router)
   - Import InfinityLogo from './InfinityLogo'
   - Export function AllChatBadge({ size = 18, title }: { size?: number; title?: string })
   - Return <span title={title} aria-label="All-Chat badge" style={{display:'inline-flex',alignItems:'center',flexShrink:0}}><InfinityLogo size={size} /></span>
   - Use inline styles (not Tailwind) — extension environment may not have Tailwind (per Phase 30 decision: "Extension UserAvatar uses inline styles not Tailwind for non-Tailwind extension environment")

3. Create all-chat-extension/src/ui/components/PremiumBadge.tsx:
   - No 'use client' directive
   - Export function PremiumBadge({ size = 18, title }: { size?: number; title?: string })
   - Mirror the identical SVG from frontend/src/components/PremiumBadge.tsx exactly (same gem polygon, same colors)
   - Use inline styles for className alternatives if needed

4. Extend all-chat-extension/src/lib/badgeOrder.ts:
   - Read the current file first to confirm its exact structure (should be identical to frontend version)
   - Add to ROLE_PRIORITIES: allchat: -2, premium: -1 (same as Plan 02)
   - No other changes
  </action>
  <verify>
    <automated>cd /home/moersener/Hobby/all-chat-extension && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>AllChatBadge.tsx and PremiumBadge.tsx exist in extension components. badgeOrder.ts has allchat:-2, premium:-1. Extension TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: ChatContainer.tsx badge render update with 3-way name-check</name>
  <files>
    all-chat-extension/src/ui/components/ChatContainer.tsx
  </files>
  <action>
1. Read all-chat-extension/src/ui/components/ChatContainer.tsx to locate the exact badge render block (approximately line 454 based on research).

2. Add imports at the top of the file (after existing imports):
   import { AllChatBadge } from './AllChatBadge'
   import { PremiumBadge } from './PremiumBadge'

3. Replace the existing badge render block with the 3-way name-check pattern.
   Current pattern (only renders if icon_url truthy — drops All-Chat badges):
   {badge.icon_url && <img src={badge.icon_url} ... />}

   New pattern:
   {badge.name === 'allchat' ? (
     <AllChatBadge key={idx} size={18} title={badge.name} />
   ) : badge.name === 'premium' ? (
     <PremiumBadge key={idx} size={18} title={badge.name} />
   ) : badge.icon_url ? (
     <img
       key={idx}
       src={badge.icon_url}
       alt={badge.name}
       style={{ height: '1em', width: 'auto', objectFit: 'contain', display: 'inline-block' }}
       title={badge.name}
     />
   ) : null}

   Note: Extension uses native <img> not Next.js <Image>. Use inline style for sizing (not h-[1em] Tailwind class).
   Note: If the existing block is inside a .map(), preserve the key prop and the surrounding map structure — only replace the inner conditional.
  </action>
  <verify>
    <automated>cd /home/moersener/Hobby/all-chat-extension && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ChatContainer.tsx has badge.name === 'allchat' conditional. AllChatBadge and PremiumBadge are imported. Extension TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `cd /home/moersener/Hobby/all-chat-extension && npx tsc --noEmit` — no TypeScript errors
- Confirm all-chat-extension/src/ui/components/AllChatBadge.tsx exists
- Confirm all-chat-extension/src/ui/components/PremiumBadge.tsx exists
- Confirm all-chat-extension/src/lib/badgeOrder.ts contains 'allchat: -2'
- Confirm all-chat-extension/src/ui/components/ChatContainer.tsx contains "badge.name === 'allchat'"
</verification>

<success_criteria>
- AllChatBadge and PremiumBadge components exist in extension with inline styles (not Tailwind)
- Extension badgeOrder.ts ROLE_PRIORITIES has allchat:-2, premium:-1
- ChatContainer.tsx renders AllChatBadge for badge.name==='allchat' (not silently dropped)
- ChatContainer.tsx renders PremiumBadge for badge.name==='premium'
- Native img tags in ChatContainer use inline height:1em sizing
- Extension TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/31-all-chat-platform-badges/31-03-SUMMARY.md`
</output>
