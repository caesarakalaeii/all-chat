---
phase: 08-observability-production-readiness
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-03"]
files_modified:
  - deployments/k8s/monitoring/grafana-dashboards/sharding-overview.json
  - deployments/k8s/monitoring/alerts/allchat-critical-alerts.yaml
  - deployments/k8s/monitoring/alerts/allchat-warning-alerts.yaml
autonomous: false
requirements: [METRICS-04, METRICS-06, METRICS-07, METRICS-08, METRICS-09, METRICS-10, METRICS-11]

must_haves:
  truths:
    - "Grafana dashboard visualizes channel distribution across pods as heatmap"
    - "Dashboard shows rebalancing timeline with event markers and details"
    - "Prometheus alerts fire when imbalance ratio exceeds 0.7 for 10 minutes"
    - "Split-brain alert fires immediately when multiple coordinators detected"
    - "Rebalancing thrashing alert fires after >5 rebalances in 30 minutes"
  artifacts:
    - path: "deployments/k8s/monitoring/grafana-dashboards/sharding-overview.json"
      provides: "Comprehensive dashboard with heatmaps and timelines"
      contains: "Channel Distribution: Pod × Time"
    - path: "deployments/k8s/monitoring/alerts/allchat-critical-alerts.yaml"
      provides: "Critical alerts (split-brain, imbalance)"
      contains: "ShardCoordinatorSplitBrain"
    - path: "deployments/k8s/monitoring/alerts/allchat-warning-alerts.yaml"
      provides: "Warning alerts (thrashing)"
      contains: "ShardRebalancingThrashing"
  key_links:
    - from: "Grafana dashboard panels"
      to: "Prometheus metrics from 08-01"
      via: "PromQL queries for shard_channel_count, shard_migration_total"
      pattern: "shard_channel_count|shard_migration_total"
    - from: "Prometheus alerts"
      to: "Grafana annotations"
      via: "ALERTS metric visible as event markers"
      pattern: "ALERTS\\{alertname="
---

<objective>
Create production-ready Grafana dashboard and Prometheus alerts for sharding observability, completing Phase 8 requirements.

Purpose: Provide operators with single-pane-of-glass visibility into channel distribution, rebalancing operations, and critical system health via comprehensive dashboard and actionable alerts with remediation steps.

Output:
- Grafana dashboard JSON with heatmaps (pod × time), rebalancing timeline, migration events table, at-a-glance imbalance ratio
- Prometheus critical alerts (split-brain, high imbalance)
- Prometheus warning alerts (rebalancing thrashing)
- Alert descriptions with remediation steps per CONTEXT.md requirement
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-observability-production-readiness/08-CONTEXT.md
@.planning/phases/08-observability-production-readiness/08-RESEARCH.md

# Metrics from prior plans
@.planning/phases/08-observability-production-readiness/08-01-PLAN.md

# Existing alert structure
@deployments/k8s/monitoring/alerts/allchat-critical-alerts.yaml
@deployments/k8s/monitoring/alerts/allchat-warning-alerts.yaml
</context>

<tasks>

<task type="auto">
  <name>Create Grafana sharding dashboard</name>
  <files>deployments/k8s/monitoring/grafana-dashboards/sharding-overview.json</files>
  <action>
Create comprehensive Grafana dashboard JSON with panels per CONTEXT.md requirements:

**Dashboard structure:**
```json
{
  "dashboard": {
    "title": "Sharding & Rebalancing Overview",
    "tags": ["allchat", "sharding", "rebalancing"],
    "timezone": "browser",
    "refresh": "30s",
    "panels": [ /* panels below */ ]
  }
}
```

**Panel 1: At-a-glance imbalance ratio (primary view per CONTEXT.md discretion):**
```json
{
  "id": 1,
  "type": "stat",
  "title": "Load Imbalance Ratio",
  "gridPos": {"h": 4, "w": 6, "x": 0, "y": 0},
  "targets": [{
    "expr": "shard_imbalance_ratio",
    "refId": "A"
  }],
  "fieldConfig": {
    "defaults": {
      "thresholds": {
        "mode": "absolute",
        "steps": [
          {"value": 0, "color": "green"},
          {"value": 0.5, "color": "yellow"},
          {"value": 0.7, "color": "red"}
        ]
      },
      "unit": "short",
      "decimals": 2
    }
  }
}
```

**Panel 2: Pod health stats:**
```json
{
  "id": 2,
  "type": "stat",
  "title": "Healthy Pods",
  "gridPos": {"h": 4, "w": 3, "x": 6, "y": 0},
  "targets": [{
    "expr": "shard_healthy_pods",
    "refId": "A"
  }]
}
```

**Panel 3: Failed pods:**
```json
{
  "id": 3,
  "type": "stat",
  "title": "Failed Pods",
  "gridPos": {"h": 4, "w": 3, "x": 9, "y": 0},
  "targets": [{
    "expr": "shard_failed_pods",
    "refId": "A"
  }],
  "fieldConfig": {
    "defaults": {
      "thresholds": {
        "steps": [
          {"value": 0, "color": "green"},
          {"value": 1, "color": "red"}
        ]
      }
    }
  }
}
```

**Panel 4: Channel distribution heatmap (Pod × Time) - per CONTEXT.md:**
```json
{
  "id": 4,
  "type": "heatmap",
  "title": "Channel Distribution Over Time: Pod × Time",
  "gridPos": {"h": 8, "w": 12, "x": 0, "y": 4},
  "targets": [{
    "expr": "shard_channel_count",
    "refId": "A",
    "legendFormat": "{{ pod_id }}"
  }],
  "options": {
    "calculate": true,
    "cellGap": 2,
    "color": {
      "mode": "scheme",
      "scheme": "Spectral"
    },
    "yAxis": {
      "axisLabel": "Pod ID",
      "decimals": 0
    }
  }
}
```

**Panel 5: Rebalancing events timeline - per CONTEXT.md (event markers + dedicated timeline):**
```json
{
  "id": 5,
  "type": "graph",
  "title": "Rebalancing Events Timeline",
  "gridPos": {"h": 8, "w": 12, "x": 12, "y": 4},
  "targets": [{
    "expr": "increase(shard_rebalancing_total[5m])",
    "refId": "A",
    "legendFormat": "Rebalancing Rate"
  }],
  "annotations": {
    "list": [{
      "name": "Rebalancing Triggers",
      "datasource": "Prometheus",
      "expr": "ALERTS{alertname=\"ShardLoadImbalance\"}",
      "titleFormat": "Imbalance Alert",
      "textFormat": "Ratio: {{ $value }}"
    }]
  }
}
```

**Panel 6: Migration events table:**
```json
{
  "id": 6,
  "type": "table",
  "title": "Recent Migration Events",
  "gridPos": {"h": 6, "w": 12, "x": 0, "y": 12},
  "targets": [{
    "expr": "shard_migration_total",
    "refId": "A",
    "format": "table",
    "instant": true
  }],
  "transformations": [{
    "id": "organize",
    "options": {
      "renameByName": {
        "status": "Status",
        "reason": "Reason",
        "Value": "Count"
      }
    }
  }]
}
```

**Panel 7: Per-pod load scores (time series):**
```json
{
  "id": 7,
  "type": "graph",
  "title": "Per-Pod Load Scores",
  "gridPos": {"h": 6, "w": 12, "x": 12, "y": 12},
  "targets": [{
    "expr": "shard_pod_load_score",
    "refId": "A",
    "legendFormat": "{{ pod_id }}"
  }]
}
```

Follow template from RESEARCH.md lines 816-925. Adjust gridPos for proper layout (12-column grid). Set refresh to 30s per observability patterns.

Create as new file, not modifying existing dashboards.
  </action>
  <verify>
JSON validation:
```bash
cat deployments/k8s/monitoring/grafana-dashboards/sharding-overview.json | jq . > /dev/null && echo "Valid JSON"
```

Panel count check:
```bash
jq '.dashboard.panels | length' deployments/k8s/monitoring/grafana-dashboards/sharding-overview.json
# Should output: 7
```

Metric reference check:
```bash
grep -o "shard_[a-z_]*" deployments/k8s/monitoring/grafana-dashboards/sharding-overview.json | sort -u
# Should list: shard_channel_count, shard_imbalance_ratio, shard_migration_total, etc.
```
  </verify>
  <done>
- Dashboard JSON is valid and complete
- 7 panels: imbalance ratio stat, pod health stats, heatmap, timeline, migration table, load scores
- Heatmap shows Pod × Time distribution per CONTEXT.md
- Timeline has event markers for rebalancing alerts
- At-a-glance imbalance ratio as primary view
  </done>
</task>

<task type="auto">
  <name>Add Prometheus critical alerts</name>
  <files>deployments/k8s/monitoring/alerts/allchat-critical-alerts.yaml</files>
  <action>
Add three critical alerts to allchat-critical-alerts.yaml PrometheusRule:

**1. ShardLoadImbalance (METRICS-09):**
```yaml
- alert: ShardLoadImbalance
  expr: shard_imbalance_ratio > 0.7
  for: 10m
  labels:
    severity: warning
    team: allchat
  annotations:
    summary: "Channel load imbalance detected (ratio {{ $value | humanize }})"
    description: |
      Load imbalance ratio has exceeded 0.7 for more than 10 minutes.
      Current ratio: {{ $value | humanize }} (threshold: 0.7)

      Impact: Some pods are handling significantly more channels than others,
      leading to uneven resource usage and potential performance degradation.

      Remediation Steps:
      1. Check if rebalancing is in cooldown: kubectl logs -n allchat -l app=source-manager | grep "cooldown"
      2. Verify rebalancing enabled: Check source-manager config for REBALANCING_ENABLED=true
      3. Check load distribution: Open Grafana dashboard "Sharding Overview" → Channel Distribution heatmap
      4. Manually trigger rebalancing if needed: curl -X POST http://source-manager:8088/admin/rebalance
      5. Check for pod failures: kubectl get pods -n allchat | grep -v Running

      Auto-rebalancing should trigger automatically. Alert fires if rebalancing
      fails or is stuck in cooldown during persistent imbalance.
```

**2. ShardCoordinatorSplitBrain (METRICS-10):**
```yaml
- alert: ShardCoordinatorSplitBrain
  expr: sum(shard_coordinator_is_leader) > 1
  for: 1m
  labels:
    severity: critical
    team: allchat
  annotations:
    summary: "Multiple coordinator leaders detected ({{ $value }} leaders)"
    description: |
      More than one source-manager pod believes it is the leader.
      This is a CRITICAL split-brain condition that can cause conflicting assignments.

      Impact: Multiple coordinators may assign same channel to different pods,
      causing duplicate messages or migration conflicts.

      IMMEDIATE ACTION REQUIRED:
      1. Check all source-manager pods: kubectl get pods -n allchat -l app=source-manager
      2. View leader election logs: kubectl logs -n allchat -l app=source-manager | grep "leader"
      3. Check Kubernetes Lease: kubectl get lease -n allchat coordinator-leader
      4. Restart all source-manager pods: kubectl rollout restart -n allchat deployment/source-manager
      5. Verify single leader after restart: Check alert resolution + Prometheus metric

      Root cause: Kubernetes Lease API clock skew or network partition.
      This should resolve automatically via lease expiration (30s).
```

**3. ShardRebalancingThrashing (METRICS-11) - move to warning alerts:**
(This is actually a warning, not critical - will add in next task)

Follow existing alert patterns from allchat-critical-alerts.yaml. Place alerts in `spec.groups[0].rules` array. Verify indentation (2 spaces).

Per CONTEXT.md: Alert severity based on impact - critical = message loss/user-visible failures, warning = degraded performance.
  </action>
  <verify>
YAML validation:
```bash
kubectl apply --dry-run=client -f deployments/k8s/monitoring/alerts/allchat-critical-alerts.yaml
```

Alert count check:
```bash
yq eval '.spec.groups[0].rules | length' deployments/k8s/monitoring/alerts/allchat-critical-alerts.yaml
# Should increase by 2 (imbalance, split-brain)
```

Remediation steps check:
```bash
grep -A 20 "Remediation Steps:" deployments/k8s/monitoring/alerts/allchat-critical-alerts.yaml | head -30
```
  </verify>
  <done>
- ShardLoadImbalance alert with 0.7 threshold and 10m duration
- ShardCoordinatorSplitBrain alert with 1m duration
- Both alerts include detailed remediation steps
- YAML is valid and matches existing alert format
  </done>
</task>

<task type="auto">
  <name>Add Prometheus warning alerts</name>
  <files>deployments/k8s/monitoring/alerts/allchat-warning-alerts.yaml</files>
  <action>
Add ShardRebalancingThrashing warning alert to allchat-warning-alerts.yaml:

```yaml
- alert: ShardRebalancingThrashing
  expr: increase(shard_rebalancing_total[30m]) > 5
  for: 5m
  labels:
    severity: warning
    team: allchat
  annotations:
    summary: "Rebalancing thrashing detected ({{ $value }} rebalances in 30min)"
    description: |
      More than 5 rebalancing operations occurred in the last 30 minutes.
      This indicates thrashing - repeated rebalancing without achieving stability.

      Impact: Frequent migrations cause connection churn and potential message loss.

      Remediation Steps:
      1. Check rebalancing logs: kubectl logs -n allchat -l app=source-manager | grep "rebalancing"
      2. Review recent migration failures: Check Grafana → Migration Events timeline
      3. Verify load calculation accuracy: Check Prometheus for pod message rates
      4. Increase cooldown duration: Set REBALANCING_COOLDOWN=10m (currently 5m)
      5. Check for oscillating load: Review time-series graphs for stable vs fluctuating loads

      Consider temporarily disabling auto-rebalancing if thrashing persists:
      kubectl set env -n allchat deployment/source-manager REBALANCING_ENABLED=false
```

Per CONTEXT.md: Threshold relaxed from >3 in 15min to >5 in 30min (less sensitive).

Follow existing warning alert patterns. Place in `spec.groups[0].rules` array.
  </action>
  <verify>
YAML validation:
```bash
kubectl apply --dry-run=client -f deployments/k8s/monitoring/alerts/allchat-warning-alerts.yaml
```

Alert expression check:
```bash
grep "increase(shard_rebalancing_total\[30m\])" deployments/k8s/monitoring/alerts/allchat-warning-alerts.yaml
```

Threshold check:
```bash
yq eval '.spec.groups[0].rules[] | select(.alert == "ShardRebalancingThrashing") | .expr' deployments/k8s/monitoring/alerts/allchat-warning-alerts.yaml
# Should output: increase(shard_rebalancing_total[30m]) > 5
```
  </verify>
  <done>
- ShardRebalancingThrashing alert with >5 in 30min threshold
- Alert includes remediation steps and temporary mitigation
- YAML is valid and matches warning alert format
  </done>
</task>

</tasks>

<verification>
Overall verification after all tasks:

1. **Dashboard availability:**
```bash
# Check dashboard file exists and is valid JSON
ls -lh deployments/k8s/monitoring/grafana-dashboards/sharding-overview.json
jq . deployments/k8s/monitoring/grafana-dashboards/sharding-overview.json > /dev/null
```

2. **Alert deployment:**
```bash
# Validate both alert files
kubectl apply --dry-run=client -f deployments/k8s/monitoring/alerts/allchat-critical-alerts.yaml
kubectl apply --dry-run=client -f deployments/k8s/monitoring/alerts/allchat-warning-alerts.yaml
```

3. **Metric coverage:**
```bash
# Verify dashboard references metrics from 08-01
grep -o "shard_[a-z_]*" deployments/k8s/monitoring/grafana-dashboards/sharding-overview.json | sort -u
# Should include: shard_channel_count, shard_imbalance_ratio, shard_migration_total
```

4. **Alert coverage:**
```bash
# Verify all required alerts present
yq eval '.spec.groups[0].rules[].alert' deployments/k8s/monitoring/alerts/allchat-critical-alerts.yaml | grep Shard
yq eval '.spec.groups[0].rules[].alert' deployments/k8s/monitoring/alerts/allchat-warning-alerts.yaml | grep Shard
```
</verification>

<success_criteria>
- [x] Grafana dashboard JSON created with 7 panels
- [x] Dashboard includes imbalance ratio stat as primary at-a-glance view
- [x] Dashboard includes Pod × Time heatmap for channel distribution
- [x] Dashboard includes rebalancing timeline with event markers
- [x] Dashboard includes migration events table
- [x] ShardLoadImbalance alert with 0.7 threshold for 10 minutes
- [x] ShardCoordinatorSplitBrain alert with immediate trigger (1m)
- [x] ShardRebalancingThrashing alert with >5 in 30min threshold
- [x] All alerts include remediation steps in descriptions
- [x] Dashboard and alerts are valid YAML/JSON
- [x] METRICS-04: shard_rebalancing_total tracked (already exists from Phase 7)
- [x] METRICS-06: shard_imbalance_ratio tracked (already exists from Phase 7)
- [x] METRICS-07: Grafana dashboard visualizes channel distribution (heatmap)
- [x] METRICS-08: Dashboard shows rebalancing timeline (graph + annotations)
- [x] METRICS-09: Imbalance ratio alert when >0.7 for 10min
- [x] METRICS-10: Split-brain alert on multiple leaders
- [x] METRICS-11: Thrashing alert on >5 rebalances in 30min
</success_criteria>

<output>
After completion, create `.planning/phases/08-observability-production-readiness/08-04-SUMMARY.md`

**CHECKPOINT: Human verification required**

User should:
1. Apply dashboard to Grafana (kubectl apply or Grafana UI import)
2. Apply alerts to Prometheus (kubectl apply PrometheusRule)
3. Verify dashboard renders correctly with sample data
4. Trigger test alert (manually set imbalance ratio) to verify alerting pipeline
5. Review alert descriptions for clarity and actionable remediation steps

Document:
- Dashboard panel layout (7 panels, gridPos coordinates)
- PromQL queries used (which metrics, aggregations)
- Alert thresholds and durations (locked per CONTEXT.md)
- Remediation step examples (for ops team reference)
- Next steps: Monitor production system with dashboard + alerts
</output>
