---
phase: 27-innertube-enrichment-badges-emotes
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - services/youtube-listener-innertube/innertube/parser_badge_test.go
  - services/youtube-listener-innertube/innertube/parser_emote_test.go
  - services/youtube-listener-innertube/yt_emote_cache/cache_test.go
  - services/message-processor/normalizer/youtube_normalizer_badges_test.go
  - services/message-processor/normalizer/youtube_normalizer_emotes_test.go
autonomous: true
requirements:
  - YTBADGE-01
  - YTBADGE-02
  - YTBADGE-03
  - YTBADGE-04
  - YTEMOTE-01
  - YTEMOTE-02
  - YTEMOTE-03
  - YTEMOTE-04
  - YTEMOTE-05

must_haves:
  truths:
    - "All 5 new test files exist and compile"
    - "Every test in the new files fails (RED state) because implementation does not exist yet"
    - "Existing passing tests in both services remain green"
    - "go test ./... compiles without error in both services (test failures are acceptable, compile errors are not)"
  artifacts:
    - path: "services/youtube-listener-innertube/innertube/parser_badge_test.go"
      provides: "Failing tests for extractBadgesRich (YTBADGE-01, YTBADGE-02)"
    - path: "services/youtube-listener-innertube/innertube/parser_emote_test.go"
      provides: "Failing tests for extractMessageText with custom emoji (YTEMOTE-01..04)"
    - path: "services/youtube-listener-innertube/yt_emote_cache/cache_test.go"
      provides: "Failing tests for Redis emote cache (YTEMOTE-05) using miniredis"
    - path: "services/message-processor/normalizer/youtube_normalizer_badges_test.go"
      provides: "Failing tests for badge normalizer with real URL and backward compat (YTBADGE-03, YTBADGE-04)"
    - path: "services/message-processor/normalizer/youtube_normalizer_emotes_test.go"
      provides: "Failing tests for emote normalizer (YTEMOTE-01, YTEMOTE-02, YTEMOTE-03)"
  key_links:
    - from: "parser_badge_test.go"
      to: "innertube/parser.go extractBadgesRich"
      via: "calls function that does not yet exist — RED"
    - from: "cache_test.go"
      to: "yt_emote_cache/cache.go CacheYTEmotes"
      via: "calls function that does not yet exist — RED"
    - from: "youtube_normalizer_emotes_test.go"
      to: "normalizer/youtube_normalizer.go emote merge logic"
      via: "asserts emotes present in normalized message — RED"
---

<objective>
Create all failing test scaffolds for Phase 27 before any implementation begins.

Purpose: Nyquist compliance — every requirement must have an automated test that is RED before going GREEN. This plan establishes the full test surface so Wave 1 and Wave 2 plans implement against a known contract.
Output: 5 new test files (3 in innertube service, 2 in message-processor), all compiling but failing. miniredis added to innertube go.mod.
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-innertube-enrichment-badges-emotes/27-RESEARCH.md
@.planning/phases/27-innertube-enrichment-badges-emotes/27-VALIDATION.md
@services/youtube-listener-innertube/innertube/types.go
@services/youtube-listener-innertube/innertube/parser.go
@services/youtube-listener-innertube/innertube/parser_test.go
@services/message-processor/normalizer/youtube_normalizer.go
</context>

<interfaces>
<!-- Key types the test files must reference. Extracted from existing code. -->
<!-- New functions (extractBadgesRich, CacheYTEmotes, EmoteEntry) do NOT yet exist — tests call them to be RED. -->

From services/youtube-listener-innertube/innertube/types.go:
```go
type EmojiData struct {
    EmojiID   string     `json:"emojiId,omitempty"`
    Shortcuts []string   `json:"shortcuts,omitempty"`
    Image     Thumbnails `json:"image,omitempty"`
    // IsCustomEmoji bool NOT YET ADDED — test will reference it, causing compile error until Plan 02
}

type LiveChatAuthorBadgeRenderer struct {
    CustomThumbnail Thumbnails `json:"customThumbnail,omitempty"`
    Icon            *IconData  `json:"icon,omitempty"`
    Tooltip         string     `json:"tooltip,omitempty"`
}

type Thumbnail struct {
    URL    string `json:"url"`
    Width  int    `json:"width,omitempty"`
    Height int    `json:"height,omitempty"`
}
```

From services/message-processor/normalizer/youtube_normalizer.go:
```go
type YouTubeNormalizer struct{}
func (n *YouTubeNormalizer) Normalize(raw *models.RawChatMessage, overlayID string) (*models.UnifiedChatMessage, error)
```

From services/message-processor/models/message.go (inferred pattern):
```go
type Emote struct {
    Code      string
    Provider  string
    URL       string
    Positions [][]int
}
```
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: innertube badge test scaffold (YTBADGE-01, YTBADGE-02)</name>
  <files>services/youtube-listener-innertube/innertube/parser_badge_test.go</files>
  <behavior>
    - TestExtractBadgesRich_MemberBadgeURL: badge with CustomThumbnail (2 thumbnails) → returns index 1 URL as badge_member_url, tooltip as badge_member_tooltip
    - TestExtractBadgesRich_MemberBadgeSingleThumbnail: badge with only 1 thumbnail → returns index 0 URL (no panic)
    - TestExtractBadgesRich_SystemBadge: badge with Icon only (no CustomThumbnail) → badge_member_url is empty, tooltip still returned in badgeTooltips slice
    - TestExtractBadgesRich_NoBadges: empty badge slice → all return values are zero/nil
    - TestParseTextMessage_MemberBadgeTagsSet: ParseMessages with a text message that has a membership badge → msg.Tags["badge_member_url"] non-empty, msg.Tags["badge_member_tooltip"] == tooltip string
  </behavior>
  <action>
    Create `services/youtube-listener-innertube/innertube/parser_badge_test.go` in package `innertube`.

    Import: "testing", "github.com/stretchr/testify/assert"

    Write the 5 tests listed in the behavior block. All tests call `extractBadgesRich` (which does not yet exist in parser.go). This will cause a compile error — that is intentional (RED state).

    For TestParseTextMessage_MemberBadgeTagsSet, construct a ChatAction with:
    - LiveChatTextMessageRenderer containing AuthorBadges with a CustomThumbnail badge having 2 thumbnails: [{URL: "https://yt.example.com/badge_32.png", Width: 32}, {URL: "https://yt.example.com/badge_48.png", Width: 48}] and Tooltip "3-Month Member"
    - TimestampUsec: "1640000000000000", AuthorName: "TestMember", AuthorExternalChannelID: "UC123"

    Assert msg.Tags["badge_member_url"] == "https://yt.example.com/badge_48.png" (index 1).
    Assert msg.Tags["badge_member_tooltip"] == "3-Month Member".
    Assert msg.Tags["badges"] still contains "3-Month Member" (backward compat: existing tooltip tag preserved).

    NOTE: extractBadgesRich does not yet exist. The test file will fail to compile until Plan 02 adds it. This is correct RED state.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/services/youtube-listener-innertube && go build ./innertube/... 2>&1 | grep -c "extractBadgesRich" || true</automated>
  </verify>
  <done>File exists, references extractBadgesRich which does not exist yet (compile error expected). Existing parser_test.go tests still pass when run with: go test ./innertube/... -run "TestParseMessages|TestValidate|TestExtractMessageText|TestParse|TestFormat|TestExtract"</done>
</task>

<task type="tdd">
  <name>Task 2: innertube emote test scaffold (YTEMOTE-01 through YTEMOTE-04)</name>
  <files>services/youtube-listener-innertube/innertube/parser_emote_test.go</files>
  <behavior>
    - TestExtractMessageText_CustomEmoji_ChannelMember: run with IsCustomEmoji=true, EmojiID="UCxxxxxx/custom_emote", Shortcuts=[":custom:"], Image with 2 thumbnails → text placeholder is ":custom:", emotes slice has 1 entry with URL from thumbnails[1] (48px)
    - TestExtractMessageText_CustomEmoji_Global: run with IsCustomEmoji=true, EmojiID="_global_emote", Shortcuts=[":global:"] → emotes slice has 1 entry
    - TestExtractMessageText_UnicodeEmoji_NoCustom: run with IsCustomEmoji=false (or absent), Shortcuts=[":smile:"] → emotes slice is EMPTY, text still contains ":smile:"
    - TestExtractMessageText_EmoteURL_UsesIndex1: custom emoji with 2 thumbnails [{URL:"small.png",Width:32},{URL:"large.png",Width:48}] → emote URL is "large.png" (index 1)
    - TestExtractMessageText_EmoteURL_FallbackIndex0: custom emoji with only 1 thumbnail [{URL:"only.png",Width:32}] → emote URL is "only.png" (index 0, no panic)
    - TestExtractMessageText_EmoteNoShortcut: custom emoji with no shortcuts → placeholder is colon-wrapped emojiId (e.g., ":UCxxxx:")
    - TestParseMessages_EmoteDataTag: ParseMessages with text message containing custom emoji → msg.Tags["emote_data"] is valid JSON array
  </behavior>
  <action>
    Create `services/youtube-listener-innertube/innertube/parser_emote_test.go` in package `innertube`.

    Import: "encoding/json", "testing", "github.com/stretchr/testify/assert", "github.com/stretchr/testify/require"

    Tests call `extractMessageText` with a new second return value `[]EmoteEntry` — this is the updated signature that does not yet exist. Also reference `EmoteEntry` struct (not yet defined). Both will compile-error until Plan 02.

    For TestParseMessages_EmoteDataTag: call ParseMessages with a ChatAction containing a text message with a custom emoji run. Assert Tags["emote_data"] can be unmarshalled to []map[string]string with non-empty result.

    NOTE: extractMessageText currently returns only (string). The tests assume a new signature returning (string, []EmoteEntry). This will fail to compile until Plan 02. That is the intended RED state.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/services/youtube-listener-innertube && go build ./innertube/... 2>&1 | grep -c "EmoteEntry\|extractMessageText" || true</automated>
  </verify>
  <done>File exists with 7 tests referencing EmoteEntry and new extractMessageText signature. Compile error expected until Plan 02.</done>
</task>

<task type="tdd">
  <name>Task 3: innertube emote cache test scaffold + add miniredis (YTEMOTE-05)</name>
  <files>
    services/youtube-listener-innertube/yt_emote_cache/cache_test.go
    services/youtube-listener-innertube/go.mod
    services/youtube-listener-innertube/go.sum
  </files>
  <behavior>
    - TestCacheYTEmotes_WritesKey: after CacheYTEmotes(ctx, rdb, "UCchannel123", []EmoteEntry{{ID:"UCemote1",Code:":e1:",URL:"https://img.png"}}), GET "yt:emote:UCchannel123:UCemote1" returns JSON containing the emote data
    - TestCacheYTEmotes_TTL24h: key has TTL between 23h50m and 24h10m after write
    - TestCacheYTEmotes_MultipleEmotes: two emotes in one call → two distinct Redis keys written
    - TestCacheYTEmotes_EmptyList: empty emote slice → no Redis operations (no panic)
    - TestCacheYTEmotes_RefreshesExistingTTL: write same key twice → TTL reset to 24h on second write (SET not SETNX)
    - TestCacheYTEmotes_TimeoutContext: 1ns timeout context → logs error and returns without panic (does not block)
  </behavior>
  <action>
    Step 1: Add miniredis to the innertube service go.mod:
    ```
    cd /home/caesar/git/all-chat/services/youtube-listener-innertube
    go get github.com/alicebob/miniredis/v2@v2.37.0
    go mod tidy
    ```

    Step 2: Create directory `services/youtube-listener-innertube/yt_emote_cache/` (new package).

    Step 3: Create `cache_test.go` in package `yt_emote_cache`.

    Import: "context", "encoding/json", "testing", "time", "github.com/alicebob/miniredis/v2", "github.com/redis/go-redis/v9", "github.com/stretchr/testify/assert", "github.com/stretchr/testify/require"

    Each test starts miniredis: `mr, err := miniredis.Run(); require.NoError(t, err); defer mr.Close()`
    Creates go-redis client: `rdb := redis.NewClient(&redis.Options{Addr: mr.Addr()})`

    Tests call `CacheYTEmotes` and `EmoteEntry` which do not yet exist in the `yt_emote_cache` package. This is the intended RED state — the package will have no non-test .go file yet, so tests will compile-error until Plan 02 creates `cache.go`.

    NOTE: Create a minimal `cache.go` stub in `yt_emote_cache/` with just the package declaration so the directory is a valid Go package (this allows `go mod tidy` to succeed). The actual functions are NOT implemented yet.
    ```go
    package yt_emote_cache
    // TODO: implement CacheYTEmotes and EmoteEntry in Plan 02
    ```
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/services/youtube-listener-innertube && go mod tidy && grep "miniredis" go.mod</automated>
  </verify>
  <done>go.mod contains miniredis dependency. cache_test.go exists with 6 tests. Stub cache.go exists. Tests compile-error on CacheYTEmotes/EmoteEntry until Plan 02.</done>
</task>

<task type="tdd">
  <name>Task 4: message-processor normalizer badge test scaffold (YTBADGE-03, YTBADGE-04)</name>
  <files>services/message-processor/normalizer/youtube_normalizer_badges_test.go</files>
  <behavior>
    - TestYouTubeNormalizer_ExtractBadges_RealMemberURL: raw message with Tags["badge_member_url"]="https://yt.img/member.png" and Tags["badge_member_tooltip"]="6-Month Member" → normalized message has Badge{Name:"member", IconURL:"https://yt.img/member.png", Version:"6-Month Member"}
    - TestYouTubeNormalizer_ExtractBadges_SVGFallback: raw message with Tags["badge_member_url"]="" and Tags["is_sponsor"]="true" → Badge.IconURL starts with "data:image/svg+xml" (SVG fallback preserved)
    - TestYouTubeNormalizer_ExtractBadges_BackwardCompat: raw message with NO badge_member_url tag (old listener format), Tags["is_sponsor"]="true" → still produces member Badge with SVG fallback (not broken)
    - TestYouTubeNormalizer_ExtractBadges_OwnerBadge: Tags["is_owner"]="true" → owner Badge present with SVG IconURL (no regression — YTBADGE-03)
    - TestYouTubeNormalizer_ExtractBadges_ModeratorBadge: Tags["is_moderator"]="true" → moderator Badge present (no regression — YTBADGE-03)
    - TestYouTubeNormalizer_ExtractBadges_MemberURLWithoutIsSponsor: Tags["badge_member_url"]="https://real.png" but Tags["is_sponsor"] absent → member Badge still produced (trigger on badge_member_url alone — innertube doesn't set is_sponsor)
  </behavior>
  <action>
    Create `services/message-processor/normalizer/youtube_normalizer_badges_test.go` in package `normalizer`.

    Import: "testing", "github.com/caesar/all-chat/services/message-processor/models", "github.com/stretchr/testify/assert", "github.com/stretchr/testify/require"

    Helper: `makeRawMsg(tags map[string]string) *models.RawChatMessage` — creates a minimal valid RawChatMessage with platform="youtube", sets provided tags.

    Tests TestYouTubeNormalizer_ExtractBadges_RealMemberURL and TestYouTubeNormalizer_ExtractBadges_MemberURLWithoutIsSponsor will FAIL until Plan 03 modifies the normalizer to check badge_member_url.

    Tests TestYouTubeNormalizer_ExtractBadges_OwnerBadge, TestYouTubeNormalizer_ExtractBadges_ModeratorBadge should PASS immediately (they verify existing behavior does not regress). Confirm this.

    Tests TestYouTubeNormalizer_ExtractBadges_SVGFallback and TestYouTubeNormalizer_ExtractBadges_BackwardCompat: SVGFallback should PASS now (is_sponsor=true path exists). BackwardCompat should PASS now. Document which tests are expected to fail.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/services/message-processor && go test ./normalizer/... -run "TestYouTubeNormalizer_ExtractBadges" -v 2>&1 | tail -20</automated>
  </verify>
  <done>File exists. Tests that verify existing behavior (owner, moderator, SVG fallback, backward compat) pass. Tests for new behavior (RealMemberURL, MemberURLWithoutIsSponsor) fail until Plan 03.</done>
</task>

<task type="tdd">
  <name>Task 5: message-processor normalizer emote test scaffold (YTEMOTE-01, YTEMOTE-02, YTEMOTE-03)</name>
  <files>services/message-processor/normalizer/youtube_normalizer_emotes_test.go</files>
  <behavior>
    - TestYouTubeNormalizer_EmoteData_ChannelEmote: raw message with Tags["emote_data"]='[{"code":":custom:","url":"https://yt.img/emote.png","id":"UCxxxxx/custom"}]' → Normalize returns msg.Message.Emotes has 1 entry with Code=":custom:", URL="https://yt.img/emote.png", Provider="youtube"
    - TestYouTubeNormalizer_EmoteData_GlobalEmote: emote_data with id starting "_" → same outcome
    - TestYouTubeNormalizer_EmoteData_MultipleEmotes: emote_data with 2 entries → Emotes slice has 2 entries
    - TestYouTubeNormalizer_EmoteData_UnicodeNoEmotes: Tags["emote_data"]="" (empty/absent) → Emotes slice is empty (YTEMOTE-03 regression)
    - TestYouTubeNormalizer_EmoteData_InvalidJSON: Tags["emote_data"]="not-json" → Normalize does not error, Emotes slice is empty (graceful degradation)
    - TestYouTubeNormalizer_EmoteData_Positions: message text is "hello :custom: world" with emote code ":custom:" → Emotes[0].Positions is non-empty (at least one position pair)
  </behavior>
  <action>
    Create `services/message-processor/normalizer/youtube_normalizer_emotes_test.go` in package `normalizer`.

    Import: "encoding/json", "testing", "github.com/caesar/all-chat/services/message-processor/models", "github.com/stretchr/testify/assert", "github.com/stretchr/testify/require"

    Use same `makeRawMsg` helper pattern (or define inline helpers).

    For position tests: set raw.Text = "hello :custom: world" and emote_data code = ":custom:". Assert Positions[0] = [6, 14] (0-indexed byte positions of ":custom:" in the text).

    Tests TestYouTubeNormalizer_EmoteData_ChannelEmote, TestYouTubeNormalizer_EmoteData_GlobalEmote, TestYouTubeNormalizer_EmoteData_MultipleEmotes, TestYouTubeNormalizer_EmoteData_Positions will FAIL until Plan 03.

    Tests TestYouTubeNormalizer_EmoteData_UnicodeNoEmotes, TestYouTubeNormalizer_EmoteData_InvalidJSON should PASS now (existing normalizer returns empty Emotes; invalid JSON returns no error because tag is not read).
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/services/message-processor && go test ./normalizer/... -run "TestYouTubeNormalizer_EmoteData" -v 2>&1 | tail -20</automated>
  </verify>
  <done>File exists. UnicodeNoEmotes and InvalidJSON tests pass. ChannelEmote, GlobalEmote, MultipleEmotes, Positions fail until Plan 03.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Both services compile: `cd /home/caesar/git/all-chat/services/youtube-listener-innertube && go build ./... && cd /home/caesar/git/all-chat/services/message-processor && go build ./...`
2. Existing tests still pass: `cd /home/caesar/git/all-chat/services/youtube-listener-innertube && go test ./... -run "TestParseMessages|TestValidate|TestExtractMilestone|TestFormat" && cd /home/caesar/git/all-chat/services/message-processor && go test ./normalizer/... -run "TestYouTubeNormalizer_ExtractBadges_Owner|TestYouTubeNormalizer_ExtractBadges_Mod|TestYouTubeNormalizer_EmoteData_Unicode|TestYouTubeNormalizer_EmoteData_Invalid"`
3. New test files exist: `ls /home/caesar/git/all-chat/services/youtube-listener-innertube/innertube/parser_badge_test.go /home/caesar/git/all-chat/services/youtube-listener-innertube/innertube/parser_emote_test.go /home/caesar/git/all-chat/services/youtube-listener-innertube/yt_emote_cache/cache_test.go /home/caesar/git/all-chat/services/message-processor/normalizer/youtube_normalizer_badges_test.go /home/caesar/git/all-chat/services/message-processor/normalizer/youtube_normalizer_emotes_test.go`
4. miniredis in innertube go.mod: `grep alicebob /home/caesar/git/all-chat/services/youtube-listener-innertube/go.mod`
</verification>

<success_criteria>
- 5 new test files created (3 in innertube, 2 in message-processor)
- All 5 files compile as part of their respective packages
- miniredis added to youtube-listener-innertube/go.mod
- New tests that verify existing behavior are GREEN
- New tests that verify new behavior are RED (expected compile or runtime failure)
- No existing test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/27-innertube-enrichment-badges-emotes/27-01-SUMMARY.md`
</output>
