---
phase: 23-design-token-system-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/platform-colors.ts
  - frontend/src/lib/__tests__/platform-colors.test.ts
  - frontend/src/app/overlay/[id]/credits/page.tsx
  - frontend/src/app/page.tsx
  - frontend/src/components/legal/LegalLayout.tsx
  - frontend/src/components/theme-marketplace/CreditRollThemePreview.tsx
  - frontend/src/components/theme-marketplace/ThemePreview.tsx
autonomous: true
requirements:
  - FOUND-03
  - FOUND-05
must_haves:
  truths:
    - "PLATFORM_COLORS static map exists and exports correct Tailwind class strings for all four platforms plus system"
    - "No dynamic class construction exists for platform colors (no string concatenation like 'text-' + platform)"
    - "Zero occurrences of bg-gradient-to-* remain in frontend/src/"
    - "ThemePreview.tsx no longer uses getPlatformColor() switch returning text-purple-400 etc."
    - "Unit tests for PLATFORM_COLORS pass (all platforms return expected values)"
    - "npm run build passes after all changes"
  artifacts:
    - path: "frontend/src/lib/platform-colors.ts"
      provides: "Static JIT-safe platform color mapping"
      exports: ["PLATFORM_COLORS", "Platform"]
    - path: "frontend/src/lib/__tests__/platform-colors.test.ts"
      provides: "Unit tests for PLATFORM_COLORS map"
      contains: "twitch"
  key_links:
    - from: "frontend/src/components/theme-marketplace/ThemePreview.tsx"
      to: "frontend/src/lib/platform-colors.ts"
      via: "import { PLATFORM_COLORS } from '@/lib/platform-colors'"
      pattern: "PLATFORM_COLORS"
---

<objective>
Create the static platform color mapping object (JIT-safe, no dynamic class construction) and execute the gradient class migration across the four known files. This runs in parallel with Plan 01 since it touches completely separate files.

Purpose: Fix two independent but critical correctness issues — Tailwind JIT can't scan dynamically constructed class names, and `bg-gradient-to-*` classes are undefined in Tailwind v4 (renamed to `bg-linear-to-*`).

Output: `frontend/src/lib/platform-colors.ts` with `PLATFORM_COLORS` constant, unit tests, ThemePreview.tsx migrated to use the static map, and 7 gradient class occurrences migrated across 4 files.
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-design-token-system-foundation/23-CONTEXT.md
@.planning/phases/23-design-token-system-foundation/23-RESEARCH.md

<interfaces>
<!-- PLATFORM_COLORS contract (from RESEARCH.md Pattern 3) -->

The static map must use complete literal class strings (no interpolation):
```typescript
// frontend/src/lib/platform-colors.ts
export const PLATFORM_COLORS = {
  twitch:  { text: 'text-twitch',   bg: 'bg-twitch'   },
  youtube: { text: 'text-youtube',  bg: 'bg-youtube'  },
  kick:    { text: 'text-kick',     bg: 'bg-kick'      },
  tiktok:  { text: 'text-tiktok',   bg: 'bg-tiktok'   },
  system:  { text: 'text-text-sub', bg: 'bg-surface'   },
} as const;

export type Platform = keyof typeof PLATFORM_COLORS;
```

Current getPlatformColor() in ThemePreview.tsx (lines 74-87) returns:
- twitch  → 'text-purple-400'  (wrong — references removed shadcn tokens)
- youtube → 'text-red-400'     (wrong)
- kick    → 'text-green-400'   (wrong)
- tiktok  → 'text-gray-400'    (wrong)
- default → 'text-gray-400'    (wrong)

After migration ThemePreview.tsx line 146 changes from:
  `${getPlatformColor(msg.platform)}`
to:
  `${PLATFORM_COLORS[msg.platform as Platform]?.text ?? PLATFORM_COLORS.system.text}`

Gradient files — exact occurrences to migrate (bg-gradient-to-* → bg-linear-to-*):
1. frontend/src/app/overlay/[id]/credits/page.tsx — 3 occurrences of bg-gradient-to-b
2. frontend/src/app/page.tsx — bg-gradient-to-br (line ~77), bg-gradient-to-r (line ~196)
3. frontend/src/components/legal/LegalLayout.tsx — bg-gradient-to-b (line ~11)
4. frontend/src/components/theme-marketplace/CreditRollThemePreview.tsx — bg-gradient-to-b (line ~103)

NOTE: The direction suffix stays the same. Only the prefix changes: bg-gradient-to-* → bg-linear-to-*
  bg-gradient-to-b → bg-linear-to-b
  bg-gradient-to-br → bg-linear-to-br
  bg-gradient-to-r → bg-linear-to-r
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create platform-colors.ts with unit tests (TDD)</name>
  <files>frontend/src/lib/__tests__/platform-colors.test.ts, frontend/src/lib/platform-colors.ts</files>
  <behavior>
    - Test: PLATFORM_COLORS.twitch.text === 'text-twitch'
    - Test: PLATFORM_COLORS.twitch.bg === 'bg-twitch'
    - Test: PLATFORM_COLORS.youtube.text === 'text-youtube'
    - Test: PLATFORM_COLORS.youtube.bg === 'bg-youtube'
    - Test: PLATFORM_COLORS.kick.text === 'text-kick'
    - Test: PLATFORM_COLORS.kick.bg === 'bg-kick'
    - Test: PLATFORM_COLORS.tiktok.text === 'text-tiktok'
    - Test: PLATFORM_COLORS.tiktok.bg === 'bg-tiktok'
    - Test: PLATFORM_COLORS.system.text === 'text-text-sub'
    - Test: All keys of PLATFORM_COLORS are: twitch, youtube, kick, tiktok, system
    - Test: All class values are string literals (no interpolation patterns — just verify each value is a complete static string)
  </behavior>
  <action>
RED: Create `frontend/src/lib/__tests__/platform-colors.test.ts` with tests for each platform's text and bg class. Import from `@/lib/platform-colors`. Run tests — they must fail (file does not exist yet).

GREEN: Create `frontend/src/lib/platform-colors.ts` with the PLATFORM_COLORS const object and Platform type export as shown in the context interfaces. Run tests — they must pass.

The PLATFORM_COLORS object must use exactly these class strings (complete literals, no concatenation):
- twitch: { text: 'text-twitch', bg: 'bg-twitch' }
- youtube: { text: 'text-youtube', bg: 'bg-youtube' }
- kick: { text: 'text-kick', bg: 'bg-kick' }
- tiktok: { text: 'text-tiktok', bg: 'bg-tiktok' }
- system: { text: 'text-text-sub', bg: 'bg-surface' }

Export as const so TypeScript infers literal types.
Also export: `export type Platform = keyof typeof PLATFORM_COLORS;`

The test file should use vitest imports (import { describe, it, expect } from 'vitest').
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/frontend && npx vitest run src/lib/__tests__/platform-colors.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All 10 unit tests pass. platform-colors.ts exports PLATFORM_COLORS and Platform type.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate ThemePreview.tsx to use PLATFORM_COLORS static map</name>
  <files>frontend/src/components/theme-marketplace/ThemePreview.tsx</files>
  <action>
In ThemePreview.tsx:

1. Add import at top of file: `import { PLATFORM_COLORS, type Platform } from '@/lib/platform-colors';`

2. Remove the entire `getPlatformColor()` function (lines 74-87 in current file).

3. Find the JSX using `getPlatformColor(msg.platform)` (line ~146, inside className template literal). Replace with:
   `PLATFORM_COLORS[msg.platform as Platform]?.text ?? PLATFORM_COLORS.system.text`

The containing className will look like:
```tsx
className={`text-xs font-semibold uppercase ${PLATFORM_COLORS[msg.platform as Platform]?.text ?? PLATFORM_COLORS.system.text}`}
```

No other changes to ThemePreview.tsx.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/frontend && npm run type-check 2>&1 | grep -E "ThemePreview|error" | head -10</automated>
  </verify>
  <done>ThemePreview.tsx has no TypeScript errors, getPlatformColor function is removed, PLATFORM_COLORS is imported and used.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate bg-gradient-to-* to bg-linear-to-* across 4 files</name>
  <files>
    frontend/src/app/overlay/[id]/credits/page.tsx,
    frontend/src/app/page.tsx,
    frontend/src/components/legal/LegalLayout.tsx,
    frontend/src/components/theme-marketplace/CreditRollThemePreview.tsx
  </files>
  <action>
In each of the four files, replace every occurrence of `bg-gradient-to-` with `bg-linear-to-`. The direction suffix (b, br, r, t, tl, etc.) stays unchanged — only the prefix changes.

Exact substitution: s/bg-gradient-to-/bg-linear-to-/g

Files and occurrence count:
- frontend/src/app/overlay/[id]/credits/page.tsx — 3 occurrences (all bg-gradient-to-b)
- frontend/src/app/page.tsx — 2 occurrences (bg-gradient-to-br and bg-gradient-to-r)
- frontend/src/components/legal/LegalLayout.tsx — 1 occurrence (bg-gradient-to-b)
- frontend/src/components/theme-marketplace/CreditRollThemePreview.tsx — 1 occurrence (bg-gradient-to-b)

After editing, verify zero occurrences remain with grep.
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/frontend && grep -r "bg-gradient-to-" src/ 2>&1; echo "Exit: $?"</automated>
  </verify>
  <done>grep returns zero matches for "bg-gradient-to-" across all frontend/src/ files. npm run build passes.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd /home/caesar/git/all-chat/frontend && npx vitest run src/lib/__tests__/platform-colors.test.ts` — all tests pass
2. `cd /home/caesar/git/all-chat/frontend && grep -r "bg-gradient-to-" src/` — zero output
3. `cd /home/caesar/git/all-chat/frontend && grep -r "getPlatformColor" src/` — zero output
4. `cd /home/caesar/git/all-chat/frontend && npm run build` — exits 0
5. `grep "PLATFORM_COLORS" /home/caesar/git/all-chat/frontend/src/components/theme-marketplace/ThemePreview.tsx` — returns match
</verification>

<success_criteria>
- PLATFORM_COLORS static map exported from frontend/src/lib/platform-colors.ts
- All 10 unit tests pass in platform-colors.test.ts
- ThemePreview.tsx uses PLATFORM_COLORS, getPlatformColor() function deleted
- Zero bg-gradient-to-* occurrences in frontend/src/
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/23-design-token-system-foundation/23-02-SUMMARY.md`
</output>
