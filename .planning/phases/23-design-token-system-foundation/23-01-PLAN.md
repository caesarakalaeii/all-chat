---
phase: 23-design-token-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/app/layout.tsx
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-06
must_haves:
  truths:
    - "globals.css contains a single @theme block with base, semantic, and component token tiers"
    - "Platform colors (Twitch #9146FF, YouTube #FF4444, Kick #53FC18, TikTok #69C9D0) produce text-twitch, bg-twitch etc. utility classes"
    - "Cascade layer order (@layer base, design-system, marketplace-themes, user-overrides) is declared before any @layer body"
    - "Barlow and DM Mono fonts are loaded via next/font/google and bridged into Tailwind via @theme inline"
    - "All four conflicting :root / .dark / duplicate @layer base blocks from the old globals.css are removed"
    - "npm run build passes with zero TypeScript errors after the rewrite"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "Single-source design token system, cascade layer order, font bridge"
      contains: "@layer base, design-system, marketplace-themes, user-overrides"
    - path: "frontend/src/app/layout.tsx"
      provides: "Barlow + DM Mono font variables injected via next/font/google"
      contains: "Barlow"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/app/globals.css"
      via: "@theme inline { --font-sans: var(--font-barlow) }"
      pattern: "--font-barlow"
    - from: "frontend/src/app/globals.css"
      to: "all pages"
      via: "Next.js layout.tsx @import './globals.css'"
      pattern: "@layer base, design-system"
---

<objective>
Perform a clean-slate rewrite of globals.css, replacing the current conflicting multi-block CSS (two @layer base blocks, one bare :root HSL block, one bare :root oklch block, one .dark block) with a single authoritative dark-only design token system using the Tailwind v4 @theme directive.

Purpose: Establish the CSS foundation that all downstream phases (24, 25, 26) depend on. Every design token — platform colors, neutral scale, semantic purpose-names, component tokens, typography, spacing, shadows — lives in one place and is inspectable in browser DevTools.

Output: A rewritten globals.css with the three-layer token hierarchy and four-layer CSS cascade order, plus an updated layout.tsx loading Barlow and DM Mono instead of Geist/Inter.
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-design-token-system-foundation/23-CONTEXT.md
@.planning/phases/23-design-token-system-foundation/23-RESEARCH.md

<interfaces>
<!-- Key patterns established in RESEARCH.md that executor must follow exactly -->

Current globals.css state (to be replaced entirely):
- Line 1-3: @import tailwindcss, tw-animate-css, shadcn/tailwind.css
- Line 5: @custom-variant dark (&:is(.dark *))
- Lines 7-39: @theme block with platform colors (wrong YouTube #FF0000, wrong TikTok #000000), HSL color-system tokens
- Lines 41-73: First @layer base block with HSL :root variables
- Lines 75-82: Second (duplicate) @layer base block
- Lines 84-99: @layer utilities with slide-up animation
- Lines 101-134: Bare :root oklch block (light theme defaults — REMOVE)
- Lines 136-168: .dark block (REMOVE — dark-only, no class toggle)
- Lines 170-210: @theme inline block (sidebar/chart tokens — keep sidebar tokens, rewrite font bridge)

Current layout.tsx fonts (to be replaced):
- Geist({ variable: '--font-sans' }) → replace with Barlow({ variable: '--font-barlow' })
- Inter({ subsets: ['latin'] }) → replace with DM_Mono({ variable: '--font-dm-mono' })
- html className: cn("font-sans", geist.variable) → cn(barlow.variable, dmMono.variable)
- body className: inter.className → remove (no class needed, body inherits from html)

LOCKED values from CONTEXT.md (non-negotiable):
- Background: #07070a
- Surface: #0d0d12
- Twitch: #9146FF (NOT #9146FF already in file — verify YouTube and TikTok are WRONG)
- YouTube: #FF4444 (current file has #FF0000 — must fix)
- Kick: #53FC18 (correct in current file)
- TikTok: #69C9D0 (current file has #000000 — must fix)

Tailwind v4 @theme rules (from RESEARCH.md):
- Use plain @theme for tokens with raw values (hex, oklch literals)
- Use @theme inline ONLY for tokens that reference var(--something)
- Platform colors: use hex values directly (not oklch) — brand-exact values
- Neutral scale: use oklch with constant chroma ~0.007, hue 270

Font loading pattern:
```typescript
import { Barlow, DM_Mono } from 'next/font/google';
const barlow = Barlow({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700', '800'],
  variable: '--font-barlow',
});
const dmMono = DM_Mono({
  subsets: ['latin'],
  weight: ['400', '500'],
  variable: '--font-dm-mono',
});
// html tag: className={cn(barlow.variable, dmMono.variable)}
```

globals.css font bridge (in @theme inline):
```css
@theme inline {
  --font-sans: var(--font-barlow);
  --font-mono: var(--font-dm-mono);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update layout.tsx to load Barlow and DM Mono fonts</name>
  <files>frontend/src/app/layout.tsx</files>
  <action>
Replace the existing font imports and configuration in layout.tsx. Remove Geist and Inter. Import Barlow and DM_Mono from 'next/font/google'.

Barlow config:
- subsets: ['latin']
- weight: ['400', '500', '600', '700', '800']
- variable: '--font-barlow'
- display: 'swap'

DM_Mono config:
- subsets: ['latin']
- weight: ['400', '500']
- variable: '--font-dm-mono'
- display: 'swap'

Update the html element to use `className={cn(barlow.variable, dmMono.variable)}`. Remove the `className` prop from the body element (fonts inherit via CSS variable on html).

Keep all other layout.tsx content unchanged (imports, metadata, ImpersonationBanner, CookieBanner, etc.).
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/frontend && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <done>layout.tsx imports Barlow and DM_Mono, html element has both font variables applied, no TypeScript errors</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite globals.css with design token system and cascade layers</name>
  <files>frontend/src/app/globals.css</files>
  <action>
Complete clean-slate rewrite of globals.css. Replace all existing content with the authoritative dark-only token system. Structure must follow exactly:

**Section 1 — Imports:**
```css
@import "tailwindcss";
@import "tw-animate-css";
```
Do NOT import shadcn/tailwind.css (it was pulling in HSL defaults we are removing). Do NOT include @custom-variant dark (dark-only, no class toggle).

**Section 2 — Cascade layer order declaration (MUST be before any @layer body):**
```css
@layer base, design-system, marketplace-themes, user-overrides;
```

**Section 3 — Font bridge (@theme inline):**
```css
@theme inline {
  --font-sans: var(--font-barlow);
  --font-mono: var(--font-dm-mono);
}
```

**Section 4 — Design tokens (@theme):**
Single @theme block organized with CSS comments into three tiers:

TIER 1: BASE TOKENS (raw palette)
- Platform colors as hex (brand-exact):
  --color-twitch: #9146FF
  --color-youtube: #FF4444  (NOT #FF0000 — was wrong in old file)
  --color-kick: #53FC18
  --color-tiktok: #69C9D0  (NOT #000000 — was wrong in old file)
- Neutral scale in oklch (constant chroma 0.007, hue 270, lightness interpolated):
  --color-neutral-950: oklch(0.09 0.007 270)   /* #07070a — bg */
  --color-neutral-900: oklch(0.11 0.009 270)   /* #0d0d12 — surface */
  --color-neutral-800: oklch(0.14 0.008 270)   /* surface-2 */
  --color-neutral-700: oklch(0.22 0.007 270)   /* elevated border */
  --color-neutral-600: oklch(0.35 0.007 270)   /* muted text */
  --color-neutral-400: oklch(0.58 0.007 270)   /* secondary text */
  --color-neutral-200: oklch(0.78 0.005 270)   /* tertiary text */
  --color-neutral-100: oklch(0.91 0.003 270)   /* primary text */

TIER 2: SEMANTIC TOKENS (purpose-named, reference base via var())
  --color-bg:           var(--color-neutral-950)
  --color-surface:      var(--color-neutral-900)
  --color-surface-2:    var(--color-neutral-800)
  --color-border:       oklch(from var(--color-neutral-100) l c h / 0.06)
  --color-border-md:    oklch(from var(--color-neutral-100) l c h / 0.10)
  --color-text:         var(--color-neutral-100)
  --color-text-sub:     var(--color-neutral-400)
  --color-text-dim:     var(--color-neutral-600)

TIER 3: COMPONENT TOKENS (specific-use, reference semantic)
  /* Stat card glow — 25% alpha versions of platform colors */
  --color-stat-glow-twitch:  color-mix(in oklch, var(--color-twitch) 25%, transparent)
  --color-stat-glow-youtube: color-mix(in oklch, var(--color-youtube) 25%, transparent)
  --color-stat-glow-kick:    color-mix(in oklch, var(--color-kick) 25%, transparent)
  --color-stat-glow-tiktok:  color-mix(in oklch, var(--color-tiktok) 25%, transparent)
  /* Shadow glows for platform elements */
  --shadow-glow-twitch:  0 0 20px color-mix(in oklch, var(--color-twitch) 40%, transparent)
  --shadow-glow-youtube: 0 0 20px color-mix(in oklch, var(--color-youtube) 40%, transparent)
  --shadow-glow-kick:    0 0 20px color-mix(in oklch, var(--color-kick) 40%, transparent)
  --shadow-glow-tiktok:  0 0 20px color-mix(in oklch, var(--color-tiktok) 40%, transparent)
  /* Badge background */
  --color-badge-bg: oklch(from var(--color-neutral-100) l c h / 0.07)
  /* Nav */
  --color-nav-bg: oklch(from var(--color-neutral-950) l c h / 0.80)

TYPOGRAPHY tokens (in @theme):
  --text-xs:   0.6875rem    /* 11px */
  --text-sm:   0.8125rem    /* 13px */
  --text-base: 0.875rem     /* 14px */
  --text-lg:   1rem         /* 16px */
  --text-xl:   1.25rem      /* 20px */
  --text-2xl:  1.5rem       /* 24px */
  --text-3xl:  2rem         /* 32px */

SPACING tokens (in @theme):
  --spacing-1: 0.25rem
  --spacing-2: 0.5rem
  --spacing-3: 0.75rem
  --spacing-4: 1rem
  --spacing-6: 1.5rem
  --spacing-8: 2rem
  --spacing-12: 3rem
  --spacing-16: 4rem

BORDER RADIUS tokens (in @theme) — replace the old var(--radius) calc() pattern:
  --radius-sm:   4px
  --radius-md:   8px
  --radius-lg:   12px
  --radius-xl:   16px
  --radius-2xl:  24px
  --radius-full: 9999px

**Section 5 — @layer base (resets):**
```css
@layer base {
  *, *::before, *::after {
    box-sizing: border-box;
  }
  html {
    font-family: var(--font-sans);
    background: var(--color-bg);
    color: var(--color-text);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }
  body {
    background: var(--color-bg);
    color: var(--color-text);
    min-height: 100vh;
  }
}
```

**Section 6 — Preserve slide-up animation (in @layer design-system):**
```css
@layer design-system {
  @keyframes slide-up {
    from { transform: translateY(100%); opacity: 0; }
    to   { transform: translateY(0);    opacity: 1; }
  }
  .animate-slide-up {
    animation: slide-up 0.4s ease-out;
  }
}
```

CRITICAL: Do NOT include the old :root HSL block, the bare :root oklch block, the .dark block, or any hsl(var(--*)) tokens. The old shadcn color system (--background, --foreground, --primary etc.) is REMOVED in this clean slate. Components using those tokens will be fixed in Phase 24.

NOTE on color-mix() — use it for the stat-glow and shadow tokens (these are used in the main app, not in OBS overlay CSS). For any overlay-specific CSS, use explicit rgba() fallbacks. The color-mix() usage here is safe for Chrome 111+ (all modern browsers).
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - globals.css builds without errors
    - No "bg-gradient-to-" patterns remain (not introduced by this task)
    - @layer order declaration exists at top of file (before any @layer body)
    - text-twitch, bg-twitch, text-youtube, bg-youtube etc. utility classes are generated (visible in build output or DevTools)
    - YouTube platform color is #FF4444, TikTok is #69C9D0 (verify in file — the old wrong values #FF0000 and #000000 are gone)
    - No duplicate :root blocks, no .dark block in file
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd /home/caesar/git/all-chat/frontend && npm run build` — must exit 0
2. `cd /home/caesar/git/all-chat/frontend && npm run type-check` — must exit 0
3. `grep -c "bg-gradient-to-" /home/caesar/git/all-chat/frontend/src/app/globals.css` — must return 0
4. `grep "FF0000\|#000000" /home/caesar/git/all-chat/frontend/src/app/globals.css` — must return no matches (wrong platform colors not present)
5. `grep "@layer base, design-system, marketplace-themes, user-overrides" /home/caesar/git/all-chat/frontend/src/app/globals.css` — must return 1 match
6. `grep "Barlow" /home/caesar/git/all-chat/frontend/src/app/layout.tsx` — must return match
</verification>

<success_criteria>
- globals.css is a single authoritative file with three-tier @theme token hierarchy
- Four cascade layers declared in correct order
- Platform colors are brand-exact (Twitch #9146FF, YouTube #FF4444, Kick #53FC18, TikTok #69C9D0)
- Barlow (weights 400-800) and DM Mono loaded as CSS variables via next/font/google
- npm run build passes
- No HSL indirection, no .dark toggle, no duplicate :root blocks
</success_criteria>

<output>
After completion, create `.planning/phases/23-design-token-system-foundation/23-01-SUMMARY.md`
</output>
