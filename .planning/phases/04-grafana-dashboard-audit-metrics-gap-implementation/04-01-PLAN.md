---
phase: 04-grafana-dashboard-audit-metrics-gap-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../caesar-deployment/apps/workloads/all-chat/servicemonitor.yaml
  - .planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-GAP-MATRIX.md
autonomous: false
requirements:
  - AUDIT-01
  - AUDIT-02
  - SM-01

must_haves:
  truths:
    - "All 7 Go listeners + tiktok-listener are scraped by Prometheus (ServiceMonitor coverage complete)"
    - "A confirmed gap matrix exists showing wired-vs-missing metrics per service"
    - "Live Prometheus audit confirms which services are actually emitting metrics"
  artifacts:
    - path: "../caesar-deployment/apps/workloads/all-chat/servicemonitor.yaml"
      provides: "ServiceMonitor with all listeners included"
      contains: "discord-listener"
    - path: ".planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-GAP-MATRIX.md"
      provides: "Confirmed gap matrix from live + code audit"
      contains: "Service"
  key_links:
    - from: "servicemonitor.yaml"
      to: "Prometheus scrape targets"
      via: "matchExpressions values list"
      pattern: "discord-listener|youtube-listener-innertube|twitch-eventsub-listener"
---

<objective>
Add three missing listeners (discord-listener, youtube-listener-innertube, twitch-eventsub-listener) to the Prometheus ServiceMonitor, then run a live audit via Grafana MCP to confirm which services are actually being scraped and emitting metrics. Produce a confirmed gap matrix document.

Purpose: ServiceMonitor is a prerequisite for any new metrics to be scraped. The live audit confirms the code-level research gaps against production reality, preventing wasted work on already-covered or unscrape-able services.
Output: Updated servicemonitor.yaml + 04-GAP-MATRIX.md
</objective>

<execution_context>
@/home/moersener/.claude/get-shit-done/workflows/execute-plan.md
@/home/moersener/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-CONTEXT.md
@.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-RESEARCH.md

<interfaces>
<!-- ServiceMonitor YAML structure (existing) -->
From ../caesar-deployment/apps/workloads/all-chat/servicemonitor.yaml:
```yaml
# allchat-listeners ServiceMonitor uses matchExpressions:
spec:
  selector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - twitch-listener
          - kick-listener
          - tiktok-listener
          - youtube-listener
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
```

<!-- Grafana MCP (grafana-caesar) is available for live Prometheus queries -->
<!-- Use query_prometheus tool from grafana-caesar MCP server -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three missing listeners to ServiceMonitor</name>
  <files>../caesar-deployment/apps/workloads/all-chat/servicemonitor.yaml</files>
  <read_first>
    - /home/moersener/Hobby/caesar-deployment/apps/workloads/all-chat/servicemonitor.yaml
  </read_first>
  <action>
Add three new listener values to the `allchat-listeners` ServiceMonitor's `matchExpressions` values list. The existing values are `twitch-listener`, `kick-listener`, `tiktok-listener`, `youtube-listener`. Add:
- `discord-listener`
- `youtube-listener-innertube`
- `twitch-eventsub-listener`

These go in the first ServiceMonitor document (allchat-listeners), under `spec.selector.matchExpressions[0].values`. Keep the existing entries, append the three new ones. Do NOT modify the allchat-source-manager or allchat-services ServiceMonitor documents.

IMPORTANT: Before adding, verify the actual Kubernetes Deployment labels for these three services match the `app` label key. Check:
```bash
grep -A5 'app:' ../caesar-deployment/apps/workloads/all-chat/discord-listener-deployment.yaml 2>/dev/null || echo "Check deployment manifest name"
```
If the deployment manifests use different label values than `discord-listener`, `youtube-listener-innertube`, `twitch-eventsub-listener`, use whatever the actual labels are.
  </action>
  <verify>
    <automated>grep -c 'discord-listener\|youtube-listener-innertube\|twitch-eventsub-listener' /home/moersener/Hobby/caesar-deployment/apps/workloads/all-chat/servicemonitor.yaml | grep -q '3'</automated>
  </verify>
  <acceptance_criteria>
    - servicemonitor.yaml contains `- discord-listener` in allchat-listeners values list
    - servicemonitor.yaml contains `- youtube-listener-innertube` in allchat-listeners values list
    - servicemonitor.yaml contains `- twitch-eventsub-listener` in allchat-listeners values list
    - allchat-source-manager and allchat-services sections are unchanged
    - Total values in allchat-listeners is now 7 (was 4)
  </acceptance_criteria>
  <done>All 7 listeners (+ tiktok) are covered by the allchat-listeners ServiceMonitor</done>
</task>

<task type="auto">
  <name>Task 2: Run live Prometheus audit and produce confirmed gap matrix</name>
  <files>.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-GAP-MATRIX.md</files>
  <read_first>
    - /home/moersener/Hobby/all-chat/.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-RESEARCH.md
  </read_first>
  <action>
Use the Grafana MCP (grafana-caesar) to query live Prometheus and produce a confirmed gap matrix. Run these queries:

1. `up{namespace="allchat"}` — which services are scraped and up
2. `listener_connection_status` — which listeners emit connection status
3. `listener_messages_received_total` — which listeners emit message counts
4. `kick_listener_messages_total` — kick-listener local metric presence
5. `discord_listener_gateway_events_total` — discord-listener local metric presence
6. `youtube_listener_messages_published_total` — innertube local metric presence
7. `processor_messages_consumed_total` — message-processor pipeline metrics
8. `gateway_websocket_connections_active` — api-gateway WebSocket metrics
9. `http_requests_total{namespace="allchat"}` — platform ops service HTTP metrics (likely zero before wiring)

For each query: record which services emit data vs zero-value vs metric not found.

Also use Grafana MCP `search_dashboards` to find existing All-Chat dashboards and check for broken/stale panels.

Write the results to `.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-GAP-MATRIX.md` with three sections:
- **Scrape Status**: Table of all 14 services showing `up` status
- **Metrics Emission**: Service x Metric table showing emitted/zero/missing (confirming or correcting RESEARCH.md code audit)
- **Dashboard Gaps**: List of existing dashboard panels with data status

The gap matrix from RESEARCH.md (lines 83-100) is the starting point. The live audit CONFIRMS or CORRECTS it.
  </action>
  <verify>
    <automated>test -f /home/moersener/Hobby/all-chat/.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-GAP-MATRIX.md && grep -c "Service" /home/moersener/Hobby/all-chat/.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-GAP-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - 04-GAP-MATRIX.md exists
    - 04-GAP-MATRIX.md contains "Scrape Status" section
    - 04-GAP-MATRIX.md contains "Metrics Emission" section
    - 04-GAP-MATRIX.md contains "Dashboard Gaps" section
    - 04-GAP-MATRIX.md covers all 14 services (minus support-bot)
    - Each service has a confirmed status (not just copied from RESEARCH.md)
  </acceptance_criteria>
  <done>Confirmed gap matrix produced from live Prometheus + code audit, documenting exact per-service metric coverage state</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User reviews gap matrix and approves metrics wiring plan</name>
  <files>.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-GAP-MATRIX.md</files>
  <action>Present the gap matrix to the user for review before proceeding with metrics wiring in Plans 02-05.</action>
  <what-built>
ServiceMonitor updated with 3 new listeners. Live Prometheus audit completed. Gap matrix document produced at 04-GAP-MATRIX.md confirming which services need wiring work.
  </what-built>
  <how-to-verify>
    1. Review the generated 04-GAP-MATRIX.md
    2. Confirm the live audit results match expectations
    3. Check that the ServiceMonitor changes are correct for the three new listeners
    4. Verify the gap matrix will feed cleanly into Plans 02-05 as a task checklist
  </how-to-verify>
  <verify>
    <automated>test -f /home/moersener/Hobby/all-chat/.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-GAP-MATRIX.md</automated>
  </verify>
  <done>User approved gap matrix and ServiceMonitor changes</done>
  <resume-signal>Type "approved" to proceed to metrics wiring plans, or describe corrections needed</resume-signal>
</task>

</tasks>

<verification>
- servicemonitor.yaml has 7 listeners in allchat-listeners (was 4)
- 04-GAP-MATRIX.md exists with confirmed live + code audit results
- Gap matrix covers all 14 services
</verification>

<success_criteria>
All listeners covered by ServiceMonitor. Confirmed gap matrix produced from live Prometheus + code audit, ready to feed Plans 02-05.
</success_criteria>

<output>
After completion, create `.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-01-SUMMARY.md`
</output>
