---
phase: 04-grafana-dashboard-audit-metrics-gap-implementation
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - ../caesar-deployment/apps/platform/kube-prometheus-stack/grafana-alerts/allchat-alerts.yaml
autonomous: true
requirements:
  - ALERT-01
  - ALERT-02
  - ALERT-03
  - ALERT-04
  - ALERT-05

must_haves:
  truths:
    - "Alert fires when any listener loses platform connection for >2min"
    - "Alert fires when message pipeline stalls for >1min while listeners are active"
    - "Alert fires when WebSocket connections drop >50% in 5min or reach zero"
    - "Alert fires when error rate exceeds 5% for any service"
    - "All alerts have inline remediation steps in descriptions"
  artifacts:
    - path: "../caesar-deployment/apps/platform/kube-prometheus-stack/grafana-alerts/allchat-alerts.yaml"
      provides: "4 new alert groups with remediation steps"
      contains: "allchat-listener-health"
  key_links:
    - from: "allchat-alerts.yaml"
      to: "Grafana alert evaluation"
      via: "grafana_alert: 1 label on ConfigMap"
      pattern: "grafana_alert"
    - from: "allchat-alerts.yaml"
      to: "Discord notification"
      via: "allchat-notification-policies.yaml routing"
      pattern: "team: allchat"
---

<objective>
Add 4 new alert groups to the existing `allchat-alerts.yaml` ConfigMap: listener disconnections, message pipeline stalls, WebSocket connection drops, and error rate spikes. Each alert includes inline remediation steps and correct severity labels for Discord notification routing.

Purpose: Alerting is the active monitoring layer that notifies the team when something breaks. Current alerts only cover YouTube quota and pod health. The 4 new categories cover the most critical failure modes: platform disconnection, pipeline stalls, delivery failures, and error spikes.
Output: Extended allchat-alerts.yaml with 4 new alert groups.
</objective>

<execution_context>
@/home/moersener/.claude/get-shit-done/workflows/execute-plan.md
@/home/moersener/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-CONTEXT.md
@.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-RESEARCH.md
@.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-02-SUMMARY.md
@.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-03-SUMMARY.md

<interfaces>
<!-- Existing alert ConfigMap structure -->
From ../caesar-deployment/apps/platform/kube-prometheus-stack/grafana-alerts/allchat-alerts.yaml:
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-allchat-alerts
  namespace: monitoring
  labels:
    grafana_alert: "1"
data:
  allchat-alerts.yaml: |
    apiVersion: 1
    groups:
      - orgId: 1
        name: allchat-critical
        folder: All-Chat Alerts
        interval: 1m
        rules:
          - uid: youtube-quota-critical
            # ... existing rules
      - orgId: 1
        name: allchat-warnings
        folder: All-Chat Alerts
        interval: 1m
        rules:
          - uid: youtube-quota-high
            # ... existing rules
```

<!-- Notification routing: team: allchat label routes to Discord webhook -->
<!-- severity: critical triggers @lead-dev mention -->
<!-- severity: warning goes to channel without mention -->

<!-- Contact points and policies in separate files — do not modify -->
From allchat-notification-policies.yaml: Routes based on `team: allchat` label.
From allchat-contact-points.yaml: Discord webhook already configured.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add listener disconnection and pipeline stall alert groups</name>
  <files>../caesar-deployment/apps/platform/kube-prometheus-stack/grafana-alerts/allchat-alerts.yaml</files>
  <read_first>
    - /home/moersener/Hobby/caesar-deployment/apps/platform/kube-prometheus-stack/grafana-alerts/allchat-alerts.yaml
    - /home/moersener/Hobby/caesar-deployment/apps/platform/kube-prometheus-stack/grafana-alerts/allchat-contact-points.yaml
    - /home/moersener/Hobby/caesar-deployment/apps/platform/kube-prometheus-stack/grafana-alerts/allchat-notification-policies.yaml
  </read_first>
  <action>
Append two new alert groups to the `groups:` array in the `allchat-alerts.yaml` data value. Keep ALL existing groups (allchat-critical, allchat-warnings) intact.

**Alert Group 1: allchat-listener-health**
```yaml
- orgId: 1
  name: allchat-listener-health
  folder: All-Chat Alerts
  interval: 1m
  rules:
    - uid: listener-disconnected-shared
      title: Listener Disconnected (shared metrics)
      condition: B
      data:
        - refId: A
          queryType: instant
          relativeTimeRange:
            from: 120
            to: 0
          datasourceUid: prometheus
          model:
            expr: listener_connection_status == 0
            refId: A
            intervalMs: 1000
            maxDataPoints: 43200
        - refId: B
          datasourceUid: __expr__
          model:
            type: threshold
            refId: B
            conditions:
              - evaluator:
                  params: [0]
                  type: lt
                query:
                  params: [A]
                type: query
            expression: A
      noDataState: OK
      execErrState: Alerting
      for: 2m
      annotations:
        summary: "{{ $labels.platform }} listener disconnected from platform"
        description: |
          Listener {{ $labels.service }} lost connection to {{ $labels.platform }}.
          Remediation:
          1. Check pod logs: kubectl -n allchat logs -l app={{ $labels.service }} --tail=50
          2. Check pod restarts: kubectl -n allchat get pods -l app={{ $labels.service }}
          3. Verify platform API status (Twitch status page, YouTube API console, etc.)
      labels:
        severity: critical
        team: allchat

    - uid: listener-disconnected-kick
      title: Kick Listener Socket Disconnected
      condition: B
      data:
        - refId: A
          queryType: instant
          relativeTimeRange:
            from: 120
            to: 0
          datasourceUid: prometheus
          model:
            expr: kick_listener_socket_state == 0
            refId: A
        - refId: B
          datasourceUid: __expr__
          model:
            type: threshold
            refId: B
            conditions:
              - evaluator:
                  params: [0]
                  type: lt
                query:
                  params: [A]
                type: query
            expression: A
      noDataState: OK
      execErrState: Alerting
      for: 2m
      annotations:
        summary: "Kick listener WebSocket disconnected"
        description: |
          Kick listener lost Pusher WebSocket connection.
          Remediation:
          1. Check pod logs: kubectl -n allchat logs -l app=kick-listener --tail=50
          2. Verify Kick platform status
          3. Check if Pusher endpoint is reachable from cluster
      labels:
        severity: critical
        team: allchat

    - uid: listener-disconnected-discord
      title: Discord Listener Disconnected
      condition: B
      data:
        - refId: A
          queryType: instant
          relativeTimeRange:
            from: 120
            to: 0
          datasourceUid: prometheus
          model:
            expr: discord_listener_shard_ownership == 0
            refId: A
        - refId: B
          datasourceUid: __expr__
          model:
            type: threshold
            refId: B
            conditions:
              - evaluator:
                  params: [0]
                  type: lt
                query:
                  params: [A]
                type: query
            expression: A
      noDataState: OK
      execErrState: Alerting
      for: 2m
      annotations:
        summary: "Discord listener has no shard ownership"
        description: |
          Discord listener lost shard ownership — no Gateway connection active.
          Remediation:
          1. Check pod logs: kubectl -n allchat logs -l app=discord-listener --tail=50
          2. Check leader election: kubectl -n allchat get leases
          3. Verify Discord bot token is valid
      labels:
        severity: critical
        team: allchat
```

**Alert Group 2: allchat-pipeline-health**
```yaml
- orgId: 1
  name: allchat-pipeline-health
  folder: All-Chat Alerts
  interval: 1m
  rules:
    - uid: pipeline-stall
      title: Message Pipeline Stalled
      condition: C
      data:
        - refId: A
          queryType: instant
          relativeTimeRange:
            from: 300
            to: 0
          datasourceUid: prometheus
          model:
            expr: rate(processor_messages_consumed_total[5m])
            refId: A
        - refId: B
          queryType: instant
          relativeTimeRange:
            from: 300
            to: 0
          datasourceUid: prometheus
          model:
            expr: sum(listener_connection_status) + sum(kick_listener_socket_state) + sum(discord_listener_shard_ownership)
            refId: B
        - refId: C
          datasourceUid: __expr__
          model:
            type: math
            refId: C
            expression: "$A == 0 && $B > 0"
      noDataState: OK
      execErrState: Alerting
      for: 1m
      annotations:
        summary: "Message pipeline stalled — no messages consumed while listeners active"
        description: |
          Message processor is not consuming messages but at least one listener is connected.
          Remediation:
          1. Check message-processor logs: kubectl -n allchat logs -l app=message-processor --tail=50
          2. Check Redis stream: redis-cli XLEN chat:raw
          3. Check consumer group lag: redis-cli XINFO GROUPS chat:raw
          4. Restart processor if needed: kubectl -n allchat rollout restart deployment/message-processor
      labels:
        severity: critical
        team: allchat
```

Use the EXACT same YAML structure as existing rules (indentation, field order, datasourceUid casing). UIDs must be globally unique — do not reuse existing UIDs.
  </action>
  <verify>
    <automated>python3 -c "import yaml; data=yaml.safe_load(open('/home/moersener/Hobby/caesar-deployment/apps/platform/kube-prometheus-stack/grafana-alerts/allchat-alerts.yaml')); alerts=yaml.safe_load(data['data']['allchat-alerts.yaml']); groups=[g['name'] for g in alerts['groups']]; assert 'allchat-listener-health' in groups, f'Missing listener-health: {groups}'; assert 'allchat-pipeline-health' in groups, f'Missing pipeline-health: {groups}'; print(f'Alert groups: {groups}')"</automated>
  </verify>
  <acceptance_criteria>
    - allchat-alerts.yaml contains alert group named `allchat-listener-health`
    - allchat-alerts.yaml contains alert group named `allchat-pipeline-health`
    - Listener health group contains rules with UIDs: listener-disconnected-shared, listener-disconnected-kick, listener-disconnected-discord
    - Pipeline health group contains rule with UID: pipeline-stall
    - All rules have `for: 2m` (listener) or `for: 1m` (pipeline) matching locked decisions
    - All rules have `severity: critical` and `team: allchat` labels
    - All rules have `description` annotation with remediation steps (contains "kubectl" command)
    - Existing allchat-critical and allchat-warnings groups are preserved unchanged
  </acceptance_criteria>
  <done>Listener disconnection alerts (3 rules covering shared metrics, kick, discord) and pipeline stall alert created with remediation steps.</done>
</task>

<task type="auto">
  <name>Task 2: Add WebSocket drop and error rate spike alert groups</name>
  <files>../caesar-deployment/apps/platform/kube-prometheus-stack/grafana-alerts/allchat-alerts.yaml</files>
  <read_first>
    - /home/moersener/Hobby/caesar-deployment/apps/platform/kube-prometheus-stack/grafana-alerts/allchat-alerts.yaml
  </read_first>
  <action>
Append two more alert groups to the `groups:` array (after Task 1's additions).

**Alert Group 3: allchat-delivery-health**
```yaml
- orgId: 1
  name: allchat-delivery-health
  folder: All-Chat Alerts
  interval: 1m
  rules:
    - uid: websocket-connections-zero
      title: Zero WebSocket Connections
      condition: B
      data:
        - refId: A
          queryType: instant
          relativeTimeRange:
            from: 300
            to: 0
          datasourceUid: prometheus
          model:
            expr: sum(gateway_websocket_connections_active)
            refId: A
        - refId: B
          datasourceUid: __expr__
          model:
            type: threshold
            refId: B
            conditions:
              - evaluator:
                  params: [1]
                  type: lt
                query:
                  params: [A]
                type: query
            expression: A
      noDataState: OK
      execErrState: Alerting
      for: 2m
      annotations:
        summary: "Zero active WebSocket connections on API Gateway"
        description: |
          No overlay clients are connected via WebSocket. If this is unexpected:
          Remediation:
          1. Check api-gateway logs: kubectl -n allchat logs -l app=api-gateway --tail=50
          2. Verify api-gateway pod is running: kubectl -n allchat get pods -l app=api-gateway
          3. Check ingress: kubectl -n allchat get ingress
          4. Test WebSocket from outside: wscat -c wss://your-domain/ws
      labels:
        severity: warning
        team: allchat

    - uid: websocket-connections-drop
      title: WebSocket Connections Dropped >50%
      condition: B
      data:
        - refId: A
          queryType: instant
          relativeTimeRange:
            from: 300
            to: 0
          datasourceUid: prometheus
          model:
            expr: >
              (sum(gateway_websocket_connections_active) - sum(gateway_websocket_connections_active offset 5m))
              / sum(gateway_websocket_connections_active offset 5m)
            refId: A
        - refId: B
          datasourceUid: __expr__
          model:
            type: threshold
            refId: B
            conditions:
              - evaluator:
                  params: [-0.5]
                  type: lt
                query:
                  params: [A]
                type: query
            expression: A
      noDataState: OK
      execErrState: OK
      for: 2m
      annotations:
        summary: "WebSocket connections dropped >50% in 5 minutes"
        description: |
          Active WebSocket connections decreased by more than 50%. Possible network issue or pod restart.
          Remediation:
          1. Check api-gateway pod events: kubectl -n allchat describe pod -l app=api-gateway
          2. Check recent deployments: kubectl -n allchat rollout history deployment/api-gateway
          3. Check ingress controller logs for connection resets
      labels:
        severity: critical
        team: allchat
```

**Alert Group 4: allchat-error-rates**
```yaml
- orgId: 1
  name: allchat-error-rates
  folder: All-Chat Alerts
  interval: 1m
  rules:
    - uid: error-rate-spike-listeners
      title: Listener Error Rate >5%
      condition: B
      data:
        - refId: A
          queryType: instant
          relativeTimeRange:
            from: 300
            to: 0
          datasourceUid: prometheus
          model:
            expr: >
              sum by (service) (rate(listener_errors_total[5m]))
              / sum by (service) (rate(listener_messages_received_total[5m]))
              > 0.05
            refId: A
        - refId: B
          datasourceUid: __expr__
          model:
            type: threshold
            refId: B
            conditions:
              - evaluator:
                  params: [0]
                  type: gt
                query:
                  params: [A]
                type: query
            expression: A
      noDataState: OK
      execErrState: OK
      for: 3m
      annotations:
        summary: "Listener {{ $labels.service }} error rate exceeds 5%"
        description: |
          Error rate for {{ $labels.service }} is above 5% of messages. Possible API issues or parsing errors.
          Remediation:
          1. Check service logs: kubectl -n allchat logs -l app={{ $labels.service }} --tail=100
          2. Check error categories: query listener_errors_total by error_category label
          3. Check platform API status for upstream issues
      labels:
        severity: warning
        team: allchat

    - uid: error-rate-spike-http
      title: HTTP Service Error Rate >5%
      condition: B
      data:
        - refId: A
          queryType: instant
          relativeTimeRange:
            from: 300
            to: 0
          datasourceUid: prometheus
          model:
            expr: >
              sum by (service) (rate(http_requests_total{status=~"5.."}[5m]))
              / sum by (service) (rate(http_requests_total[5m]))
              > 0.05
            refId: A
        - refId: B
          datasourceUid: __expr__
          model:
            type: threshold
            refId: B
            conditions:
              - evaluator:
                  params: [0]
                  type: gt
                query:
                  params: [A]
                type: query
            expression: A
      noDataState: OK
      execErrState: OK
      for: 3m
      annotations:
        summary: "Service {{ $labels.service }} HTTP error rate exceeds 5%"
        description: |
          HTTP 5xx error rate for {{ $labels.service }} is above 5%.
          Remediation:
          1. Check service logs: kubectl -n allchat logs -l app={{ $labels.service }} --tail=100
          2. Check recent deployments: kubectl -n allchat rollout history deployment/{{ $labels.service }}
          3. Check database connectivity if service uses PostgreSQL
      labels:
        severity: warning
        team: allchat
```

Ensure all UIDs are unique across the entire file. Verify existing UIDs before adding: youtube-quota-critical, youtube-quota-exhausted, youtube-quota-high, pod-crashloop, pod-not-running (plus Task 1's new UIDs).
  </action>
  <verify>
    <automated>python3 -c "import yaml; data=yaml.safe_load(open('/home/moersener/Hobby/caesar-deployment/apps/platform/kube-prometheus-stack/grafana-alerts/allchat-alerts.yaml')); alerts=yaml.safe_load(data['data']['allchat-alerts.yaml']); groups=[g['name'] for g in alerts['groups']]; assert len(groups)==6, f'Expected 6 groups, got {len(groups)}: {groups}'; uids=[r['uid'] for g in alerts['groups'] for r in g['rules']]; assert len(uids)==len(set(uids)), f'Duplicate UIDs found: {[u for u in uids if uids.count(u)>1]}'; print(f'All {len(groups)} groups valid, {len(uids)} unique UIDs')"</automated>
  </verify>
  <acceptance_criteria>
    - allchat-alerts.yaml contains 6 total alert groups (2 existing + 4 new)
    - New groups: allchat-listener-health, allchat-pipeline-health, allchat-delivery-health, allchat-error-rates
    - All rule UIDs are unique (no duplicates across entire file)
    - websocket-connections-zero rule has `for: 2m` and `severity: warning`
    - websocket-connections-drop rule has `for: 2m` and `severity: critical`
    - error-rate-spike-listeners rule has `for: 3m` and `severity: warning`
    - error-rate-spike-http rule has `for: 3m` and `severity: warning`
    - All rules have `team: allchat` label (for notification routing)
    - All rules have `description` annotation containing "Remediation:" and "kubectl" command
    - Existing rules (youtube-quota-critical, youtube-quota-exhausted, youtube-quota-high, pod-crashloop, pod-not-running) are preserved unchanged
  </acceptance_criteria>
  <done>4 new alert groups covering listener disconnections, pipeline stalls, WebSocket drops, and error rate spikes. All with inline remediation steps and correct severity routing.</done>
</task>

</tasks>

<verification>
- YAML parses without errors
- 6 total alert groups (2 existing + 4 new)
- All UIDs unique
- All new rules have remediation steps
- grafana_alert: "1" label preserved on ConfigMap
</verification>

<success_criteria>
4 new alert categories operational: listener disconnections (3 rules), pipeline stalls (1 rule), WebSocket drops (2 rules), error rate spikes (2 rules). Total: 8 new alert rules with inline remediation. Existing 5 alert rules preserved. All stored as code in allchat-alerts.yaml.
</success_criteria>

<output>
After completion, create `.planning/phases/04-grafana-dashboard-audit-metrics-gap-implementation/04-05-SUMMARY.md`
</output>
