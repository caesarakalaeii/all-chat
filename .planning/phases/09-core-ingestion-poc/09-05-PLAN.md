---
phase: 09-core-ingestion-poc
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - services/youtube-listener-innertube/poller/poller_test.go
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Poller tests compile without type errors"
    - "All poller tests pass (8/8)"
  artifacts:
    - path: "services/youtube-listener-innertube/poller/poller_test.go"
      provides: "MockClient with correct GetPollInterval signature returning time.Duration"
      min_lines: 400
  key_links:
    - from: "poller/poller_test.go"
      to: "poller/poller.go"
      via: "ClientInterface"
      pattern: "GetPollInterval.*time\\.Duration"
---

<objective>
Fix poller test compilation error by correcting MockClient.GetPollInterval return type from int to time.Duration.

Purpose: Enable automated verification of polling loop and backoff behavior.
Output: Compilable and passing poller tests.
</objective>

<execution_context>
@/home/moersener/.claude/get-shit-done/workflows/execute-plan.md
@/home/moersener/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-core-ingestion-poc/09-VERIFICATION.md
@.planning/phases/09-core-ingestion-poc/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Fix MockClient GetPollInterval signature</name>
  <files>services/youtube-listener-innertube/poller/poller_test.go</files>
  <action>
Fix the MockClient.GetPollInterval method signature to match the ClientInterface:

**Current (incorrect):**
```go
func (m *MockClient) GetPollInterval(resp *innertube.LiveChatResponse) int {
    return 2000 // milliseconds
}
```

**Change to:**
```go
func (m *MockClient) GetPollInterval(resp *innertube.LiveChatResponse) time.Duration {
    return 2 * time.Second
}
```

**Verification gap:**
- Mock returns `int` but interface expects `time.Duration`
- Prevents compilation of all 8 poller tests
- Blocks automated verification of polling loop and backoff behavior

**Why this fix:**
The ClientInterface (defined in poller/poller.go) specifies `GetPollInterval(*innertube.LiveChatResponse) time.Duration`, but the mock implementation uses `int`. This causes a type mismatch preventing test compilation.

**No other changes needed:**
- All test logic remains unchanged
- Return value semantics stay the same (2 seconds)
- Other mock methods are correct
  </action>
  <verify>
cd services/youtube-listener-innertube && go test ./poller -v

All 8 poller tests should compile and pass:
- TestPoller_SuccessfulPolling
- TestPoller_TransientError
- TestPoller_FatalError
- TestPoller_ContextCancellation
- TestPoller_GracefulShutdown
- TestBackoff_Wait_FatalError
- TestBackoff_Wait_TransientError
- TestBackoffSequence
  </verify>
  <done>
go test ./poller passes with 8/8 tests
Polling loop and backoff behavior verified through automated tests
No compilation errors
  </done>
</task>

</tasks>

<verification>
Run full poller test suite:
```bash
cd services/youtube-listener-innertube
go test ./poller -v -count=1 -timeout=3m
```

Expected: PASS with 8/8 tests, no compilation errors
</verification>

<success_criteria>
- [ ] poller_test.go compiles without type errors
- [ ] All 8 poller tests pass
- [ ] MockClient signature matches ClientInterface exactly
- [ ] Polling loop and backoff behavior fully verifiable via automated tests
</success_criteria>

<output>
After completion, create `.planning/phases/09-core-ingestion-poc/09-05-SUMMARY.md`
</output>
