---
phase: 09-core-ingestion-poc
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - services/youtube-listener-innertube/handlers/health_test.go
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Health handler tests compile without type errors"
    - "All health handler tests pass (6/6)"
  artifacts:
    - path: "services/youtube-listener-innertube/handlers/health_test.go"
      provides: "Mock with correct Ping signature using context.Context"
      min_lines: 156
  key_links:
    - from: "handlers/health_test.go"
      to: "handlers/health.go"
      via: "RedisHealthChecker interface"
      pattern: "Ping\\(ctx context\\.Context\\)"
---

<objective>
Fix health handler test compilation error by correcting mock signature to match RedisHealthChecker interface.

Purpose: Enable automated verification of health check logic and graceful shutdown behavior.
Output: Compilable and passing health handler tests.
</objective>

<execution_context>
@/home/moersener/.claude/get-shit-done/workflows/execute-plan.md
@/home/moersener/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-core-ingestion-poc/09-VERIFICATION.md
@.planning/phases/09-core-ingestion-poc/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Fix mock signature in health handler tests</name>
  <files>services/youtube-listener-innertube/handlers/health_test.go</files>
  <action>
Fix the mockRedisHealthChecker.Ping method signature to match the RedisHealthChecker interface:

**Current (line 18):**
```go
func (m *mockRedisHealthChecker) Ping(ctx interface{}) error {
```

**Change to:**
```go
func (m *mockRedisHealthChecker) Ping(ctx context.Context) error {
```

**Verification gap from 09-VERIFICATION.md:**
- Mock uses `interface{}` instead of `context.Context` parameter
- Prevents compilation of health handler tests
- Blocks verification of Truth #4: graceful shutdown

**Why this fix:**
The RedisHealthChecker interface (in handlers/health.go:13) defines `Ping(ctx context.Context) error`, but the mock implementation uses `interface{}`. This causes a type mismatch preventing test compilation.

**No other changes needed:**
- Mock behavior logic remains unchanged (check shouldFail flag)
- All test cases are correct (they just need the code to compile)
- The actual health.go implementation is correct (uses context.Context properly)
  </action>
  <verify>
cd services/youtube-listener-innertube && go test ./handlers -v

All 6 health handler tests should pass:
- TestLivenessProbe_AlwaysReturns200
- TestReadinessProbe_Returns503_WhenRedisUnavailable
- TestReadinessProbe_Returns503_WhenInnerTubeNotInitialized
- TestReadinessProbe_Returns200_WhenAllChecksPass
- TestReadinessProbe_NilInnerTubeChecker
  </verify>
  <done>
go test ./handlers passes with 6/6 tests
Health check logic verified through automated tests
No compilation errors
  </done>
</task>

</tasks>

<verification>
Run full health handler test suite:
```bash
cd services/youtube-listener-innertube
go test ./handlers -v -count=1
```

Expected: PASS with 6/6 tests, no compilation errors
</verification>

<success_criteria>
- [ ] health_test.go compiles without type errors
- [ ] All 6 health handler tests pass
- [ ] Mock signature matches RedisHealthChecker interface exactly
- [ ] Truth #4 from ROADMAP.md now fully verifiable via automated tests
</success_criteria>

<output>
After completion, create `.planning/phases/09-core-ingestion-poc/09-04-SUMMARY.md`
</output>
