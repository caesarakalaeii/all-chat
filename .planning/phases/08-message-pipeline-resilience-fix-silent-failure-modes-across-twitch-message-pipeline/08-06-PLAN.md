---
phase: 08-message-pipeline-resilience-fix-silent-failure-modes-across-twitch-message-pipeline
plan: 06
type: execute
wave: 2
depends_on: [08-04]
files_modified:
  - services/twitch-listener/publisher/stream_publisher.go
  - services/twitch-listener/publisher/stream_publisher_test.go
  - services/kick-listener/publisher/redis.go
  - services/kick-listener/publisher/redis_test.go
  - services/youtube-listener-innertube/publisher/redis_publisher.go
  - services/youtube-listener-innertube/publisher/redis_publisher_test.go
  - services/discord-listener/publisher/stream_publisher.go
  - services/discord-listener/publisher/stream_publisher_test.go
  - services/twitch-eventsub-listener/publisher/stream_publisher.go
  - services/twitch-eventsub-listener/publisher/stream_publisher_test.go
autonomous: true
requirements: [D-01, D-07]
must_haves:
  truths:
    - "twitch-listener wraps its StreamPublisher.Publish with RingBufferPublisher"
    - "kick-listener wraps its StreamPublisher.Publish with RingBufferPublisher"
    - "youtube-listener-innertube wraps its StreamPublisher.Publish with RingBufferPublisher"
    - "discord-listener wraps its StreamPublisher.Publish with RingBufferPublisher"
    - "twitch-eventsub-listener wraps its StreamPublisher.Publish with RingBufferPublisher"
    - "Each listener creates RingBufferPublisher with capacity 1000 and its service name for metrics"
    - "Each listener calls RingBufferPublisher.Stop() during graceful shutdown"
    - "LI-01, LI-02, LI-03 failure modes are eliminated — no silent XADD drops"
  artifacts:
    - path: "services/twitch-listener/publisher/stream_publisher.go"
      provides: "RingBufferPublisher integration for Twitch IRC messages"
      contains: "RingBufferPublisher"
    - path: "services/kick-listener/publisher/redis.go"
      provides: "RingBufferPublisher integration for Kick WebSocket messages"
      contains: "RingBufferPublisher"
    - path: "services/youtube-listener-innertube/publisher/redis_publisher.go"
      provides: "RingBufferPublisher integration for InnerTube messages"
      contains: "RingBufferPublisher"
    - path: "services/discord-listener/publisher/stream_publisher.go"
      provides: "RingBufferPublisher integration for Discord messages"
      contains: "RingBufferPublisher"
    - path: "services/twitch-eventsub-listener/publisher/stream_publisher.go"
      provides: "RingBufferPublisher integration for EventSub messages"
      contains: "RingBufferPublisher"
  key_links:
    - from: "services/twitch-listener/publisher/stream_publisher.go"
      to: "shared/listener/ring_buffer.go"
      via: "NewRingBufferPublisher wrapping XADD"
      pattern: "listener.NewRingBufferPublisher"
    - from: "services/kick-listener/publisher/redis.go"
      to: "shared/listener/ring_buffer.go"
      via: "NewRingBufferPublisher wrapping XADD"
      pattern: "listener.NewRingBufferPublisher"
---

<objective>
Wire RingBufferPublisher from shared/listener SDK into all 5 Go listeners that publish to Redis Streams, completing D-07 ("so all Go listeners get it") and eliminating LI-01, LI-02, and LI-03 failure modes.

Purpose: Plan 08-04 creates the RingBufferPublisher but no listener uses it yet. Without wiring, XADD failures in listeners still silently drop messages. This plan closes that gap.

Output: All 5 Go listeners (twitch, kick, youtube-innertube, discord, twitch-eventsub) use RingBufferPublisher for XADD operations. tiktok-listener is Node.js and out of scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-message-pipeline-resilience-fix-silent-failure-modes-across-twitch-message-pipeline/08-CONTEXT.md
@.planning/phases/08-message-pipeline-resilience-fix-silent-failure-modes-across-twitch-message-pipeline/08-04-SUMMARY.md

<interfaces>
From shared/listener/ring_buffer.go (created by Plan 08-04):
```go
type PublishFunc func(ctx context.Context, payload []byte) error

type RingBufferPublisher struct { ... }

func NewRingBufferPublisher(capacity int, publishFn PublishFunc, logger *zap.Logger, serviceName string) *RingBufferPublisher
func (rb *RingBufferPublisher) Publish(ctx context.Context, payload []byte) error
func (rb *RingBufferPublisher) Stop()
```

From services/twitch-listener/publisher/stream_publisher.go (typical pattern):
```go
type StreamPublisher struct {
    client *redis.Client
    logger *zap.Logger
}

func (p *StreamPublisher) Publish(ctx context.Context, msg *models.RawChatMessage) error {
    data, _ := json.Marshal(msg)
    return p.client.XAdd(ctx, &redis.XAddArgs{
        Stream: "chat:raw",
        Values: map[string]interface{}{"data": string(data)},
    }).Err()
}
```

Each listener's StreamPublisher follows this same pattern: marshal message to JSON, XADD to chat:raw. The RingBufferPublisher wraps the raw XADD call (after marshal) so that if XADD fails, the serialized payload is buffered for retry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire RingBufferPublisher into twitch-listener and kick-listener</name>
  <files>
    services/twitch-listener/publisher/stream_publisher.go,
    services/twitch-listener/publisher/stream_publisher_test.go,
    services/twitch-listener/cmd/main.go,
    services/kick-listener/publisher/redis.go,
    services/kick-listener/publisher/redis_test.go,
    services/kick-listener/cmd/main.go
  </files>
  <read_first>
    services/twitch-listener/publisher/stream_publisher.go,
    services/twitch-listener/cmd/main.go,
    services/kick-listener/publisher/redis.go,
    services/kick-listener/cmd/main.go,
    shared/listener/ring_buffer.go
  </read_first>
  <behavior>
    - twitch-listener StreamPublisher has a ringBuffer *listener.RingBufferPublisher field
    - twitch-listener NewStreamPublisher creates RingBufferPublisher wrapping the raw XADD call with capacity 1000 and serviceName "twitch-listener"
    - twitch-listener Publish serializes message then delegates to ringBuffer.Publish instead of direct XADD
    - twitch-listener StreamPublisher.Close (or Stop) calls ringBuffer.Stop()
    - kick-listener follows identical pattern with serviceName "kick-listener"
    - Both listeners' go.mod updated to reference shared/listener module
    - Existing tests continue to pass; new tests verify ring buffer integration
  </behavior>
  <action>
    **Pattern for each listener (twitch-listener shown, kick-listener identical):**

    **Modify `services/twitch-listener/publisher/stream_publisher.go`:**

    1. Add import for `"github.com/your-org/all-chat/shared/listener"` (use actual module path from go.mod).

    2. Add `ringBuffer *listener.RingBufferPublisher` field to StreamPublisher struct.

    3. In constructor (NewStreamPublisher or equivalent), create the ring buffer wrapping the raw XADD:
       ```go
       rawPublish := func(ctx context.Context, payload []byte) error {
           return p.client.XAdd(ctx, &redis.XAddArgs{
               Stream: "chat:raw",
               MaxLen: 10000,
               Approx: true,
               Values: map[string]interface{}{"data": string(payload)},
           }).Err()
       }
       p.ringBuffer = listener.NewRingBufferPublisher(1000, rawPublish, logger, "twitch-listener")
       ```

    4. Modify `Publish` method to marshal then delegate:
       ```go
       func (p *StreamPublisher) Publish(ctx context.Context, msg *models.RawChatMessage) error {
           data, err := json.Marshal(msg)
           if err != nil {
               return fmt.Errorf("failed to marshal message: %w", err)
           }
           return p.ringBuffer.Publish(ctx, data)
       }
       ```

    5. Add `Stop()` method (or extend existing Close):
       ```go
       func (p *StreamPublisher) Stop() {
           if p.ringBuffer != nil {
               p.ringBuffer.Stop()
           }
       }
       ```

    6. Update `cmd/main.go` to call `publisher.Stop()` during graceful shutdown.

    **For PublishBatch (twitch-listener, youtube-innertube):** The batch method should iterate messages and call `p.ringBuffer.Publish` for each — the ring buffer handles individual failures. This replaces the pipeline approach, which was already problematic per LI-02.

    **Repeat identical pattern for kick-listener** using serviceName "kick-listener". kick-listener's publisher is in `publisher/redis.go`.

    **Tests:**
    - TestStreamPublisherPublishWithRingBuffer: Verify that when XADD fails, Publish returns nil (buffered) not error
    - TestStreamPublisherStopDrainsBuffer: Verify Stop calls through to ring buffer
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/services/twitch-listener && go test ./publisher/... -v -timeout 30s && cd /home/caesar/git/all-chat/services/kick-listener && go test ./publisher/... -v -timeout 30s</automated>
  </verify>
  <done>twitch-listener and kick-listener wrap XADD with RingBufferPublisher. Failed publishes are buffered and retried instead of silently dropped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire RingBufferPublisher into youtube-innertube, discord, and eventsub listeners</name>
  <files>
    services/youtube-listener-innertube/publisher/redis_publisher.go,
    services/youtube-listener-innertube/publisher/redis_publisher_test.go,
    services/youtube-listener-innertube/cmd/main.go,
    services/discord-listener/publisher/stream_publisher.go,
    services/discord-listener/publisher/stream_publisher_test.go,
    services/discord-listener/cmd/main.go,
    services/twitch-eventsub-listener/publisher/stream_publisher.go,
    services/twitch-eventsub-listener/publisher/stream_publisher_test.go,
    services/twitch-eventsub-listener/cmd/main.go
  </files>
  <read_first>
    services/youtube-listener-innertube/publisher/redis_publisher.go,
    services/youtube-listener-innertube/cmd/main.go,
    services/discord-listener/publisher/stream_publisher.go,
    services/discord-listener/cmd/main.go,
    services/twitch-eventsub-listener/publisher/stream_publisher.go,
    services/twitch-eventsub-listener/cmd/main.go,
    shared/listener/ring_buffer.go
  </read_first>
  <behavior>
    - youtube-listener-innertube StreamPublisher wraps XADD with RingBufferPublisher, serviceName "youtube-listener-innertube"
    - discord-listener StreamPublisher wraps XADD with RingBufferPublisher, serviceName "discord-listener"
    - twitch-eventsub-listener StreamPublisher wraps XADD with RingBufferPublisher, serviceName "twitch-eventsub-listener"
    - All three call ringBuffer.Stop() during graceful shutdown
    - All three listeners' go.mod updated to reference shared/listener module
    - Existing tests continue to pass
  </behavior>
  <action>
    Apply the identical RingBufferPublisher wiring pattern from Task 1 to the remaining 3 Go listeners:

    **youtube-listener-innertube** (`publisher/redis_publisher.go`):
    - serviceName: "youtube-listener-innertube"
    - This listener has both `Publish` and `PublishBatch` — both should use ring buffer
    - The `publishToRedis` internal method is the raw XADD call — wrap this

    **discord-listener** (`publisher/stream_publisher.go`):
    - serviceName: "discord-listener"
    - Same pattern as twitch-listener

    **twitch-eventsub-listener** (`publisher/stream_publisher.go`):
    - serviceName: "twitch-eventsub-listener"
    - Same pattern as twitch-listener

    For each:
    1. Add `ringBuffer *listener.RingBufferPublisher` field
    2. Create ring buffer in constructor wrapping raw XADD
    3. Modify Publish to marshal then delegate to ringBuffer.Publish
    4. Add Stop() method
    5. Update cmd/main.go graceful shutdown to call publisher.Stop()
    6. Update go.mod if needed to import shared/listener

    **Note:** youtube-listener (quota-based, not deployed to production per memory) is NOT included — only youtube-listener-innertube is wired.

    **Tests:**
    - Each listener gets a TestStreamPublisherPublishWithRingBuffer test verifying buffer-on-failure behavior
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/services/youtube-listener-innertube && go test ./publisher/... -v -timeout 30s && cd /home/caesar/git/all-chat/services/discord-listener && go test ./publisher/... -v -timeout 30s && cd /home/caesar/git/all-chat/services/twitch-eventsub-listener && go test ./publisher/... -v -timeout 30s</automated>
  </verify>
  <done>All 5 Go listeners (twitch, kick, youtube-innertube, discord, twitch-eventsub) wrap XADD with RingBufferPublisher per D-07. LI-01, LI-02, LI-03 failure modes eliminated across all Go listeners. tiktok-listener (Node.js) is out of scope.</done>
</task>

</tasks>

<verification>
- `cd /home/caesar/git/all-chat && make build-all` — all services compile with ring buffer integration
- grep for `RingBufferPublisher` in each listener's publisher directory returns results
- grep for `listener.NewRingBufferPublisher` across services/ returns 5 results (one per Go listener)
- Each listener's cmd/main.go contains `Stop()` call for ring buffer cleanup
- All listener tests pass: `go test ./publisher/... -timeout 30s` in each service directory
</verification>

<success_criteria>
All 5 Go listeners use RingBufferPublisher for XADD operations. Failed publishes are buffered (up to 1000 messages) and retried every 500ms instead of being silently dropped. Each listener reports ring_buffer_depth and ring_buffer_drops_total metrics with its service name. Graceful shutdown drains or stops the ring buffer. LI-01, LI-02, and LI-03 are fully eliminated.
</success_criteria>

<output>
After completion, create `.planning/phases/08-message-pipeline-resilience-fix-silent-failure-modes-across-twitch-message-pipeline/08-06-SUMMARY.md`
</output>
