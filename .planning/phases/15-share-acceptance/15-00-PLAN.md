---
phase: 15-share-acceptance
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - services/share-service/cycles/detector_test.go
  - services/share-service/handlers/shares_test.go
  - frontend/src/app/dashboard/shares/components/AcceptModal.test.tsx
  - frontend/src/app/dashboard/shares/components/AddSourceModal.test.tsx
  - services/api-gateway/handlers/websocket_test.go
  - services/message-processor/dedup/dedup_test.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "Test stubs exist for all production code in phase"
    - "All test files compile and pass (as empty stubs)"
  artifacts:
    - path: "services/share-service/cycles/detector_test.go"
      provides: "Test scaffolding for cycle detection"
      min_lines: 20
    - path: "services/share-service/handlers/shares_test.go"
      provides: "Test scaffolding for acceptance endpoint"
      min_lines: 20
    - path: "frontend/src/app/dashboard/shares/components/AcceptModal.test.tsx"
      provides: "Test scaffolding for AcceptModal"
      min_lines: 20
    - path: "frontend/src/app/dashboard/shares/components/AddSourceModal.test.tsx"
      provides: "Test scaffolding for AddSourceModal"
      min_lines: 20
  key_links: []
---

<objective>
Create test stubs for all phase 15 production code to enable continuous validation during implementation.

Purpose: Satisfy Nyquist Rule by establishing automated verification points before implementing production code.

Output: Minimal test scaffolds that compile and pass, ready for TDD implementation in subsequent waves.
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-share-acceptance/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend test stubs</name>
  <files>
    services/share-service/cycles/detector_test.go
    services/share-service/handlers/shares_test.go
    services/api-gateway/handlers/websocket_test.go
    services/message-processor/dedup/dedup_test.go
  </files>
  <action>
Create minimal test scaffolds for all backend code in phase 15:

**detector_test.go** (cycle detection tests):
```go
package cycles

import (
	"testing"
)

func TestCycleDetector_HasCycle(t *testing.T) {
	t.Skip("Wave 0 stub - implement in Wave 1")
}

func TestCycleDetector_DirectCycle(t *testing.T) {
	t.Skip("Wave 0 stub - implement in Wave 1")
}

func TestCycleDetector_IndirectCycle(t *testing.T) {
	t.Skip("Wave 0 stub - implement in Wave 1")
}
```

**shares_test.go** (acceptance endpoint tests):
```go
package handlers

import (
	"testing"
)

func TestAcceptShare_ValidAcceptance(t *testing.T) {
	t.Skip("Wave 0 stub - implement in Wave 1")
}

func TestAcceptShare_CycleDetection(t *testing.T) {
	t.Skip("Wave 0 stub - implement in Wave 1")
}

func TestAcceptShare_ExpiryValidation(t *testing.T) {
	t.Skip("Wave 0 stub - implement in Wave 1")
}
```

**websocket_test.go** (notification tests):
```go
package handlers

import (
	"testing"
)

func TestNotifyUser(t *testing.T) {
	t.Skip("Wave 0 stub - implement in Wave 2")
}
```

**dedup_test.go** (deduplication tests):
```go
package dedup

import (
	"testing"
)

func TestDeduplicator_IsDuplicateForOverlay(t *testing.T) {
	t.Skip("Wave 0 stub - implement in Wave 2")
}

func TestDeduplicator_OverlayIsolation(t *testing.T) {
	t.Skip("Wave 0 stub - implement in Wave 2")
}
```

Each stub uses `t.Skip()` to pass without implementation, satisfying Nyquist Rule.
  </action>
  <verify>
    <automated>go test ./services/share-service/cycles -v && go test ./services/share-service/handlers -v && go test ./services/api-gateway/handlers -v && go test ./services/message-processor/dedup -v</automated>
  </verify>
  <done>All backend test files exist, compile successfully, and pass with skip markers</done>
</task>

<task type="auto">
  <name>Task 2: Create frontend test stubs</name>
  <files>
    frontend/src/app/dashboard/shares/components/AcceptModal.test.tsx
    frontend/src/app/dashboard/shares/components/AddSourceModal.test.tsx
  </files>
  <action>
Create minimal test scaffolds for frontend components:

**AcceptModal.test.tsx**:
```typescript
import { describe, it, expect } from 'vitest';

describe('AcceptModal', () => {
  it.skip('renders with overlay dropdown and expiry options', () => {
    // Wave 0 stub - implement in Wave 1
  });

  it.skip('validates custom hours range (1-168)', () => {
    // Wave 0 stub - implement in Wave 1
  });

  it.skip('calls onAccepted with sender_overlay_id on success', () => {
    // Wave 0 stub - implement in Wave 1
  });
});
```

**AddSourceModal.test.tsx**:
```typescript
import { describe, it, expect } from 'vitest';

describe('AddSourceModal', () => {
  it.skip('displays sender name and overlay preview', () => {
    // Wave 0 stub - implement in Wave 1
  });

  it.skip('calls API when Add clicked', () => {
    // Wave 0 stub - implement in Wave 1
  });

  it.skip('closes without action when Skip clicked', () => {
    // Wave 0 stub - implement in Wave 1
  });
});
```

Each stub uses `it.skip()` to pass without implementation.
  </action>
  <verify>
    <automated>npm test -- AcceptModal.test.tsx AddSourceModal.test.tsx</automated>
  </verify>
  <done>All frontend test files exist and pass with skip markers</done>
</task>

</tasks>

<verification>
Run full test suite to confirm all stubs pass:

```bash
# Backend
make test-backend

# Frontend
npm test

# All stubs should show as "skipped" not "failed"
```
</verification>

<success_criteria>
- All 6 test files created and committed
- Backend tests compile and pass (with t.Skip)
- Frontend tests compile and pass (with it.skip)
- Zero test failures in phase 15 test files
- VALIDATION.md can be updated to nyquist_compliant: true
</success_criteria>

<output>
After completion, create `.planning/phases/15-share-acceptance/15-00-SUMMARY.md`
</output>
