---
phase: 03-kick-integration-edge-cases
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api-gateway/replay/buffer_test.go
autonomous: true
requirements:
  - REL-04
gap_closure: true

must_haves:
  truths:
    - "Replay buffer unit tests compile without errors"
    - "Test coverage for replay buffer operations can be measured"
    - "All test cases execute successfully validating edge cases"
  artifacts:
    - path: "services/api-gateway/replay/buffer_test.go"
      provides: "Working unit tests for replay buffer"
      min_lines: 230
      compiles: true
  key_links:
    - from: "services/api-gateway/replay/buffer_test.go"
      to: "services/api-gateway/replay/buffer.go"
      via: "DeletionEvent struct field reference"
      pattern: "DeletionType:"
---

<objective>
Fix replay buffer unit test compilation error blocking test validation.

Purpose: The verification report found that buffer_test.go uses the wrong field name (`Type` instead of `DeletionType`) in 9 locations, causing compilation failure. This prevents running tests, measuring coverage, and validating replay buffer edge cases.

Output: Working unit tests that compile and run, allowing verification of 88.5% test coverage claim and replay buffer functionality.
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@services/api-gateway/replay/buffer.go
@services/api-gateway/replay/buffer_test.go
@.planning/phases/03-kick-integration-edge-cases/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Fix DeletionEvent field name in buffer_test.go</name>
  <files>services/api-gateway/replay/buffer_test.go</files>
  <action>
Update all 9 occurrences of `Type:` field references to `DeletionType:` in buffer_test.go to match the DeletionEvent struct definition in buffer.go.

Affected lines (from verification report):
- Line 32: event1 initialization
- Line 38: event2 initialization
- Line 44: event3 initialization
- Line 74: event in TestReplayBuffer_ExclusiveTimestampRange
- Line 103: event in TestReplayBuffer_TTL
- Line 148: event1 in TestReplayBuffer_Prune
- Line 154: event2 in TestReplayBuffer_Prune
- Line 186: event1 in TestReplayBuffer_Isolation
- Line 192: event2 in TestReplayBuffer_Isolation

The struct definition in buffer.go line 15 clearly defines:
```go
DeletionType string `json:"deletion_type"` // "single", "batch", "clear"
```

Replace each occurrence of:
```go
Type: "single"
Type: "batch"
```

With:
```go
DeletionType: "single"
DeletionType: "batch"
```

After the fix, the test file should compile without errors and all test cases should execute.
  </action>
  <verify>
Run Go tests for the replay package and confirm compilation success:
```bash
cd /home/caesar/git/all-chat/services/api-gateway
go test -v ./replay/... -coverprofile=coverage.out
go tool cover -func=coverage.out | grep "replay/buffer.go"
```

Expected output:
- All tests pass (PASS)
- No compilation errors
- Coverage report shows percentage for buffer.go
  </verify>
  <done>
- buffer_test.go compiles without errors
- All 5 test functions execute successfully
- Test coverage can be measured and reported
- Verification gap #12 resolved (load test dependencies can now be validated)
  </done>
</task>

</tasks>

<verification>
1. Compilation check: `go build ./services/api-gateway/replay/...` succeeds
2. Test execution: `go test ./services/api-gateway/replay/...` passes all tests
3. Coverage measurement: Coverage report generated and shows buffer.go coverage percentage
4. Field consistency: All DeletionEvent struct initialization uses `DeletionType` field
</verification>

<success_criteria>
- Replay buffer unit tests compile without errors
- All test cases pass (TestReplayBuffer_AddAndGetSince, TestReplayBuffer_ExclusiveTimestampRange, TestReplayBuffer_TTL, TestReplayBuffer_Prune, TestReplayBuffer_Isolation)
- Test coverage for replay buffer can be measured and reported
- Verification gap #12 ("Load test validates 1,000+ message batch deletion completes without UI blocking") dependency resolved
</success_criteria>

<output>
After completion, create `.planning/phases/03-kick-integration-edge-cases/03-04-SUMMARY.md`
</output>
