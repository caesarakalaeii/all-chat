---
phase: 18-revocation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - services/share-service/handlers/shares_revoke_test.go
autonomous: true
requirements:
  - SHARE-06
  - SHARE-07
must_haves:
  truths:
    - "Test file exists and compilation fails with undefined: RevokeShareRequest (RED gate)"
    - "Test stubs cover auth check (403 when caller is neither sender nor recipient)"
    - "Test stubs cover status check (409 when share is not accepted)"
    - "Test stubs cover source deactivation assertion (repo method called with share_id)"
  artifacts:
    - path: "services/share-service/handlers/shares_revoke_test.go"
      provides: "RED test stubs for revocation handler"
      contains: "TestRevokeShareRequest"
  key_links:
    - from: "shares_revoke_test.go"
      to: "handlers.ShareHandler.RevokeShareRequest"
      via: "direct call in test"
      pattern: "RevokeShareRequest"
---

<objective>
Create test stubs for the revocation handler so that Wave 1 has RED tests to turn GREEN.

Purpose: Nyquist compliance — every code-producing task in Wave 1 must have a pre-existing test file that fails (or compiles but has failing assertions).
Output: `shares_revoke_test.go` with stubs for SHARE-06 auth check, status check, and SHARE-07 source deactivation assertion.
</objective>

<execution_context>
@/home/caesar/.claude/get-shit-done/workflows/execute-plan.md
@/home/caesar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-revocation/18-CONTEXT.md
@.planning/phases/18-revocation/18-RESEARCH.md

<interfaces>
<!-- Existing handler structure to stub against -->
From services/share-service/handlers/shares.go:
```go
// ShareHandler struct — executor must add RevokeShareRequest method
type ShareHandler struct {
    shareRepo     *repository.ShareRepository
    userRepo      *repository.UserSearchRepository
    db            *pgxpool.Pool
    logger        *zap.Logger
    cycleDetector CycleDetector
}
```

From services/share-service/models/share_request.go:
```go
const (
    StatusPending  = "pending"
    StatusAccepted = "accepted"
    StatusRejected = "rejected"
    StatusExpired  = "expired"
    // StatusRevoked = "revoked"  <-- Wave 1 adds this
)
```

Existing test pattern (look at services/share-service/handlers/ for existing *_test.go files for mock patterns).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shares_revoke_test.go with RED stubs</name>
  <files>services/share-service/handlers/shares_revoke_test.go</files>
  <action>
Create `services/share-service/handlers/shares_revoke_test.go` with test stubs that produce a RED state because `RevokeShareRequest` does not yet exist on `ShareHandler`.

Use the stub-based pattern established in Phase 17: a mockRevokeRepo (or reuse existing mock patterns in the handlers package) that records whether `RevokeShare` was called.

Write these four test functions (stubs that t.Fatal with "not implemented" or reference the non-existent method causing compile failure):

1. `TestRevokeShareRequest_AuthCheck` — caller is neither sender nor recipient → expect 403
2. `TestRevokeShareRequest_StatusCheck` — share has status != 'accepted' → expect 409
3. `TestRevokeShareRequest_Success` — valid caller + accepted share → expect 200, assert RevokeShare called
4. `TestRevokeShareRequest_SourceDeactivation` — on success, assert overlay_chat_sources updated (mock repo records call)

The RED gate: reference `shareHandler.RevokeShareRequest` which does not exist yet — this causes a compile error, satisfying the Nyquist RED requirement. Do NOT use t.Skip.

Follow the existing test file patterns in the package (check services/share-service/handlers/ for existing test setup, mock structures, and gin.Context construction via httptest).
  </action>
  <verify>
    <automated>cd /home/caesar/git/all-chat/services/share-service && go test ./handlers/... -run TestRevokeShareRequest 2>&1 | grep -E "undefined|RevokeShareRequest|FAIL"</automated>
  </verify>
  <done>go test ./handlers/... -run TestRevokeShareRequest outputs a compile error referencing undefined RevokeShareRequest — RED gate confirmed</done>
</task>

</tasks>

<verification>
RED state confirmed: `go test ./handlers/... -run TestRevokeShareRequest` fails with compile error "undefined: RevokeShareRequest" or similar.
</verification>

<success_criteria>
- `shares_revoke_test.go` exists in `services/share-service/handlers/`
- Running `go test ./handlers/... -run TestRevokeShareRequest` produces a compile-time RED failure referencing the missing `RevokeShareRequest` method
- Test names match the VALIDATION.md per-task verification map
</success_criteria>

<output>
After completion, create `.planning/phases/18-revocation/18-00-SUMMARY.md`
</output>
